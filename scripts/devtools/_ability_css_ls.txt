.ability-modal-mask{
    position:fixed;inset:0;z-index:9990;
    display:flex;align-items:center;justify-content:center;
    background:rgba(3,7,18,.78);
    backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);
    animation:abilityMaskIn .25s ease;
}
@keyframes abilityMaskIn{from{opacity:0}to{opacity:1}}
.ability-modal{
    width:calc(100vw - 40px);
    height:calc(100vh - 24px);
    max-width:1880px;
    display:flex;flex-direction:column;
    background:linear-gradient(160deg,rgba(15,22,44,.96),rgba(8,12,28,.98));
    border:1px solid rgba(212,175,55,.28);
    border-radius:18px;
    box-shadow:0 24px 80px rgba(0,0,0,.6),0 0 60px rgba(99,102,241,.12),inset 0 1px 0 rgba(255,255,255,.06);
    overflow:hidden;
    animation:abilityModalIn .3s cubic-bezier(.2,.8,.3,1);
}
@keyframes abilityModalIn{from{opacity:0;transform:translateY(18px) scale(.97)}to{opacity:1;transform:none}}
.ability-modal-head{
    display:flex;align-items:center;justify-content:space-between;
    padding:12px 20px;
    border-bottom:1px solid rgba(212,175,55,.18);
    background:rgba(255,255,255,.02);
    flex-shrink:0;
}
.ability-modal-title{display:flex;align-items:center;gap:10px;color:#FFE9B0;font-size:17px;font-weight:700;letter-spacing:.5px}
.ability-modal-title svg{width:20px;height:20px;color:#F0C75C;filter:drop-shadow(0 0 6px rgba(212,175,55,.6))}
.ability-modal-close{
    width:32px;height:32px;border-radius:8px;border:1px solid rgba(255,255,255,.14);
    background:rgba(255,255,255,.05);color:rgba(255,255,255,.75);font-size:14px;cursor:pointer;
    display:flex;align-items:center;justify-content:center;transition:all .2s;
}
.ability-modal-close:hover{background:rgba(255,80,80,.18);border-color:rgba(255,120,120,.5);color:#fff}
.ability-modal-body{flex:1;min-height:0;position:relative;display:flex;flex-direction:column}

/* 通用状态（loading / error / empty） */
.ability-state{
    flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:14px;
    color:rgba(255,255,255,.85);padding:30px;text-align:center;
}
.ability-loading-ring{
    width:44px;height:44px;border-radius:50%;
    border:3px solid rgba(212,175,55,.18);border-top-color:#F0C75C;
    animation:abilitySpin .8s linear infinite;
    box-shadow:0 0 24px rgba(212,175,55,.25);
}
@keyframes abilitySpin{to{transform:rotate(360deg)}}
.ability-state-title{font-size:17px;font-weight:700;color:#FFE9B0;line-height:1.6}
.ability-state-text{font-size:13px;color:rgba(255,255,255,.55);line-height:1.8}
.ability-state-icon{font-size:34px;color:#F59E0B}
.ability-empty-icon svg{width:52px;height:52px;color:#F0C75C;filter:drop-shadow(0 0 12px rgba(212,175,55,.5))}

/* 能力问卷 */
.ability-survey{display:flex;flex-direction:column;flex:1;min-height:0;position:relative}
.ability-survey-head{
    display:flex;align-items:center;justify-content:space-between;
    padding:10px 20px;flex-shrink:0;
    border-bottom:1px solid rgba(255,255,255,.06);
}
.ability-survey-tip{font-size:15px;font-weight:700;color:#fff}
.ability-survey-sub{display:block;margin-top:2px;font-size:12px;font-weight:400;color:rgba(255,255,255,.45)}
.ability-survey-count{
    font-size:13px;color:rgba(255,255,255,.7);padding:6px 14px;white-space:nowrap;
    border:1px solid rgba(212,175,55,.35);border-radius:20px;
    background:rgba(212,175,55,.1);
}
.ability-survey-count b{color:#FFE9B0;font-size:15px}
.ability-survey-body{
    flex:1;min-height:0;overflow-y:auto;padding:12px 20px;
    display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;
    align-content:stretch;
}
.ability-cat-block{
    display:flex;flex-direction:column;min-width:0;
    background:rgba(255,255,255,.03);
    border:1px solid rgba(255,255,255,.09);
    border-radius:12px;padding:10px 12px;
    transition:border-color .2s,box-shadow .2s;
}
.ability-cat-block:hover{border-color:rgba(240,199,92,.35);box-shadow:0 0 16px rgba(212,175,55,.08)}
.ability-cat-title{
    font-size:14px;font-weight:700;color:var(--cat-color,#8B8B9E);
    margin-bottom:8px;padding-bottom:6px;border-bottom:1px solid rgba(255,255,255,.08);
    display:flex;align-items:center;gap:6px;flex-shrink:0;
}
.ability-cat-title::before{content:'◆';font-size:10px;opacity:.9}
.ability-cat-techs{
    display:flex;flex-wrap:wrap;gap:7px;align-content:flex-start;flex:1;
}
.ability-tech-chip{
    position:relative;padding:7px 13px 7px 30px;
    font-size:14px;font-weight:500;color:rgba(255,255,255,.88);
    background:rgba(255,255,255,.05);
    border:1px solid rgba(255,255,255,.13);
    border-radius:8px;cursor:pointer;user-select:none;white-space:nowrap;
    transition:all .18s ease;
}
.ability-tech-chip::before{
    content:'';position:absolute;left:10px;top:50%;transform:translateY(-50%);
    width:11px;height:11px;border-radius:50%;
    border:1.5px solid rgba(255,255,255,.35);
    background:transparent;transition:all .18s ease;
}
.ability-tech-chip:hover{border-color:rgba(240,199,92,.55);box-shadow:0 0 10px rgba(240,199,92,.18);transform:translateY(-1px)}
.ability-tech-chip.selected{
    color:#fff;font-weight:700;
    background:linear-gradient(135deg,var(--cat-color,#8B8B9E),rgba(20,18,15,.9));
    border-color:var(--cat-color,#8B8B9E);
    box-shadow:0 0 12px -2px var(--cat-color,#8B8B9E),0 0 22px -6px var(--cat-color,#8B8B9E),inset 0 0 6px rgba(255,255,255,.12);
}
.ability-tech-chip.selected::before{border-color:#fff;background:#fff;box-shadow:0 0 6px rgba(255,255,255,.8)}
.ability-tech-chip.selected::after{
    content:'✓';position:absolute;left:9px;top:50%;transform:translateY(-50%);
    color:#1B2238;font-size:9px;font-weight:900;
}
.ability-survey-foot{
    flex-shrink:0;display:flex;align-items:center;justify-content:flex-end;gap:12px;
    padding:10px 20px;
    border-top:1px solid rgba(255,255,255,.06);
    background:rgba(255,255,255,.02);
}
.ability-confirm-btn{min-width:210px;justify-content:center}
/* 重置按钮：低强调警示色，半透明背景 + 发光边框，与深色科技感统一 */
.ability-reset-btn{
    min-width:96px;justify-content:center;position:relative;
    color:#E8A66B;
    background:rgba(232,166,107,.07);
    border:1px solid rgba(232,166,107,.32);
    box-shadow:0 0 12px -4px rgba(232,166,107,.35),inset 0 0 8px rgba(232,166,107,.05);
    transition:all .2s ease;
}
.ability-reset-btn::before{
    content:'↻';margin-right:5px;display:inline-block;font-size:14px;line-height:1;
    transition:transform .45s cubic-bezier(.3,.7,.3,1);
}
.ability-reset-btn:hover{
    color:#FFD9A8;
    background:rgba(232,166,107,.14);
    border-color:rgba(240,150,80,.65);
    box-shadow:0 0 16px -2px rgba(232,166,107,.5);
    transform:translateY(-1px);
}
.ability-reset-btn:hover::before{transform:rotate(180deg)}
.ability-reset-btn:active{transform:scale(.96)}
/* 重置二次确认层：覆盖当前问卷，轻量提示，不刷新页面 */
.ability-reset-confirm{
    position:absolute;inset:0;z-index:20;
    display:flex;align-items:center;justify-content:center;
    background:rgba(3,7,18,.55);
    backdrop-filter:blur(3px);
    opacity:0;pointer-events:none;
    transition:opacity .2s ease;
    border-radius:16px;
}
.ability-reset-confirm.show{opacity:1;pointer-events:auto}
.ability-reset-confirm-box{
    width:360px;max-width:calc(100% - 40px);
    padding:26px 28px 24px;text-align:center;
    background:linear-gradient(180deg,rgba(30,28,38,.97),rgba(14,16,26,.97));
    border:1px solid rgba(212,175,55,.35);border-radius:16px;
    box-shadow:0 18px 50px rgba(0,0,0,.55),0 0 30px -8px rgba(212,175,55,.35),inset 0 1px 0 rgba(255,255,255,.06);
    transform:translateY(10px) scale(.97);
    transition:transform .22s cubic-bezier(.2,.8,.3,1);
}
.ability-reset-confirm.show .ability-reset-confirm-box{transform:none}
.ability-reset-confirm-title{font-size:17px;font-weight:700;color:#FFE9B0;letter-spacing:.5px}
.ability-reset-confirm-text{margin-top:10px;font-size:13px;line-height:1.7;color:rgba(255,255,255,.68)}
.ability-reset-confirm-btns{display:flex;justify-content:center;gap:12px;margin-top:22px}
.ability-reset-confirm-box .btn{
    min-width:96px;justify-content:center;
    color:rgba(255,255,255,.78);
    background:rgba(255,255,255,.06);
    border:1px solid rgba(255,255,255,.16);
}
.ability-reset-confirm-box .btn:hover{color:#fff;border-color:rgba(255,255,255,.35);background:rgba(255,255,255,.1)}
.ability-reset-confirm-ok{
    min-width:112px;justify-content:center;
    color:#FFD9A8;
    background:linear-gradient(135deg,rgba(212,150,60,.28),rgba(170,100,30,.18));
    border:1px solid rgba(240,160,70,.5);
    box-shadow:0 0 14px -4px rgba(240,160,70,.5);
    transition:all .2s ease;
}
.ability-reset-confirm-ok:hover{
    color:#fff;
    background:linear-gradient(135deg,rgba(232,160,66,.42),rgba(180,108,34,.3));
    border-color:rgba(255,180,90,.8);
    box-shadow:0 0 20px -2px rgba(240,160,70,.6);
    transform:translateY(-1px);
}
.ability-reset-confirm-ok:active{transform:scale(.96)}
/* 中尺寸桌面（1600×900 等）：压缩间距与内边距，尽力保持一屏（13px 为原基础字号，未进一步缩小） */
@media (max-width:1650px){
    .ability-modal{width:calc(100vw - 24px);height:calc(100vh - 16px)}
    .ability-survey-head{padding:8px 16px}
    .ability-survey-body{padding:10px 14px;gap:10px}
    .ability-cat-block{padding:8px 10px}
    .ability-cat-title{margin-bottom:6px;padding-bottom:5px}
    .ability-cat-techs{gap:6px}
    .ability-tech-chip{padding:6px 11px 6px 26px;font-size:13px}
    .ability-survey-foot{padding:8px 16px}
}
/* 小尺寸（1366×768 及以下）：进一步压缩边距，保留滚动兜底（内容不被裁切） */
@media (max-width:1200px){
    .ability-survey-body{padding:8px 12px;gap:8px}
    .ability-cat-block{padding:6px 8px;border-radius:10px}
    .ability-tech-chip{padding:5px 10px 5px 24px;font-size:13px}
    .ability-cat-techs{gap:5px}
    .ability-confirm-btn{min-width:170px}
}

/* 个人能力图谱 */
.ability-graph-page{display:flex;flex-direction:column;flex:1;min-height:0}
.ability-graph-bar{
    display:flex;align-items:center;justify-content:space-between;
    padding:12px 20px;flex-shrink:0;
    border-bottom:1px solid rgba(255,255,255,.06);
}
.ability-graph-title{font-size:15px;font-weight:700;color:#FFE9B0}
.ability-graph-actions{display:flex;align-items:center;gap:10px}
.ability-graph-wrap{flex:1;min-height:0;position:relative;overflow:hidden}
.ability-graph-foot{
    display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px;
    padding:10px 20px;flex-shrink:0;
    border-top:1px solid rgba(255,255,255,.06);
}
.ability-graph-summary{font-size:12px;color:rgba(255,255,255,.5)}