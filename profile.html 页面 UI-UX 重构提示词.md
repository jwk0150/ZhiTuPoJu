# profile.html 页面 UI/UX 重构任务

你现在需要对现有项目中的 `frontend/profile.html` 进行一次完整的 UI/UX 重构。

## 一、最重要的硬性要求

这是一次：

> **只重构前端视觉设计和用户交互体验，不重写业务功能的 UI/UX 升级。**

必须最大程度保留现有代码中的：

- HTML 功能结构
- JavaScript 业务逻辑
- API 地址
- API 请求方式
- 请求参数
- 请求返回字段
- 数据库字段映射
- ECharts 数据来源
- 简历上传逻辑
- 简历解析逻辑
- DeepSeek 调用逻辑
- AI 职业访谈逻辑
- 职业画像分析逻辑
- 用户资料 CRUD
- 简历删除逻辑
- Tab 切换逻辑
- Toast 提示逻辑
- Loading 状态
- 错误处理逻辑

**严禁因为 UI 重构而修改、删除或重新设计后端 API。**

---

# 二、现有项目技术栈

前端：

- 原生 HTML
- 原生 CSS
- 原生 JavaScript
- ECharts 5.5.0
- GSAP 3.12.5
- Google Fonts
- 无 Vue
- 无 React
- 无 Webpack
- 无 Vite
- 无构建流程

后端：

- FastAPI
- PostgreSQL
- DeepSeek API

页面是独立的：

`frontend/profile.html`

后续由：

`portal.html`

通过：

「用户中心 → 我的资料」

进入。

---

# 三、当前页面功能

当前 `profile.html` 已经包含四个核心功能：

1. 基本信息
2. 我的简历
3. AI职业访谈
4. 职业画像

这些功能必须全部保留。

当前 API 也必须全部保留。

---

# 四、页面重新定位

不要把页面设计成传统意义上的：

> “后台个人资料管理页面”

而应该设计成：

> **“我的职业档案 / My Career Profile”**

这是整个岗位与能力图谱平台中：

> **用户个人能力数据的入口和职业画像中心。**

用户进入这个页面后，应该形成这样的认知：

```text
我的资料
    ↓
我的经历
    ↓
我的技能
    ↓
我的能力
    ↓
我的职业方向
    ↓
我的岗位匹配
```

整个页面应该体现：

> **个人信息 → 简历 → AI访谈 → 能力画像 → 岗位匹配**

这一完整职业成长链路。

---

# 五、整体视觉风格

必须与现有 `portal.html` 保持统一的科技数据平台风格。

不要做成：

- 普通企业后台
- Bootstrap 管理后台
- Element Plus 风格
- 简历网站
- 招聘网站
- 纯白色 SaaS 后台

应该保持：

> **深色科技感 + 数据驾驶舱 + AI职业分析 + 高级企业级后台**

视觉关键词：

- 深色
- 克制
- 高级
- 数据感
- 科技感
- AI感
- 轻微玻璃拟态
- 细边框
- 微弱发光
- 数据卡片
- 清晰的信息层级

不要大量使用夸张渐变。

不要让整个页面变成“霓虹灯”。

---

# 六、不要破坏现有主题

首先检查当前 `portal.html` 和 `profile.html` 已经存在的：

- CSS 变量
- 背景色
- 字体
- 边框
- 阴影
- 圆角
- accent color
- 动画

尽可能复用现有设计语言。

如果 `profile.html` 已经存在独立 CSS 命名空间：

继续保持。

不要让 profile 页面的 CSS 污染：

`portal.html`

---

# 七、页面整体布局

将当前页面优化为以下结构：

```text
┌──────────────────────────────────────────────────────────┐
│ ← 返回平台                         我的职业档案           │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────┐   张三                         [编辑资料]       │
│  │      │   大数据技术 · 本科                            │
│  │ 头像 │   XXX大学 · 2026届                             │
│  │      │                                                 │
│  └──────┘   ● 数据分析师                                 │
│                                                          │
│              资料完整度 82%                              │
│              ████████████████░░░░                        │
│              建议补充项目经历                             │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  08              72              05              82%     │
│  已识别技能       职业竞争力        推荐岗位        资料完整度 │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  个人资料     我的简历     AI职业访谈     职业画像        │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                     当前内容区域                          │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

# 八、顶部 Hero 区域

这是页面最重要的视觉区域。

不要简单显示：

> 姓名 / 专业 / 学校

而要做成：

## Career Profile Header

包含：

### 1. 头像

如果没有真实头像：

使用：

> 用户姓名首字

例如：

`张`

头像应该有轻微科技感边框。

---

### 2. 用户名称

例如：

> 张三

下面：

> 大数据技术 · 本科

再下面：

> XXX大学 · 2026届

---

### 3. 目标职业标签

例如：

```text
DATA ANALYST
```

或者：

```text
数据分析师
```

使用标签形式。

---

### 4. 编辑资料按钮

右侧：

```text
[ 编辑资料 ]
```

点击后进入原有编辑状态。

**不要修改现有编辑逻辑。**

---

# 九、资料完整度

在 Hero 区域加入：

```text
资料完整度

82%

████████████████░░░░

补充项目经历后，可以进一步提升职业画像准确度
```

百分比必须使用现有数据或者现有计算逻辑。

如果当前代码没有独立计算逻辑：

可以使用已有资料字段计算。

但：

**不要修改后端 API。**

---

# 十、KPI 区域

保留现有 KPI 数据：

- 技能数量
- 简历数量
- 推荐岗位数量
- 竞争力评分

但是视觉上重新设计。

建议：

```text
┌────────────┐
│ 08         │
│ 已识别技能  │
│ SKILLS     │
└────────────┘
```

四个卡片横向排列。

数字使用较大字号。

数字出现时使用 GSAP 数字递增动画。

例如：

`0 → 72`

但是不要影响原来的数据逻辑。

---

# 十一、Tab 导航

保留原有四个 Tab：

```text
个人资料
我的简历
AI职业访谈
职业画像
```

优化成高级数据平台风格。

选中状态：

- accent 色底部线
- 微弱发光
- 字体高亮

不要使用传统 Bootstrap Tab 样式。

切换必须继续使用现有：

`data-view`

或者当前项目已有的 Tab 切换机制。

**不要重写业务逻辑。**

---

# 十二、个人资料 Tab

重新设计成：

## 基本信息

使用卡片布局。

```text
基本信息                              [编辑资料]

姓名
张三

学校
XXX大学

专业
大数据技术

学历
本科

年级
2026届

目标岗位
数据分析师
```

---

## 个人简介

独立卡片：

```text
个人简介

对数据分析、机器学习和大数据技术感兴趣……
```

---

## 编辑状态

保持原有：

默认只读

点击：

`编辑资料`

之后：

字段解锁。

保存：

调用原有：

`POST /api/profile/profile/update`

**不要修改 API。**

---

# 十三、我的简历 Tab

这是第二个核心页面。

顶部增加：

```text
我的简历

管理你的职业履历，让 AI 帮你提炼能力。

                    [ 上传简历 ]
```

下面增加一个非常重要的双入口设计：

```text
┌────────────────────────────┐
│                            │
│          📄                │
│                            │
│       我有简历              │
│                            │
│   上传简历并进行 AI 分析     │
│                            │
│       [上传简历]            │
│                            │
└────────────────────────────┘


┌────────────────────────────┐
│                            │
│          ✦                 │
│                            │
│       我没有简历            │
│                            │
│   通过 AI 职业访谈生成       │
│                            │
│       [开始访谈]            │
│                            │
└────────────────────────────┘
```

注意：

这两个按钮必须调用已有功能。

不要创建新的后端接口。

---

# 十四、简历上传区域

保留现有：

- PDF
- DOC
- DOCX
- TXT
- 8MB限制
- 拖拽上传

重新设计视觉：

```text
┌────────────────────────────────────────────┐
│                                            │
│                  ↑                         │
│                                            │
│              上传你的简历                   │
│                                            │
│      拖拽文件到这里，或点击选择文件          │
│                                            │
│       PDF / DOC / DOCX / TXT · ≤ 8MB        │
│                                            │
└────────────────────────────────────────────┘
```

拖拽进入时：

- 边框高亮
- 背景轻微变化
- GSAP 动画

---

# 十五、简历列表

每一份简历设计为：

```text
┌────────────────────────────────────────────┐
│ 📄  我的简历_2026.pdf                       │
│     PDF · 2.4 MB                           │
│                                            │
│     ● 已完成分析                            │
│                                            │
│     提取字符 4,823                         │
│                                            │
│                    [分析] [查看] [删除]     │
└────────────────────────────────────────────┘
```

状态：

```text
uploaded
analyzed
generated
error
```

继续使用现有状态。

不要修改后端返回字段。

---

# 十六、AI职业访谈 Tab

这是整个页面最需要重点设计的页面。

不要设计成普通聊天机器人。

应该设计成：

> **AI职业档案构建工作台**

布局：

```text
┌───────────────────────────────────────────────────────────┐
│ AI职业访谈                                                 │
│ 通过对话，让 AI 帮你梳理职业经历                            │
├─────────────────────┬─────────────────────────────────────┤
│                     │                                     │
│ 职业档案构建进度     │          AI职业顾问                 │
│                     │                                     │
│ ● 基础信息          │   ┌──────────────────────────┐      │
│ ● 教育经历          │   │ 你好，我会通过几个问题     │      │
│ ◉ 技能能力          │   │ 帮你梳理职业经历。         │      │
│ ○ 项目经历          │   └──────────────────────────┘      │
│ ○ 职业目标          │                                     │
│ ○ 个人优势          │              ┌─────────────────┐    │
│                     │              │ 我学的是大数据。 │    │
│ ─────────────       │              └─────────────────┘    │
│                     │                                     │
│ 当前完成度           │   AI：                              │
│                     │   大数据方向比较广，你目前接触       │
│ ███████░░░ 68%      │   比较多的是数据分析、机器学习，     │
│                     │   还是数据开发？                     │
│                     │                                     │
│                     │ ┌────────────────────────────────┐ │
│                     │ │ 输入你的回答……                 │ │
│                     │ └────────────────────────────────┘ │
│                     │                         [发送 ↑]     │
└─────────────────────┴─────────────────────────────────────┘
```

---

# 十七、职业访谈视觉要求

AI消息：

- 左侧
- 深色/半透明卡片
- 微弱边框

用户消息：

- 右侧
- 使用平台 accent 色
- 不要使用刺眼的纯色

AI头像：

使用：

`✦`

或者：

`AI`

---

# 十八、职业访谈增加“阶段进度”

左侧显示：

```text
职业档案构建

01 基础信息       ✓
02 教育经历       ✓
03 技能能力       ●
04 项目经历       ○
05 职业目标       ○
06 个人优势       ○
```

这个进度必须是前端视觉层。

不要强行修改后端 API。

如果当前后端没有阶段字段：

可以根据已有聊天状态在前端进行简单展示。

---

# 十九、职业画像 Tab

这是最终数据展示页面。

顶部：

```text
我的职业画像

AI 已基于你的资料、简历和职业访谈生成分析

                    [重新分析]
```

如果当前没有分析结果：

显示：

```text
尚未生成职业画像

完善个人资料并完成简历分析或职业访谈后，
即可生成你的职业能力画像。

[开始职业访谈]
```

---

# 二十、职业画像第一屏

第一行：

```text
职业竞争力

82

较上次 +6

[ ECharts Gauge ]
```

旁边：

```text
职业定位

数据分析 / 大数据方向

核心能力

Python
SQL
数据处理
机器学习
```

---

# 二十一、能力雷达图

继续使用现有 ECharts Radar。

六个维度必须保持：

- 技术能力
- 项目经验
- 数据能力
- 工程能力
- 创新能力
- 学习能力

不要修改后端数据字段。

只优化：

- 图表容器
- 标题
- 数据标签
- 背景
- tooltip
- 动画
- 边框
- 布局

---

# 二十二、技能标签

保留现有技能数据。

视觉：

```text
核心技能

Python       92
SQL          86
Spark        78
机器学习      74
Vue          68
```

根据 score：

大 / 中 / 小

继续使用现有逻辑。

---

# 二十三、优势与待提升

增加两个分析卡片。

## 核心优势

```text
01  数据处理能力
    具备较好的数据处理基础

02  Python 技术能力
    已有实际项目经验
```

## 待提升

```text
01  SQL高级应用
02  工程实践经验
03  商业分析能力
```

如果后端已有对应字段，直接使用。

如果没有，不要凭空生成。

---

# 二十四、岗位推荐

保留现有：

TOP 10 岗位。

每个岗位：

```text
数据分析师

XX科技有限公司
上海

匹配度 87%

██████████████████░░
```

使用现有 `job_postings` 数据。

不要修改数据库查询逻辑。

---

# 二十五、动画要求

使用现有 GSAP。

适当增加：

### 页面首次加载

Hero：

淡入 + 上移

KPI：

数字滚动

Tab：

轻微淡入

Card：

错峰出现

### 简历上传

上传成功：

scale + fade

### AI分析

分析过程中：

使用：

`AI ANALYZING`

动态点：

`.` → `..` → `...`

### 职业画像

ECharts：

使用原有动画。

不要增加大量动画。

动画必须：

> 快、轻、克制。

---

# 二十六、响应式

保持：

`max-width: 900px`

移动端：

`640px`

但是重新优化：

桌面：

```text
2列
```

AI访谈：

```text
左侧进度
+
右侧聊天
```

移动端：

```text
顶部进度
↓
聊天区域
```

职业画像：

```text
单列
```

---

# 二十七、颜色规范

优先使用现有项目 CSS Variables。

如果缺少变量，再建立统一变量：

```css
--profile-bg
--profile-surface
--profile-surface-hover
--profile-border
--profile-text
--profile-text-muted
--profile-accent
--profile-accent-soft
--profile-success
--profile-warning
--profile-danger
```

不要到处写硬编码颜色。

---

# 二十八、字体

继续使用现有：

- DM Sans
- Noto Sans SC
- JetBrains Mono

推荐：

标题：

DM Sans

中文正文：

Noto Sans SC

数字 / KPI / 技术标签：

JetBrains Mono

---

# 二十九、重要的代码保护规则

在修改之前：

### 第一步

完整阅读当前：

`profile.html`

不要直接覆盖。

### 第二步

识别：

- 所有 API
- 所有 DOM ID
- 所有 class
- 所有 JS 函数
- 所有事件监听
- 所有 ECharts 实例
- 所有状态变量
- 所有数据字段

建立一份内部映射。

### 第三步

只修改：

- HTML布局
- CSS
- UI交互动画
- UI状态展示

---

# 三十、绝对禁止

不要：

❌ 引入 Vue

❌ 引入 React

❌ 引入 Bootstrap

❌ 引入 Tailwind

❌ 引入 Element Plus

❌ 修改 FastAPI

❌ 修改 API URL

❌ 修改请求参数

❌ 修改数据库结构

❌ 修改数据库字段

❌ 删除现有 JS 功能

❌ 删除现有 ECharts

❌ 删除现有职业访谈

❌ 删除简历上传

❌ 修改 DeepSeek 调用逻辑

❌ 将 profile.html 改造成新的 SPA 框架

❌ 拆成多个 HTML 页面

---

# 三十一、最终验收标准

重构完成后必须保证：

## 功能

- [ ] 页面可以正常打开
- [ ] 返回平台正常
- [ ] 用户资料可以读取
- [ ] 用户资料可以编辑
- [ ] 用户资料可以保存
- [ ] 简历可以上传
- [ ] 简历可以删除
- [ ] 简历可以分析
- [ ] AI职业访谈可以开始
- [ ] AI职业访谈可以继续
- [ ] AI职业画像可以读取
- [ ] AI职业画像可以重新分析
- [ ] ECharts 正常显示
- [ ] Toast 正常显示
- [ ] Loading 正常显示

## UI

- [ ] 页面与 portal.html 风格统一
- [ ] 顶部 Hero 信息层级清晰
- [ ] KPI 卡片更加高级
- [ ] Tab 更有科技数据平台风格
- [ ] 个人资料更加清晰
- [ ] 简历上传体验更加直观
- [ ] AI职业访谈具有明显 AI 感
- [ ] 职业画像具有数据分析报告感
- [ ] 页面没有明显视觉拥挤
- [ ] 移动端不会出现严重溢出

---

# 三十二、最终设计目标

请不要单纯理解为：

> “把页面变漂亮。”

真正目标是：

> **把 profile.html 从一个普通的个人资料页面，升级成整个“岗位与能力图谱平台”的用户职业档案中心。**

用户进入页面后应该自然经历：

```text
认识自己
   ↓
完善资料
   ↓
上传 / 生成简历
   ↓
AI职业访谈
   ↓
提取技能
   ↓
建立能力画像
   ↓
匹配岗位
   ↓
发现职业提升方向
```

整个页面应该让用户感受到：

> **“这个平台正在逐渐理解我的职业能力。”**

最后：

**请直接基于现有 `profile.html` 修改，不要重新从零创建一个示例页面。**

修改完成后，请输出：

1. 修改后的完整 `profile.html`
2. 简要说明修改了哪些 UI/UX
3. 明确说明哪些 API、JS 业务逻辑和数据库字段保持不变
4. 列出如果存在的兼容性问题

不要为了视觉效果牺牲任何现有功能。