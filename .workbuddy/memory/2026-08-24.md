# 2026-08-24

## 岗位大新闻页面去 AI 感 + 单屏紧凑排版

- 目标：去掉页面 AI 味儿（实时更新脉冲、emoji 标签、分类小圆点、TOP5 发光徽章、涨幅箭头、机器人封面），并按参考图排版。
- 严格按参考图结构：顶部标题 + 今日焦点（左主卡 + 右3小卡）+ 三栏（热门资讯 / 最新资讯(带筛选) / 热门排行 TOP5）+ 延伸阅读 + 更多资讯按钮。
- 二次优化：压缩所有留白与卡片高度，让内容尽量在一屏内可见（桌面 ~1080 屏基本全覆盖，含延伸阅读）。
- 涉及文件：`frontend/pages/news/index.html`、`css/news.css`、`js/home.js`、`js/data.js`、`js/common.js`（robot 视觉图 → chip/gear 抽象图形）、`js/detail.js`（分类标签与按钮同步）。
- 数据层去掉 emoji flag、涨幅字段；新增 `focusCards` 配置焦点右侧3卡。
- 本地预览用 `python -m http.server 8080`（已在后台运行，绑定 127.0.0.1）。
