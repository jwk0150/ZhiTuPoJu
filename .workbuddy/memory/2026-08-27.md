# 2026-08-27

## 岗位大新闻 · 仅第一屏数字杂志化重设计
- 按用户两张参考图：图1=视觉风格（图1已存 Downloads/Codex...png）；图2=内容池。
- 仅修改 `frontend/pages/news/index.html` 的 `screen-focus` 第一屏，下游 5 屏结构与 `home.js` 的 renderHot/renderLatest/renderRanking/renderRelated 一律不动。
- 新生成配图：`frontend/assets/news/hero-magazine.png`（ImageGen 1024x1536，深色未来城市+人物背影+ JOB FUTURE 招牌 + 金色远处光）。
- 新增 `css/news.css` 一大段 `.jn-screen--hero / .jn-hero-*` 杂志样式（深色沉浸背景、超大排版、大数据、玻璃头版卡、编辑列表、滚动提示、入场动效）；原 CSS 完整保留。
- `home.js`：去掉旧焦点轮播/分页，新增 `renderFocus()` 输出杂志 Hero（标题/数据/视觉/编辑列表/头版卡），启用 `JN.animateDataValues` count-up；`3分钟速览` 按钮平滑滚到下一屏。
- 校验：`node --check` 通过；8090 静态服务器全部资源 200；vm DOM 桩冒烟测试 16/16 命中（标题/数据/标签/头版/视觉/编辑 01-04）。

## 岗位大新闻 · 添加全页大气背景图
- 新生成 ImageGen 1920x1080：`frontend/assets/news/bg-atmosphere.png`（深黑 #05080D 底 + 右上金色径向光 + 左下蓝色径向光 + 颗粒纹理 + 暗角，右下有浅水印）。
- `css/news.css`：
  - `.jn-page` 新增 `position:relative`，新增 `::before` 固定层（position:fixed; inset:0; z-index:0; pointer-events:none）挂 `url('../../../assets/news/bg-atmosphere.png')` 配 `radial-gradient mask` 收边。
  - `.jn-page > *` 显式 `z-index:1` 浮在背景之上。
  - `.jn-screen--hero` 由三段径向渐变改为 `transparent`，让背景图的蓝/金光晕直接透出，不再重复叠加。
- 预览：8090 静态服务器对 css 与 bg 资源均 200。
- 注意点：背景图右下有 ImageGen 水印，靠 mask 暗角 + 内容覆盖淡化；如未来要做到完全干净可改用 ImageGen 的 `nologo` 或本地代码重新生成。

## 岗位大新闻 · 亮色主题切换
- 用户要求"换个风格，亮一点" → 从深黑科技风切换为暖白高级杂志风。
- 新生成 ImageGen 1920x1080：`frontend/assets/news/bg-atmosphere-light.png`（暖白 #F5F2ED 底 + 右上金色径向光 + 左下蓝色径向光 + 纸张颗粒纹理 + 暗角）。
- `css/news.css` Design Tokens 全面替换：
  - --jn-bg: #0B1014 → #F5F2ED；--jn-card: #141C24 → #FFFFFF；--jn-card-2/3 对应浅化
  - --jn-border: rgba(255,255,255,0.07) → rgba(0,0,0,0.08)；--jn-border-strong 同理
  - --jn-text: #E8ECF0 → #1A1A1A；--jn-text-2: #9AA6B3 → #4B5563；--jn-text-3 → #6B7280
  - --jn-accent: #C4A86B → #A88B4E（金色略暗更沉稳）；--jn-accent-2 → #8B7340
  - --jn-shadow: 降 3-4 倍透明度（0.22→0.06, 0.32→0.10）
  - --jn-focus-ring: var(--jn-bg) → #FFFFFF（间隔环用白色在浅底上更清晰）
- 批量替换硬编码：rgba(255,255,255,0.xx) → rgba(0,0,0,0.xx)；#05080D → #F0EDE8；rgba(5,8,13,xx) → rgba(240,237,232,xx)
- Hero 视觉区：background: #05080D → #F0EDE8；::after 暗角渐变用浅色替代；visual-tag/scroll-btn 用白底半透明磨砂
- 编辑分类标签色：从暗字+亮底改为白字+深底（#4A7C9B / #3D8B7A / #C87B7B / #8B6FB5）
- 全站联动：所有子页面（industry/all/detail）共用 news.css，一键切主题
- 保留旧图 `bg-atmosphere.png`（深色版），未来可做暗/亮双主题切换