# 「执图破局」AI 协作开发 Teach 文档

> **目标读者**：项目组全体开发者（5人）
> **文档目的**：让开发者**直接基于本指南使用 AI 工具（Cursor / Claude Code / Trae / GitHub Copilot / 文心快码 等）**，就能高效、规范地完成本项目开发。
> **核心原则**：人是主导，AI 是助手。**架构决策、核心算法、数据准确性必须由开发者把关**，AI 负责代码生成、文档编写、重复性劳动。
> **配套文档**：[项目开发指南.md](./项目开发指南.md)（必读）

---

## ⚠️ 强制约定：AI 每次运行时必须执行的两件事

### 🔴 约定1：开发者身份识别（每次 AI 启动时必做）

```
【AI 必须做的第一件事】
    ↓
向开发者询问：👤 "请问您是项目组的哪位开发者？
                ① Tech Lead（技术负责人）
                ② 数据工程师（Data Engineer）
                ③ 后端/AI工程师（Backend & AI Engineer）
                ④ 前端工程师（Frontend Engineer）
                ⑤ 测试/DevOps工程师（QA & DevOps Engineer）"
    ↓
收到回复后：
   ✅ 加载对应角色的工作流（详见第6部分）
   ✅ 加载对应模块的代码上下文
   ✅ 锁定该角色的职责边界
   ✅ 在整个会话中保持该角色身份
```

**为什么需要这个？**
- 不同角色有**不同的代码所有权**和**不同的关注重点**
- 让 AI 知道你是谁，可以**精准加载相关上下文**（避免无关信息干扰）
- 防止 AI 越权修改其他人的代码模块
- 让 AI 的回答**更聚焦、更专业**

### 🔴 约定2：任务完成时生成项目汇报（每次任务结束后必做）

```
【每次开发者向 AI 发出"完成"或"结束"信号时】
    ↓
AI 必须自动生成【项目汇报文件】，包含：
    ├─ 📋 任务概要
    ├─ 👤 执行人（即开发者身份）
    ├─ 📅 执行时间
    ├─ 📝 本次完成的具体工作
    ├─ 📁 涉及的代码文件清单
    ├─ 🧪 测试情况
    ├─ ⚠️ 遇到的问题与解决方案
    ├─ 📊 当前进度（按分工模块）
    ├─ 🚧 待办事项 / 下一步建议
    └─ 🤝 需要其他角色协助的事项
    ↓
汇报文件保存路径：docs/09-daily-reports/YYYY-MM-DD/<角色>-<任务名>.md
    ↓
自动更新 项目总进度看板：docs/09-daily-reports/PROGRESS.md
```

**为什么需要这个？**
- 5人团队**信息同步**是核心痛点
- 每个人做完了什么、遇到了什么问题、其他成员必须知道
- 答辩时**汇总成完整开发日志**
- Tech Lead 可基于汇报**判断整体进度风险**
- **自动归档**，无需人工整理

---

## 目录

---

## 目录

- [⚠️ 强制约定：AI 每次运行时必须执行的两件事](#️-强制约定ai-每次运行时必须执行的两件事)
- [第0部分 阅读须知](#第0部分-阅读须知)
- [第1部分 AI 工具使用总则](#第1部分-ai-工具使用总则)
- [第2部分 环境与工具准备](#第2部分-环境与工具准备)
- [第3部分 AI 协作开发的正确姿势](#第3部分-ai-协作开发的正确姿势)
- [第4部分 分模块 AI 开发指南](#第4部分-分模块-ai-开发指南)
- [第5部分 提示词模板库](#第5部分-提示词模板库)
- [第6部分 各岗位 AI 工作流](#第6部分-各岗位-ai-工作流)
- [第7部分 常见场景与最佳实践](#第7部分-常见场景与最佳实践)
- [第8部分 AI 生成代码审查清单](#第8部分-ai-生成代码审查清单)
- [第9部分 避坑指南](#第9部分-避坑指南)
- [📊 附录A：项目汇报模板与生成规则](#附录a项目汇报模板与生成规则)
- [附录B：项目级 AI 规则配置文件](#附录b项目级-ai-规则配置文件)

---

## 第0.5部分 AI 启动流程（强制执行）

### 0.5.1 标准启动流程（AI 必须遵循）

```yaml
# AI 启动 Checklist（每次会话开始必做）

step_1_身份识别:
  必做: 是
  AI行为: |
    1. 询问开发者角色（提供5个选项）
    2. 询问开发者姓名/昵称（用于汇报文件署名）
    3. 询问当前任务目标（如"今天要开发哪部分"）
  询问话术: |
    "👋 您好！我是执图破局项目的 AI 协作助手。
    在开始之前，我需要确认几个信息：
    
    ① 请问您是项目组的哪位开发者？
       1️⃣ Tech Lead（技术负责人）
       2️⃣ 数据工程师（Data Engineer）
       3️⃣ 后端/AI 工程师（Backend & AI Engineer）
       4️⃣ 前端工程师（Frontend Engineer）
       5️⃣ 测试/DevOps 工程师（QA & DevOps Engineer）
    
    ② 请问您的姓名/花名是？
    
    ③ 您本次会话的开发任务是什么？（一句话描述）"

step_2_加载上下文:
  必做: 是
  AI行为: |
    根据开发者角色，加载：
    1. 项目开发指南.md 中对应角色的章节
    2. 本文档第6部分"各岗位 AI 工作流"
    3. 当前任务相关的代码模块
    4. 该角色负责的目录（代码所有权）

step_3_职责边界确认:
  必做: 是
  AI行为: |
    告知开发者：
    "✅ 已识别您为【角色】。
    
    📂 您的代码所有权目录：
    [列出该角色的代码目录]
    
    🚧 您的当前任务：
    [开发者刚才描述的任务]
    
    ⚠️ 提醒：
    [该角色当前的注意事项]"
  
step_4_开始工作:
  必做: 是
  AI行为: 正常进入开发协作

step_5_任务结束触发汇报:
  必做: 是
  AI行为: |
    当开发者说以下任一关键词时触发：
    - "完成了"、"做完了"、"结束"、"搞定"
    - "今天先到这里"、"先这样"、"OK"
    - "暂停"、"保存进度"、"生成汇报"
    
    AI 必须立即执行【项目汇报生成流程】（见 0.5.2）
```

### 0.5.2 项目汇报生成流程（强制执行）

#### 触发条件

开发者发送包含以下关键词的消息：
```
✅ 完成类："完成"、"搞定"、"结束"、"OK"、"做完"、"done"
✅ 暂停类："暂停"、"休息"、"先这样"、"保存"、"记录"
✅ 显式类："生成汇报"、"写日报"、"汇报进度"
✅ 会话结束：超时、关闭、切换任务
```

#### AI 必须执行的步骤

```yaml
step_1_信息收集:
  AI行为: |
    在生成汇报前，AI 必须通过 AskUserQuestion 或对话收集：
    
    ① "本次任务完成了哪些具体工作？（请列举）"
    ② "修改/新增了哪些文件？"
    ③ "是否运行了测试？结果如何？"
    ④ "是否遇到问题？是如何解决的？"
    ⑤ "下一步计划做什么？"
    ⑥ "需要其他角色协助的事项？"

step_2_生成汇报文件:
  必做: 是
  路径规则: |
    docs/09-daily-reports/
    └── YYYY-MM-DD/
        ├── tech-lead-任务名.md
        ├── data-engineer-任务名.md
        ├── backend-engineer-任务名.md
        ├── frontend-engineer-任务名.md
        ├── qa-devops-任务名.md
        └── SESSION-SUMMARY.md（本次会话总结）
  
  文件命名: |
    命名格式：{角色英文}-{任务简述}.md
    示例：
    - backend-engineer-spark-client-setup.md
    - frontend-engineer-graph-visualization.md
    - data-engineer-lagou-spider.md

step_3_更新总进度看板:
  必做: 是
  文件路径: docs/09-daily-reports/PROGRESS.md
  AI行为: |
    在 PROGRESS.md 中追加/更新：
    1. 该角色的最新工作内容
    2. 当前模块完成度（百分比）
    3. 阻塞项 & 风险项
    4. 整体项目进度

step_4_输出汇报确认:
  必做: 是
  AI行为: |
    向开发者输出：
    "✅ 已生成项目汇报文件：
    📄 路径：docs/09-daily-reports/YYYY-MM-DD/{角色}-{任务名}.md
    📊 已更新总进度看板：docs/09-daily-reports/PROGRESS.md
    
    📋 汇报摘要：
    [简短的汇报摘要，5行以内]"
```

### 0.5.3 项目汇报模板

#### 单任务汇报模板（必填）

```markdown
# 项目汇报 - [任务名]

## 📋 基本信息

| 字段 | 内容 |
|------|------|
| **开发者** | [姓名/花名] |
| **角色** | [Tech Lead / 数据工程师 / 后端AI工程师 / 前端工程师 / 测试DevOps工程师] |
| **日期** | YYYY-MM-DD |
| **任务名称** | [任务简述] |
| **关联里程碑** | [M1/M2/.../M8] |
| **优先级** | [P0/P1/P2] |
| **状态** | [已完成 / 部分完成 / 阻塞中] |

## 📝 本次完成工作

### ✅ 已完成
1. [具体工作1，含技术细节]
2. [具体工作2，含技术细节]
3. [具体工作3，含技术细节]

### 🚧 进行中（如有）
- [进行中的工作]

### ❌ 未完成（如有）
- [原因说明]

## 📁 涉及的文件清单

### 新增文件
- `path/to/file1.py` - [文件用途]
- `path/to/file2.vue` - [文件用途]

### 修改文件
- `path/to/file3.py` - [修改内容]

### 删除文件（如有）
- [删除的文件及原因]

## 🧪 测试情况

| 测试类型 | 覆盖率/通过率 | 备注 |
|---------|--------------|------|
| 单元测试 | XX% | [备注] |
| 集成测试 | 通过/失败 | [备注] |
| 准确率测试 | XX% | [备注：是否达标] |

### 测试用例清单
- [ ] test_xxx_1 - 通过
- [ ] test_xxx_2 - 通过

## ⚠️ 遇到的问题与解决方案

### 问题1：[问题简述]
- **现象**：[详细描述]
- **原因**：[根因分析]
- **解决方案**：[如何解决]
- **耗时影响**：[X小时]

### 问题2：[问题简述]
- [同上结构]

## 📊 当前进度

### 本角色负责模块进度

| 模块 | 进度 | 状态 |
|------|------|------|
| 模块A | 80% | 进行中 |
| 模块B | 100% | 已完成 |
| 模块C | 0% | 未开始 |

### 整体项目进度（如已知）

- M1（启动与架构）：✅ 已完成
- M2（数据采集）：🚧 进行中（X%）
- M3（图谱构建）：⬜ 未开始
- ...

## 🤝 需要协助的事项

### 需要 [其他角色] 协助
- [具体协作事项，含接口定义/期望时间]

### 需要 Tech Lead 决策
- [需决策的技术方案]

## 📋 下一步计划

1. [下一步1]
2. [下一步2]
3. [下一步3]

## 🔗 相关链接

- **PR**：#XX
- **文档**：[文档路径]
- **测试报告**：[路径]
- **会议纪要**：[路径]

## ✍️ 备注

[其他需要说明的事项]

---

**汇报时间**：YYYY-MM-DD HH:MM
**汇报生成方式**：AI 协作助手自动生成
**下一步操作**：等待其他成员确认 / 继续下一任务
```

#### 会话总结汇报模板（多任务汇总时使用）

```markdown
# 会话总结 - [日期]

## 📅 总体信息
| 字段 | 内容 |
|------|------|
| **开发者** | [姓名] |
| **角色** | [角色] |
| **会话开始** | YYYY-MM-DD HH:MM |
| **会话结束** | YYYY-MM-DD HH:MM |
| **总耗时** | X 小时 Y 分钟 |
| **完成任务数** | N 个 |

## 📋 本次会话完成的所有任务

1. **任务1**：[任务名]
   - 状态：✅ 完成
   - 涉及文件：X 个
   - 汇报文件：[链接]

2. **任务2**：[任务名]
   - 状态：✅ 完成
   - 涉及文件：X 个
   - 汇报文件：[链接]

## 📊 当日累计进度

[各模块进度汇总]

## ⚠️ 关键阻塞 / 风险

[需要团队关注的问题]

## 🎯 明日计划

[接下来的工作安排]
```

### 0.5.4 总进度看板模板（PROGRESS.md）

```markdown
# 执图破局 - 项目总进度看板

> **最后更新**：YYYY-MM-DD HH:MM
> **更新人**：[开发者姓名]
> **整体进度**：[X]%

## 📊 项目里程碑进度

| 里程碑 | 负责角色 | 进度 | 状态 |
|--------|---------|------|------|
| M1 启动与架构 | Tech Lead | 100% | ✅ |
| M2 数据采集 | 数据工程师 | 75% | 🚧 |
| M3 图谱构建 | 后端/AI工程师 | 60% | 🚧 |
| M4 智能层 | 后端/AI工程师 | 40% | 🚧 |
| M5 人岗匹配 | 后端/AI工程师 | 20% | 🚧 |
| M6 前端开发 | 前端工程师 | 30% | 🚧 |
| M7 集成测试 | 测试/DevOps | 0% | ⬜ |
| M8 提交 | 全员 | 0% | ⬜ |

## 👥 各角色工作概览

### 🔹 Tech Lead（[姓名]）
- **今日完成**：[简述]
- **进行中**：[简述]
- **待办**：[简述]
- **详细汇报**：[链接]

### 🔹 数据工程师（[姓名]）
- **今日完成**：[简述]
- **进行中**：[简述]
- **待办**：[简述]
- **详细汇报**：[链接]

### 🔹 后端/AI 工程师（[姓名]）
- **今日完成**：[简述]
- **进行中**：[简述]
- **待办**：[简述]
- **详细汇报**：[链接]

### 🔹 前端工程师（[姓名]）
- **今日完成**：[简述]
- **进行中**：[简述]
- **待办**：[简述]
- **详细汇报**：[链接]

### 🔹 测试/DevOps 工程师（[姓名]）
- **今日完成**：[简述]
- **进行中**：[简述]
- **待办**：[简述]
- **详细汇报**：[链接]

## 🚨 风险与阻塞

| 风险项 | 影响 | 责任人 | 状态 |
|--------|------|--------|------|
| [风险1] | 高 | [角色] | 处理中 |
| [风险2] | 中 | [角色] | 已解决 |

## 🎯 三大硬性指标

| 指标 | 当前 | 目标 | 状态 |
|------|------|------|------|
| JD 解析准确率 | - | ≥ 90% | ⬜ |
| 简历提取准确率 | - | ≥ 90% | ⬜ |
| 人岗匹配准确率 | - | ≥ 90% | ⬜ |
| 单元测试覆盖率 | - | ≥ 60% | ⬜ |

## 📅 关键日期

| 日期 | 事件 |
|------|------|
| YYYY-MM-DD | M1 完成 |
| YYYY-MM-DD | [下一个里程碑] |
| YYYY-MM-DD | 提交截止 |
```

### 0.5.5 汇报生成的 AI 实现指引

#### Claude Code / Cursor 中的具体做法

```yaml
方案A_使用自定义命令:
  工具: Claude Code
  命令: /report
  行为: |
    用户输入 /report 时，AI 自动：
    1. 扫描本次会话完成的工作
    2. 收集 git diff 信息
    3. 调用模板生成汇报
    4. 保存到 docs/09-daily-reports/

方案B_使用关键词触发:
  工具: Cursor / Claude Code
  关键词: ["完成", "结束", "搞定", "保存", "汇报"]
  行为: |
    AI 检测到关键词时：
    1. 主动询问汇报细节
    2. 生成汇报文件
    3. 更新 PROGRESS.md

方案C_使用AskUserQuestion:
  工具: 任意AI工具
  实现: |
    AI 在检测到"完成"关键词时，使用 AskUserQuestion：
    Question: "任务完成，请选择要生成的内容："
    Options:
      - "生成本任务汇报"
      - "生成会话总结"
      - "更新总进度看板"
      - "全部生成"
```

#### 推荐的提示词模板（在 AI 工具中预置）

```markdown
# AI 系统提示词（添加到所有 AI 工具的 system prompt）

你正在协助"执图破局"项目开发。

## ⚠️ 必须遵循的两大约定

### 约定1：身份识别
每次会话开始时，必须首先向开发者询问：
1. 您的角色（5个选项）
2. 您的姓名/花名
3. 本次任务目标

### 约定2：自动生成汇报
当开发者表达任务完成时（如"完成"、"结束"、"搞定"等），
必须自动：
1. 收集汇报信息
2. 生成汇报文件到 docs/09-daily-reports/YYYY-MM-DD/[角色]-[任务名].md
3. 更新 docs/09-daily-reports/PROGRESS.md
4. 输出确认信息

## 📋 角色与代码所有权

- Tech Lead：架构、核心算法、跨模块集成
- 数据工程师：/data-collection、/data-processing
- 后端/AI 工程师：/backend、/ai-core、/knowledge-graph
- 前端工程师：/frontend
- 测试/DevOps：/tests、/.github、/docker、/scripts

## 📝 详细规范
详见 @项目开发指南.md 和 @AI协作开发teach文档.md
```

### 0.5.6 目录结构补充

```
docs/
├── 09-daily-reports/              ← 新增：项目汇报目录
│   ├── PROGRESS.md                ← 总进度看板（唯一）
│   ├── README.md                  ← 汇报说明
│   ├── YYYY-MM-DD/                ← 按日期归档
│   │   ├── tech-lead-xxx.md
│   │   ├── data-engineer-xxx.md
│   │   ├── backend-engineer-xxx.md
│   │   ├── frontend-engineer-xxx.md
│   │   ├── qa-devops-xxx.md
│   │   └── SESSION-SUMMARY.md
│   └── weekly-summary/            ← 周报汇总
│       └── week-01-summary.md
```

---

## 第0部分 阅读须知

### 0.1 为什么需要本 Teach 文档？

AI 编程工具能力强大，但**用得不好会产生大量低质量代码、幻觉、安全漏洞**。本项目是**企业级 + 竞赛级**双重标准，对代码质量、准确率、可维护性要求极高。我们必须**用对方法、用好工具**。

### 0.2 适用 AI 工具清单

| 工具 | 适用场景 | 推荐等级 |
|------|---------|---------|
| **Claude Code** | 全场景，复杂任务、架构设计 | ⭐⭐⭐⭐⭐ |
| **Cursor** | 前端开发、代码补全、Refactor | ⭐⭐⭐⭐⭐ |
| **Trae** | 中文场景、对话式开发 | ⭐⭐⭐⭐ |
| **GitHub Copilot** | 代码补全、单元测试 | ⭐⭐⭐⭐ |
| **通义灵码** | 中文代码补全 | ⭐⭐⭐ |
| **文心快码** | 中文代码生成 | ⭐⭐⭐ |
| **ChatGPT / Gemini** | 问题排查、方案咨询 | ⭐⭐⭐ |

> **强烈推荐**：本项目优先使用 **Claude Code / Cursor**，因为它们对大型代码库的理解能力更强。

### 0.3 阅读路径建议

- **首次阅读**：第1、2、3部分（必读，30分钟）
- **开发前**：阅读对应模块的第4部分
- **日常使用**：参考第5部分的提示词模板
- **遇到问题**：第7、9部分

---

## 第1部分 AI 工具使用总则

### 1.1 铁律七条（违反任何一条，PR将被拒绝）

#### 🚫 铁律1：不得让 AI 自主决策架构
```
❌ 错误做法："用AI帮我设计整个系统架构"
✅ 正确做法：架构由 Tech Lead 设计 → 让 AI 生成具体实现代码
```

#### 🚫 铁律2：不得直接采纳 AI 生成的业务逻辑代码
```
❌ 错误做法：AI 生成的匹配算法直接 merge 到 main
✅ 正确做法：AI 生成 → 开发者理解 → 单元测试验证 → 人工 review → merge
```

#### 🚫 铁律3：不得让 AI 凭空捏造数据或文档
```
✅ 必须做：让 AI 仅基于已提供的事实生成代码/文档
✅ 必须做：AI 提到的库/API 必须有官方文档支撑
```

#### 🚫 铁律4：必须保持人在回路（Human-in-the-Loop）
```
每一个 AI 生成的代码块 → 开发者必须：
1. 读懂（不理解则不用）
2. 测试（不通过则不用）
3. 优化（可改进则改）
4. 标注（用 # AI-Generated 注释标明）
```

#### 🚫 铁律5：必须锁定依赖版本
```
❌ 错误做法："pip install fastapi"（不锁版本）
✅ 正确做法：在 requirements.txt 锁定到 0.115.x，并让 AI 写明依据
```

#### 🚫 铁律6：准确率指标必须由人验证
```
JD解析准确率 / 简历提取准确率 / 匹配准确率 ≥ 90%
这3个核心指标必须由开发者基于测试集人工验证，
不能仅依赖 AI 的自我报告。
```

#### 🚫 铁律7：AI 生成的代码必须有完整测试
```
每个 AI 生成的函数/类 → 必须有对应单元测试
覆盖率 ≥ 项目平均水平
```

### 1.2 AI 工具的角色定位

| 角色 | 责任方 |
|------|--------|
| 架构师 | 人类（Tech Lead） |
| 算法设计 | 人类 + AI 协作 |
| 业务编码 | **AI 生成 → 人类审查** |
| 单元测试 | **AI 生成 → 人类补充** |
| 文档编写 | **AI 生成 → 人类校对** |
| Code Review | **人类主导**，AI 辅助 |
| 测试结果评估 | **人类主导** |
| 部署运维 | 人类（DevOps） |

### 1.3 信息安全规则

#### 不得向 AI 输入的内容
- 🚫 讯飞星火 API Key 等真实密钥
- 🚫 个人姓名、手机号、身份证号
- 🚫 内部未公开数据
- 🚫 公司商业机密
- 🚫 包含敏感信息的真实简历样本

#### 必须替换的内容
```
讯飞星火 APPID: XXXXXX  → 占位符: <SPARK_APPID>
讯飞星火 APIKey: XXXXXX → 占位符: <SPARK_API_KEY>
真实手机号: 13800001111 → 占位符: <PHONE_NUMBER>
```

---

## 第2部分 环境与工具准备

### 2.1 推荐开发环境

| 工具 | 版本 | 说明 |
|------|------|------|
| **Python** | 3.11.x | 后端开发 |
| **Node.js** | 20.x LTS | 前端开发 |
| **pnpm** | 9.x | 前端包管理（推荐，比npm快） |
| **Docker Desktop** | 4.x | 容器化 |
| **Git** | 2.40+ | 版本控制 |
| **VSCode** | 最新版 | 推荐IDE（与Cursor兼容） |
| **PyCharm** | 2024.x | Python开发（可选） |

### 2.2 必备 VSCode / Cursor 插件

```json
// .vscode/extensions.json
{
  "recommendations": [
    "ms-python.python",
    "ms-python.black-formatter",
    "ms-python.isort",
    "ms-python.mypy-type-checker",
    "vue.volar",
    "antfu.iconify",
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "ms-playwright.playwright",
    "ms-azuretools.vscode-docker",
    "hashicorp.terraform",
    "tamasfe.even-better-toml",
    "redhat.vscode-yaml"
  ]
}
```

### 2.3 项目级 AI 规则配置

#### 2.3.1 Cursor `.cursorrules`

在项目根目录创建 `.cursorrules` 文件（详见附录）。

#### 2.3.2 Claude Code `.claude/CLAUDE.md`

```markdown
# 项目 AI 协作规则

## 项目概述
本项目是"执图破局"——就业市场岗位-能力知识图谱动态构建与匹配平台。
赛题编号 XH-202621，发榜方：科大讯飞。
详见 @项目开发指南.md

## ⚠️ 强制约定（每次会话必须执行）

### 约定1：开发者身份识别
每次会话开始的第一件事，必须向开发者询问：
1. 您是项目组的哪位开发者？
   - Tech Lead（技术负责人）
   - 数据工程师（Data Engineer）
   - 后端/AI工程师（Backend & AI Engineer）
   - 前端工程师（Frontend Engineer）
   - 测试/DevOps工程师（QA & DevOps Engineer）
2. 您的姓名/花名（用于汇报文件署名）
3. 您本次会话的开发任务

### 约定2：自动生成项目汇报
当开发者说"完成"、"结束"、"搞定"、"保存"等关键词时，必须自动：
1. 收集汇报信息（通过 AskUserQuestion）
2. 生成汇报文件 → docs/09-daily-reports/YYYY-MM-DD/{角色}-{任务名}.md
3. 更新总进度看板 → docs/09-daily-reports/PROGRESS.md
4. 向开发者输出确认信息

## 5 个角色与代码所有权

| 角色 | 代码所有权目录 |
|------|---------------|
| Tech Lead | 架构、跨模块集成、核心算法 |
| 数据工程师 | /data-collection、/data-processing、/data-analysis |
| 后端/AI工程师 | /backend、/ai-core、/knowledge-graph |
| 前端工程师 | /frontend |
| 测试/DevOps | /tests、/.github、/docker、/scripts |

## 技术栈
- 后端：Python 3.11 + FastAPI 0.115.x + Neo4j 5.x + MySQL 8.0
- 前端：Vue 3.5 + Vite 5 + AntV G6 + ECharts
- AI：讯飞星火 X2 + BGE-M3 + LangChain 0.3
- 数据：Scrapy + Playwright + Elasticsearch 8

## 硬性指标
- JD解析准确率 ≥ 90%
- 简历提取准确率 ≥ 90%
- 匹配准确率 ≥ 90%
- 单元测试覆盖率 ≥ 60%

## 代码规范
- Python：Black (行宽100) + isort + flake8 + mypy
- TypeScript：ESLint + Prettier
- 必须写类型注解
- 必须写 docstring
- 关键函数必须有单元测试

## 重要提示
- 不要伪造API密钥，使用占位符 <SPARK_API_KEY>
- 不要编造不存在的库或API
- 生成的代码必须标注 # AI-Generated
- 所有外部依赖必须锁定版本
```

#### 2.3.3 Continue.dev 配置

`.continue/config.json`：

```json
{
  "models": [
    {
      "title": "Claude 4 Sonnet",
      "provider": "anthropic",
      "model": "claude-sonnet-4-5",
      "contextLength": 200000
    }
  ],
  "tabAutocompleteModel": {
    "title": "Claude 4 Sonnet (Tab)",
    "provider": "anthropic",
    "model": "claude-sonnet-4-5"
  }
}
```

---

## 第3部分 AI 协作开发的正确姿势

### 3.1 三阶段工作流（黄金法则）

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│   阶段1: 人类规划 (5分钟)                                  │
│   ├── 明确需求（用户故事、用例）                            │
│   ├── 设计接口（API签名、Schema）                          │
│   └── 列出实现要点（关键步骤、约束）                        │
│                          ↓                               │
│   阶段2: AI 生成 (10分钟)                                  │
│   ├── 提供上下文（项目结构、依赖、规范）                    │
│   ├── 给出明确指令（功能、输入输出、约束）                  │
│   └── 让 AI 生成初版代码 + 单元测试                        │
│                          ↓                               │
│   阶段3: 人类审查与优化 (15分钟)                            │
│   ├── 阅读理解每一行代码                                   │
│   ├── 运行测试验证正确性                                   │
│   ├── 优化性能与可读性                                     │
│   ├── 补充边界情况处理                                     │
│   └── 添加 # AI-Generated 注释                            │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 3.2 提示词工程（Prompt Engineering）核心技巧

#### 3.2.1 提示词黄金结构

```
# 角色
你是一位 [具体技术领域] 资深工程师，拥有 [X年] 经验...

# 上下文
本项目使用 [技术栈]，遵循 [规范]...

# 任务
实现 [具体功能]...

# 要求
1. [具体要求1]
2. [具体要求2]
3. [具体要求3]

# 输入
[具体的输入数据/文件路径]

# 输出
[期望的输出格式]

# 示例
[期望的代码示例]

# 约束
- 必须使用 [具体库版本]
- 必须写完整类型注解
- 必须写单元测试
```

#### 3.2.2 反例 vs 正例

##### ❌ 反例：模糊提示词
```
帮我写一个爬虫
```

##### ✅ 正例：结构化提示词
```
# 角色
你是一位 Python 后端工程师，擅长 Scrapy 爬虫开发。

# 上下文
本项目正在开发"执图破局"——岗位能力知识图谱平台。
需要从招聘网站爬取岗位JD数据。
项目位于 /data-collection/spiders/ 目录。

# 任务
实现一个 Scrapy 爬虫，爬取拉勾网 AI 工程师岗位 JD。

# 要求
1. 使用 Scrapy 2.12.x
2. 支持代理池（scrapy-rotating-proxies）
3. 输出字段：job_id, title, company, salary, location, skills, description, posted_date
4. 数据保存为 JSON Lines 格式
5. 遵守 robots.txt，爬取间隔 ≥ 3 秒
6. 必须有完整的异常处理和重试机制

# 输入
起始URL：https://www.lagou.com/wn/jobs?kd=AI%E5%B7%A5%E7%A8%8B%E5%B8%88

# 输出
1. spider 完整代码（lagou_spider.py）
2. items.py 定义
3. pipelines.py 数据清洗管道
4. 单元测试（test_lagou_spider.py）

# 约束
- 严禁硬编码任何真实 Cookie/Token
- 所有 API Key 使用占位符
- 代码必须符合 PEP 8 + Black 格式化
```

### 3.3 上下文管理（避免上下文污染）

#### 3.3.1 上下文加载策略

| 场景 | 应加载的上下文 |
|------|-------------|
| **新功能开发** | 需求文档 + 相关接口 + 依赖模块代码 |
| **Bug修复** | 错误日志 + 相关代码 + 最近commit |
| **代码重构** | 整个文件 + 单元测试 + 调用方 |
| **性能优化** | 性能瓶颈代码 + 调用链 + 监控数据 |

#### 3.3.2 上下文精简原则

```
❌ 不要：把整个项目丢给 AI（容易超出上下文窗口）
✅ 应该：只加载与当前任务直接相关的文件
```

#### 3.3.3 Cursor 上下文使用技巧

- `@Codebase` - 全局搜索相关代码（慎用，会消耗大量 tokens）
- `@File` - 指定单个文件
- `@Folder` - 指定目录
- `@Docs` - 加载官方文档
- `@Web` - 网络搜索（适合查最新API）

### 3.4 与 AI 对话的最佳实践

#### 3.4.1 一次只做一件事
```
❌ "帮我实现岗位发现、图谱构建、匹配算法三个功能"
✅ "帮我实现岗位发现功能的第一步：从JD文本中识别技能关键词"
```

#### 3.4.2 迭代式对话
```
第1轮："写一个简单的函数，计算两个列表的重合度"
第2轮："现在加入权重，核心技能权重2.0，普通技能权重1.0"
第3轮："再加异常处理：空列表、None值等"
第4轮："加单元测试"
```

#### 3.4.3 反馈与修正
```
如果 AI 生成的代码不符合预期：
1. 明确指出问题："这里的循环嵌套过深，请优化到 O(n) 复杂度"
2. 提供反例："如果传入 None 你的代码会崩溃，请处理"
3. 要求解释："请逐行解释你的代码"
4. 重新生成："按以下要求重写：..."
```

#### 3.4.4 保存有效的提示词
```
✅ 在 docs/06-development/ai-prompts/ 下建立提示词库
✅ 复用验证过的提示词（成功率高的模板）
✅ 持续迭代优化提示词
```

---

## 第4部分 分模块 AI 开发指南

### 模块一：项目初始化

**负责角色**：Tech Lead + 全员

#### 任务清单
- [ ] 创建项目目录结构
- [ ] 初始化 Git 仓库
- [ ] 搭建 Docker Compose
- [ ] 配置代码规范（Black、ESLint）
- [ ] 配置 CI/CD（GitHub Actions）
- [ ] 创建 README.md

#### 推荐提示词

```markdown
# 项目初始化提示词

请帮我初始化一个企业级 Python + Vue 3 全栈项目，目录结构如下：
[粘贴 项目开发指南.md 第4部分的目录结构]

要求：
1. 后端 Python 3.11 + FastAPI 0.115 + Poetry 依赖管理
2. 前端 Vue 3.5 + Vite 5 + TypeScript + Pinia
3. Docker Compose 一键启动 MySQL 8.0 + Neo4j 5.x + Redis 7 + Elasticsearch 8
4. 配置 Black + isort + flake8 + mypy
5. 配置 ESLint + Prettier
6. 创建 .gitignore（覆盖 Python、Node、IDE、Docker）
7. 创建 README.md（含项目介绍、技术栈、快速开始）
8. 创建 GitHub Actions CI 配置（lint + test + build）
9. 创建 pre-commit hooks 配置

输出：
1. 完整的目录结构
2. 所有配置文件内容
3. 一键启动脚本
```

---

### 模块二：多源数据采集

**负责角色**：数据工程师

#### 推荐技术
- Scrapy 2.12.x（静态页面）
- Playwright 1.48.x（JS动态页面）
- scrapy-rotating-proxies（代理池）

#### AI 开发步骤

##### Step 1: 设计数据 Schema

```markdown
# 提示词
设计岗位数据 Schema，要求：
1. 字段：job_id, title, company, salary_min, salary_max, location, experience, education, skills, description, posted_date, source, source_url, crawled_at
2. 使用 Pydantic v2 模型
3. 包含字段验证（如 salary_min < salary_max）
4. 输出 models/schemas/job.py
```

##### Step 2: 实现 Scrapy 爬虫

```markdown
# 提示词
实现一个 Scrapy 爬虫爬取拉勾网 AI 工程师岗位，要求：
1. 继承 BaseJDSpider 基类（我已实现，见 path/to/base.py）
2. 处理分页（最多100页）
3. 提取 skills 字段（从 description 中抽取）
4. 异常处理：网络超时、解析错误、反爬封禁
5. 数据存储到 MySQL（通过 pipeline）
6. 完整的单元测试

参考文件：
- 基类：@data-collection/spiders/base_spider.py
- 配置：@data-collection/settings.py
```

##### Step 3: 数据清洗管道

```markdown
# 提示词
实现 Scrapy Pipeline，对采集的JD数据进行清洗：
1. 去除 HTML 标签
2. 提取纯文本中的技能关键词（基于 skills_keyword_dict.json）
3. 薪资归一化（如"15-25K" → {min: 15000, max: 25000}）
4. 去重（基于 title + company + description 的 hash）
5. 数据质量评分（0-100）
6. 输出到 MySQL（使用 SQLAlchemy）

输入：raw_job dict
输出：cleaned_job dict + quality_score
```

#### 常见问题

| 问题 | 解决方案 |
|------|---------|
| 爬虫被反爬 | 让 AI 生成 User-Agent 轮换、IP代理池逻辑 |
| 数据格式不统一 | 让 AI 设计统一 Schema 并实现 adapter 模式 |
| 增量爬取 | 让 AI 设计基于 last_crawled_at 的增量逻辑 |

---

### 模块三：知识图谱构建

**负责角色**：后端 / AI 工程师

#### 推荐技术
- Neo4j 5.x（主数据库）
- py2neo 2021.x（Python客户端）
- HanLP 2.1.x（中文NER）
- 讯飞星火 X2（关系抽取）

#### AI 开发步骤

##### Step 1: Neo4j Schema 设计

```markdown
# 提示词
设计 Neo4j 图谱 Schema，要求：
1. 节点：Job, Skill, Industry, Category, Company
2. 关系：REQUIRES, BELONGS_TO, RELATED_TO, EVOLVES_TO
3. 每个节点带：created_at, updated_at, source, version
4. 支持时序查询（某技能过去N个月的需求变化）
5. 输出：
   - schema.cypher（Cypher脚本）
   - app/knowledge_graph/schema.py（Python封装）
   - 索引和约束定义
   - 示例数据
```

##### Step 2: NER 实体抽取

```markdown
# 提示词
实现技能实体抽取模块，要求：
1. 输入：JD 文本（中文）
2. 输出：技能列表（去重、标准化）
3. 方法：
   a. 先用 HanLP 做基础 NER
   b. 用自定义词典匹配（skill_dict.json）
   c. 用讯飞星火做 few-shot 抽取（处理未登录词）
4. 实体消歧：
   - "ML" → "机器学习"
   - "DL" → "深度学习"
   - "Java开发" 与 "Java工程师" 视为同类岗位
5. 性能：单条 JD 处理 < 500ms
6. 完整单元测试（accuracy ≥ 90%）
```

##### Step 3: 关系抽取（Prompt Engineering）

```markdown
# 提示词
设计讯飞星火 Prompt，用于从岗位定义中抽取能力要求关系：

输入示例：
"招聘AI算法工程师，要求掌握Python、PyTorch，熟悉LLM，有3年经验"

期望输出（JSON）：
{
  "job_title": "AI算法工程师",
  "required_skills": [
    {"name": "Python", "level": "required", "weight": 1.0},
    {"name": "PyTorch", "level": "required", "weight": 1.0}
  ],
  "preferred_skills": [
    {"name": "LLM", "level": "preferred", "weight": 0.7}
  ],
  "experience_years": 3
}

Prompt 要求：
1. Few-shot 提供3个示例
2. 严格的 JSON 输出格式（即使有错也尝试输出）
3. 角色设定：你是 HR 专家
4. 温度参数：temperature=0.1
5. 提供错误处理：JSON 解析失败时重试
```

##### Step 4: 图谱入库

```markdown
# 提示词
实现 Neo4j 入库模块，要求：
1. 使用 py2neo 或 neo4j Python Driver
2. 批量写入（每批500条）提升性能
3. 关系去重（避免重复边）
4. 事务管理（保证原子性）
5. 索引自动创建
6. 写入失败回滚
7. 单元测试（使用 testcontainers 启动 Neo4j）
```

---

### 模块四：AI 智能层

**负责角色**：后端 / AI 工程师

#### 推荐技术
- LangChain 0.3.x（RAG编排）
- ChromaDB 0.5.x（向量存储）
- BGE-M3（中文向量化）
- 讯飞星火 X2（主推理）

#### AI 开发步骤

##### Step 1: LLM 客户端封装

```markdown
# 提示词
封装讯飞星火 API 客户端，要求：
1. 支持同步/异步调用
2. 实现指数退避重试（最多3次）
3. 实现多模型路由：
   - 通用对话：Spark X2
   - 复杂推理：Spark X1.5
   - 成本优化：Spark Lite
4. 实现 Function Calling 支持
5. 完整的 token 用量统计
6. 完整的异常分类（限流、超时、内容安全等）
7. Pydantic 配置管理
8. 单元测试（mock API）
```

##### Step 2: RAG 链路

```markdown
# 提示词
实现 RAG 检索增强生成链路：

输入：用户查询（如"AI算法工程师需要掌握哪些技能？"）
输出：基于图谱知识库的答案

实现步骤：
1. 查询理解（意图识别、关键词提取）
2. 向量化（BGE-M3）
3. ChromaDB 检索 top-5 相关节点
4. Neo4j 检索相关关系（图谱展开）
5. 上下文拼装（图谱 + 向量检索结果）
6. 讯飞星火生成最终答案
7. 幻觉检测（基于图谱事实校验）

要求：
1. 端到端响应时间 ≤ 5秒
2. 支持流式输出（SSE）
3. 答案必须可溯源（标注引用节点）
4. 完整单元测试
```

##### Step 3: 幻觉检测

```markdown
# 提示词
实现大模型生成内容的幻觉检测模块：

方法：
1. 事实验证：将生成内容中的关键事实（实体、关系、数字）
   与 Neo4j 图谱进行回查
2. 一致性校验：多次生成结果对比（self-consistency）
3. 不确定性表达：要求 LLM 给出置信度分数

输入：generated_text, source_query
输出：
{
  "has_hallucination": bool,
  "confidence_score": float,  # 0-1
  "unverified_claims": [str],
  "verified_claims": [str]
}

要求：
1. 召回率 ≥ 95%（不能漏掉幻觉）
2. 准确率 ≥ 85%（不能误报）
3. 处理时间 ≤ 2秒
4. 完整单元测试（提供正反例样本）
```

---

### 模块五：人岗匹配算法

**负责角色**：后端 / AI 工程师

#### 核心算法

```
匹配分数 = 0.4 × 技能重合度
         + 0.3 × 语义相似度（基于 Embedding）
         + 0.2 × 图谱距离得分
         + 0.1 × 经验匹配度
```

#### AI 开发步骤

##### Step 1: 简历解析

```markdown
# 提示词
实现简历解析模块，要求：
1. 支持 PDF 和 Word 格式
2. 提取字段：
   - 基本信息：姓名、电话、邮箱
   - 教育背景：学校、专业、学历、起止时间
   - 工作经历：公司、职位、时间段、职责
   - 项目经历：项目名、角色、技术栈、成果
   - 技能列表：技能名称、熟练度
   - 证书/奖项
3. 处理复杂排版（多栏、表格、图片）
4. OCR 支持（扫描件 PDF，使用讯飞 OCR）
5. 字段标准化（如"硕士" → "Master"）
6. 准确率 ≥ 90%（测试集50份简历）

技术栈：
- PDF: pdfplumber + PyPDF2
- Word: python-docx
- OCR: 讯飞 OCR API
- NER: HanLP + 讯飞星火
```

##### Step 2: 多维度匹配评分

```markdown
# 提示词
实现多维度人岗匹配评分器：

输入：候选人画像（parsed_resume）, 岗位画像（job_info）
输出：
{
  "overall_score": float,  # 0-100
  "skill_match": {
    "score": float,
    "matched_skills": [str],
    "missing_skills": [str],
    "extra_skills": [str]
  },
  "semantic_match": {
    "score": float,
    "explanation": str
  },
  "graph_distance": {
    "score": float,
    "path_length": int,
    "related_skills": [str]
  },
  "experience_match": {
    "score": float,
    "candidate_years": int,
    "required_years": int
  },
  "recommendation": str,  # "强烈推荐" / "推荐" / "一般" / "不推荐"
  "gap_analysis": str,  # 差距分析（自然语言）
  "learning_path": [str]  # 学习路径建议
}

算法要求：
1. 技能重合度：Jaccard相似度 + 权重（核心技能×2）
2. 语义相似度：BGE-M3 embedding + cosine similarity
3. 图谱距离：Neo4j shortestPath
4. 经验匹配：考虑候选人所有经历年限

准确率 ≥ 90%（测试集100条）
```

##### Step 3: 学习路径推荐

```markdown
# 提示词
基于人岗差距分析，生成个性化学习路径：

输入：missing_skills（缺失技能列表）, candidate_profile（候选人画像）
输出：3-5 阶段学习路径

要求：
1. 按优先级排序（核心技能优先）
2. 估算学习时长
3. 推荐学习资源（文档、课程、实战项目）
4. 阶段化、可执行
5. 基于讯飞星火生成（结合 prompt engineering）

示例输出：
{
  "stages": [
    {
      "stage": 1,
      "skill": "PyTorch",
      "priority": "high",
      "estimated_weeks": 2,
      "resources": ["PyTorch官方教程", "动手学深度学习"],
      "milestones": ["完成MNIST分类", "实现ResNet"]
    }
  ],
  "total_estimated_weeks": 8
}
```

---

### 模块六：前端开发

**负责角色**：前端工程师

#### 推荐 AI 工具
- **Cursor**（强烈推荐，支持 inline edit、composer）
- **Claude Code**（适合复杂页面）
- **GitHub Copilot**（代码补全）

#### AI 开发步骤

##### Step 1: 项目脚手架

```markdown
# 提示词
使用 Vite 5 创建一个 Vue 3 + TypeScript 项目：
- 集成 Vue Router 4
- 集成 Pinia 2
- 集成 Ant Design Vue 4
- 集成 Axios（封装请求拦截器）
- 集成 ESLint + Prettier
- 集成 Vitest（单元测试）
- 配置路径别名 @ → src
- 配置代理（开发环境转发到后端 http://localhost:8000）

输出 package.json 和 vite.config.ts
```

##### Step 2: 图谱可视化页面（核心亮点）

```markdown
# 提示词
实现知识图谱可视化页面，要求：
1. 使用 AntV G6 5.x
2. 力导向布局，支持拖拽、缩放
3. 节点类型：
   - Job（蓝色圆形）
   - Skill（橙色方形）
   - Industry（绿色菱形）
   - Company（灰色圆形）
4. 边的样式根据关系类型区分颜色
5. 交互：
   - 点击节点显示详情面板
   - 双击节点展开/收起子图
   - 鼠标悬停高亮关联节点
6. 筛选器：
   - 按技术栈（前端/后端/AI）
   - 按级别（初中高）
   - 按时间范围
7. 支持 10000+ 节点流畅渲染（虚拟化）
8. TypeScript 完整类型定义
9. 组件化、可复用
```

##### Step 3: 简历上传与匹配页面

```markdown
# 提示词
实现简历上传与人岗匹配结果展示页面：

页面布局：
1. 上传区：
   - 拖拽上传 / 点击选择
   - 支持 PDF、Word
   - 文件大小 ≤ 10MB
   - 上传进度条
2. 加载状态：
   - 解析中（带动画）
   - 预计耗时提示
3. 解析结果展示：
   - 候选人基本信息卡片
   - 技能标签云
   - 工作经历时间轴
4. 匹配岗位列表：
   - Top 10 匹配岗位
   - 每个岗位卡片显示：匹配分数、缺失技能、推荐理由
   - 支持按分数排序、按行业筛选
5. 差距分析图表：
   - 雷达图（候选人 vs 岗位要求）
   - 柱状图（各项技能得分）
6. 学习路径时间轴：
   - 阶段化展示
   - 每个阶段可展开看资源

技术栈：Vue 3 + TypeScript + Ant Design Vue + ECharts
要求：响应式、加载态、错误处理
```

##### Step 4: 数据看板

```markdown
# 提示词
实现系统数据看板，要求：
1. 关键指标卡片：
   - 岗位总数
   - 技能点总数
   - 简历解析总数
   - 匹配服务调用次数
2. 趋势图表（ECharts）：
   - 岗位增长趋势（按月）
   - 热门技能 Top 20
   - 行业分布饼图
3. 数据刷新：
   - 手动刷新按钮
   - 自动刷新（30秒间隔，可关闭）
4. 导出功能：导出为 Excel
```

---

### 模块七：测试与质量保障

**负责角色**：测试 / DevOps 工程师

#### AI 开发步骤

##### Step 1: 测试数据生成

```markdown
# 提示词
生成100条岗位JD测试数据集：

要求：
1. JSON Lines 格式，每行一个岗位
2. 覆盖10个行业：AI、大数据、云计算、前端、后端、运维、产品、运营、设计、数据分析
3. 覆盖3个级别：初级、中级、高级
4. 每条JD包含：job_id, title, company, description, skills, posted_date
5. description 至少200字，真实自然
6. skills 至少5个，多样化
7. 字段格式规范

输出：data/test/jd_test_set_v1.jsonl（100条）
```

##### Step 2: 准确率评估脚本

```markdown
# 提示词
实现三大准确率评估脚本：

1. JD解析准确率评估（tests/accuracy/test_jd_parse_accuracy.py）
2. 简历提取准确率评估（tests/accuracy/test_resume_extract_accuracy.py）
3. 人岗匹配准确率评估（tests/accuracy/test_match_accuracy.py）

通用要求：
1. 加载测试集（JSON/JSONL）
2. 调用对应服务
3. 计算准确率
4. 生成详细报告：
   - 总样本数
   - 正确数
   - 错误数
   - 各类别准确率
   - 失败案例（前10个）
5. 报告保存到 reports/accuracy/
6. 阈值断言（≥ 90%）
7. CI 友好：失败时非零退出码

设计原则：
- 可独立运行：python tests/accuracy/eval_jd.py
- 支持指定测试集路径：--test-set path/to/data.jsonl
- 支持指定服务URL：--api-url http://localhost:8000
- 输出 Markdown 报告
```

##### Step 3: 集成测试

```markdown
# 提示词
实现核心流程集成测试：

测试用例：
1. 数据采集 → 入库 → 图谱构建（端到端）
2. 简历上传 → 解析 → 匹配 → 报告（完整链路）
3. RAG 查询 → 图谱检索 → 答案生成（智能问答）

要求：
1. 使用 pytest + httpx（异步HTTP）
2. 使用 docker-compose 启动测试依赖
3. 使用 fixtures 准备测试数据
4. 断言业务结果准确性
5. 测试报告自动生成（HTML）
```

---

## 第5部分 提示词模板库

### 5.1 通用模板

#### 模板1：代码生成

```markdown
# 角色
你是一位 [技术领域] 资深工程师，拥有 [X] 年 [项目类型] 开发经验。

# 项目背景
[简要描述项目，2-3句话]

# 技术栈
- [语言/框架及版本]
- [关键库及版本]
- [工具及版本]

# 任务
实现 [具体功能描述]

# 输入
[输入数据/参数]

# 输出
[期望输出]

# 详细要求
1. [要求1]
2. [要求2]
3. [要求3]

# 代码规范
- 必须写完整类型注解
- 必须写 docstring（Google 风格）
- 必须写单元测试
- 关键函数必须有异常处理
- 必须使用 logging 记录关键步骤

# 约束
- 严禁使用 [禁用项]
- 必须使用 [强制项]

# 参考
- 官方文档：[URL]
- 类似实现：[URL]
```

#### 模板2：Bug修复

```markdown
# Bug 描述
[具体描述 bug 现象]

# 复现步骤
1. [步骤1]
2. [步骤2]
3. [步骤3]

# 期望行为
[期望的正确行为]

# 实际行为
[实际发生的错误]

# 错误日志
```
[粘贴错误堆栈]
```

# 相关代码
@[文件路径]

# 环境信息
- Python 版本：
- 相关库版本：
- 操作系统：

# 修复要求
1. 找到根本原因
2. 修复 Bug
3. 添加回归测试
4. 解释修复原理
```

#### 模板3：代码重构

```markdown
# 当前代码
@[文件路径]

# 重构目标
[目标描述，如：提升性能 / 提升可读性 / 解耦]

# 重构要求
1. [具体要求1]
2. [具体要求2]

# 约束
- 保持外部接口不变
- 必须通过所有现有测试
- 必须提升性能（提供 benchmark 对比）
```

#### 模板4：单元测试

```markdown
# 任务
为 @[文件路径] 编写完整的单元测试

# 测试要求
1. 覆盖所有公开函数
2. 覆盖正常路径和异常路径
3. 覆盖边界情况（空值、极值、None）
4. 使用 pytest + pytest-mock
5. 测试覆盖率 ≥ 90%
6. 测试用例名称描述性强

# 测试框架
[pytest / unittest]

# Mock 要求
[需要 mock 的外部依赖]

# 输出
tests/test_[module].py
```

### 5.2 项目专用模板

#### 模板5：Neo4j 查询优化

```markdown
# 当前查询
```cypher
[粘贴 Cypher 查询]
```

# 性能问题
- 当前耗时：X 秒
- 数据规模：节点 Y 个，边 Z 个

# 优化要求
1. 分析查询计划（EXPLAIN/PROFILE）
2. 添加必要索引
3. 优化查询结构
4. 提供优化前后的对比
```

#### 模板6：大模型 Prompt 优化

```markdown
# 当前 Prompt
[粘贴 Prompt]

# 当前问题
[准确率低 / 格式错误 / 速度慢 / 幻觉多]

# 测试样本
[提供 5 个测试输入和期望输出]

# 优化要求
1. Few-shot 示例（3-5 个）
2. 明确的输出格式（JSON Schema）
3. 角色设定
4. 思维链（Chain of Thought）
5. 温度参数调优
6. 提供测试方法
```

#### 模板7：项目汇报生成（开发者完成某项任务时使用）

```markdown
# 任务
生成项目汇报文件

# 必填信息
1. 开发者姓名/花名：[姓名]
2. 角色：[Tech Lead / 数据工程师 / 后端AI工程师 / 前端工程师 / 测试DevOps工程师]
3. 任务名称：[一句话描述本次任务]
4. 完成情况：
   - 已完成：[列举]
   - 进行中：[列举]
   - 未完成：[列举及原因]
5. 涉及文件：
   - 新增：[路径]
   - 修改：[路径]
6. 测试情况：
   - 单元测试覆盖率：XX%
   - 准确率：XX%（如适用）
7. 遇到的问题：[描述]
8. 解决方案：[描述]
9. 下一步计划：[列举]
10. 需其他角色协助：[列举]

# 输出要求
1. 创建目录：docs/09-daily-reports/YYYY-MM-DD/
2. 保存文件：{角色英文}-{任务名}.md
3. 命名规范：tech-lead / data-engineer / backend-engineer / frontend-engineer / qa-devops
4. 使用本 teach 文档 0.5.3 节提供的模板
5. 更新 PROGRESS.md 中的对应模块进度
6. 输出确认信息（含文件路径和摘要）
```

#### 模板8：会话结束触发汇报

当开发者说"完成"、"结束"、"搞定"、"保存进度"等关键词时使用。

```markdown
# 触发条件
检测到开发者表达任务结束

# AI 立即执行的动作
1. 使用 AskUserQuestion 收集汇报信息：
   - Q1: 本次完成了哪些工作？（多选）
   - Q2: 是否所有计划任务都完成了？
   - Q3: 是否遇到阻塞或风险？
   - Q4: 需要其他角色协助的事项？
   - Q5: 下一步计划？
2. 基于收集的信息生成汇报文件
3. 更新 PROGRESS.md
4. 输出确认消息

# 询问话术示例
"任务完成前，请确认几个信息：
① 本次完成了哪些工作？（请详细列举）
② 修改/新增了哪些文件？
③ 测试情况如何？（覆盖率、准确率）
④ 遇到了什么问题？是如何解决的？
⑤ 下一步计划做什么？
⑥ 是否需要其他角色协助？

确认后，我会自动生成汇报文件到 docs/09-daily-reports/YYYY-MM-DD/"
```

---

## 第6部分 各岗位 AI 工作流

> **本部分内容已根据"约定1：身份识别"调整。每个岗位都明确了 AI 应加载的上下文、代码所有权和典型任务清单。**

### 6.0 通用 AI 工作流前置步骤（5个角色通用）

#### AI 启动后的标准动作

```yaml
动作1_身份确认:
  时机: 会话开始
  AI行为: |
    👋 输出欢迎语，询问 3 个问题：
    1. 您的角色？
    2. 您的姓名？
    3. 本次任务？

动作2_上下文加载:
  时机: 收到回答后
  AI行为: |
    根据角色，加载：
    - @项目开发指南.md 中该角色的章节
    - 该角色的代码目录
    - 相关提示词模板

动作3_职责边界声明:
  时机: 开始工作前
  AI行为: |
    输出：
    "✅ 已识别您为【{角色}】{姓名}
    📂 代码所有权：{目录列表}
    🎯 本次任务：{任务}
    ⚠️ 提醒：{该角色的注意事项}"
```

#### 任务结束时的 AI 标准动作

```yaml
动作1_触发检测:
  时机: 检测到"完成/结束/搞定/OK/保存"等关键词
  AI行为: 立即进入"汇报生成流程"

动作2_信息收集:
  时机: 汇报生成前
  AI行为: |
    使用 AskUserQuestion 收集：
    1. 本次完成的工作
    2. 涉及的文件
    3. 测试情况
    4. 遇到的问题与解决方案
    5. 下一步计划
    6. 需要其他角色协助的事项

动作3_生成文件:
  时机: 信息收集后
  AI行为: |
    1. 创建目录 docs/09-daily-reports/{YYYY-MM-DD}/
    2. 生成汇报文件 {角色}-{任务名}.md
    3. 更新 PROGRESS.md

动作4_确认输出:
  时机: 文件生成后
  AI行为: |
    向开发者输出：
    ✅ 汇报文件路径
    📋 汇报摘要
    📊 进度更新情况
```

---

### 6.1 Tech Lead 工作流

#### 角色专属上下文

```
代码所有权：
├── 项目架构设计
├── /backend/app/core/（核心配置）
├── /backend/app/services/（业务服务编排）
├── 跨模块集成
└── /docs/01-architecture/、/docs/02-design/

主要工作：
- 架构设计与决策
- ADR（架构决策记录）撰写
- 跨模块集成方案
- 技术难点攻关
- 代码审查
- 整体进度把控
```

#### 每日工作流

```markdown
1. 【架构决策】
   提示词模板：
   "我需要为 [功能] 做出架构决策。备选方案有 A、B、C。
   请对比三者的：
   - 性能
   - 可维护性
   - 学习成本
   - 与项目技术栈的契合度
   - 团队成员的熟悉度
   给出推荐方案及理由。"

2. 【代码审查】
   提示词模板：
   "请审查以下 PR 的代码变更：
   @[PR URL 或 diff]
   重点关注：
   - 架构一致性
   - 性能影响
   - 安全风险
   - 测试覆盖
   给出 review 意见。"

3. 【技术调研】
   提示词模板：
   "调研 [技术/库]，对比 [同类技术]：
   - 功能特性
   - 性能基准
   - 社区活跃度
   - 学习曲线
   - 与项目技术栈的兼容性
   给出推荐。"

4. 【文档审核】
   提示词模板：
   "审核 @[文档路径] 的完整性、准确性、可读性。"
```

### 6.2 后端 / AI 工程师工作流

#### 角色专属上下文

```
代码所有权：
├── /backend/（后端整体）
├── /backend/app/ai/（AI能力层：LLM/NER/RAG）
├── /backend/app/knowledge_graph/（图谱构建与查询）
├── /backend/app/matching/（人岗匹配算法）
├── /backend/app/services/（业务服务）
└── /backend/tests/

主要工作：
- FastAPI 后端开发
- 讯飞星火API集成与Prompt工程
- NER 模型微调
- Neo4j 图谱 Schema 与 Cypher
- RAG 链路编排
- 简历解析与人岗匹配算法
```

```markdown
1. 【功能开发】
   第1步：用 AI 生成代码框架
   第2步：开发者审查、补全、修改
   第3步：用 AI 生成单元测试
   第4步：开发者补充边界用例
   第5步：运行测试，确保通过

2. 【接口设计】
   "请帮我设计 [功能] 的 RESTful API：
   - 接口路径
   - 请求参数（类型、是否必填、说明）
   - 响应格式（JSON Schema）
   - 错误码定义
   - 示例请求和响应
   输出 OpenAPI YAML。"

3. 【SQL/Cypher 编写】
   "帮我编写 [业务需求] 的 SQL/Cypher：
   - 业务需求：[描述]
   - 数据规模：[节点/记录数]
   - 性能要求：[QPS]
   - 索引建议
   - 查询计划分析"

4. 【大模型 Prompt 优化】
   "我的 Prompt 当前效果不佳：
   [粘贴 Prompt]
   测试集：
   [输入1] → 期望输出1
   [输入2] → 期望输出2
   当前问题：[准确率 X%]
   请优化 Prompt，目标准确率 ≥ Y%。"
```

### 6.3 数据工程师工作流

#### 角色专属上下文

```
代码所有权：
├── /data-collection/（爬虫集群）
│   ├── /spiders/（Scrapy 爬虫）
│   ├── /pipelines/（数据管道）
│   └── /middlewares/（中间件）
├── /data-processing/（数据处理）
│   ├── /etl/（ETL管道）
│   ├── /cleaning/（数据清洗）
│   └── /quality/（数据质量评估）
└── /backend/app/data-analysis/（数据分析）

主要工作：
- Scrapy + Playwright 多源爬虫
- ETL 管道（去重/格式统一/质量评分）
- 时序数据管理（数据版本化）
- NetworkX 图算法（中心性、社区发现）
- 时间序列分析与趋势预测
- 数据质量评估体系
```

```markdown
1. 【爬虫开发】
   "实现爬虫 [目标网站]：
   - 目标数据：[字段]
   - 反爬策略：[已知限制]
   - 数据量级：[预期数据量]
   - 输出：[存储格式/位置]
   要求：遵守 robots.txt、可配置代理池、异常处理完善。"

2. 【数据清洗】
   "对 [数据源] 进行数据清洗：
   - 输入：[原始数据 schema]
   - 清洗规则：
     1. [规则1]
     2. [规则2]
   - 质量评分维度：[维度]
   - 输出：[清洗后 schema]
   实现为 Pandas Pipeline。"

3. 【ETL 调度】
   "设计 ETL 任务调度方案：
   - 任务依赖图
   - 失败重试策略
   - 监控告警
   - 数据校验
   使用 Airflow / Celery / 自研"
```

### 6.4 前端工程师工作流

#### 角色专属上下文

```
代码所有权：
├── /frontend/（前端整体）
│   ├── /src/views/（页面组件）
│   │   ├── /Dashboard/（数据看板）
│   │   ├── /KnowledgeGraph/（图谱可视化）
│   │   ├── /JobMatch/（人岗匹配）
│   │   ├── /DataCollection/（数据采集）
│   │   └── /Settings/（系统设置）
│   ├── /src/components/（通用组件）
│   ├── /src/stores/（Pinia 状态）
│   ├── /src/api/（API 封装）
│   └── /src/router/（路由）
└── /frontend/tests/（前端测试）

主要工作：
- Vue 3 + Vite + TypeScript 项目搭建
- AntV G6 图谱可视化（核心亮点）
- ECharts 趋势图表
- 简历上传与匹配结果展示
- 对话式查询界面（自然语言问答）
- 响应式设计与交互优化
```

```markdown
1. 【组件开发】（Cursor 优势）
   在 Cursor 中：
   - 用 Cmd+K 选中代码 → "Convert this to Vue 3 Composition API"
   - 用 Cmd+L 选中函数 → "Add JSDoc comments"
   - 用 Composer 描述需求 → 生成完整组件

2. 【UI 还原】
   "基于 @[设计稿/截图] 实现 UI：
   - 使用 [Ant Design Vue / Element Plus]
   - 响应式设计
   - 完整交互（hover、active、disabled）
   - TypeScript 完整类型"

3. 【可视化】
   "使用 [AntV G6 / ECharts] 实现 [图表类型]：
   - 数据：[data schema]
   - 交互：[交互要求]
   - 样式：[配色/主题]
   - 性能：[数据规模]"

4. 【Bug 修复】
   "修复 Vue 组件 bug：
   - 现象：[描述]
   - 复现步骤：[步骤]
   - 相关代码：@[路径]
   - 控制台报错：[错误]
   请分析原因并修复。"
```

### 6.5 测试 / DevOps 工程师工作流

#### 角色专属上下文

```
代码所有权：
├── /tests/（测试用例）
│   ├── /unit/（单元测试）
│   ├── /integration/（集成测试）
│   ├── /e2e/（端到端测试）
│   ├── /accuracy/（准确率测试）
│   └── /fixtures/（测试数据）
├── /.github/（GitHub Actions）
│   └── /workflows/（CI/CD 配置）
├── /docker/（Docker 配置）
├── /scripts/（运维脚本）
└── /docs/04-deployment/（部署文档）

主要工作：
- 测试用例设计与维护（覆盖率 ≥ 60%）
- 三大准确率评估（JD/简历/匹配）
- Docker 镜像构建与一键部署
- GitHub Actions CI/CD 流水线
- 监控告警与日志系统
- 数据库备份与恢复
```

```markdown
1. 【测试用例生成】
   "为 @[服务/接口] 生成测试用例：
   - 正常路径
   - 异常路径
   - 边界情况
   - 安全测试（注入、越权）
   - 性能测试
   使用 pytest + pytest-mock。"

2. 【Docker 镜像优化】
   "优化 Dockerfile：
   - 减小镜像体积（多阶段构建）
   - 提升构建速度（缓存优化）
   - 安全加固（非root用户）
   - 健康检查
   当前大小：X MB
   目标大小：Y MB"

3. 【CI/CD 流水线】
   "设计 CI/CD 流水线：
   - 触发条件
   - 阶段划分
   - 并行任务
   - 缓存策略
   - 部署策略（蓝绿/灰度）
   使用 GitHub Actions。"

4. 【监控告警】
   "设计监控告警方案：
   - 监控指标（业务 + 系统）
   - 告警阈值
   - 告警渠道（钉钉/飞书/邮件）
   - 升级策略
   使用 Prometheus + Grafana + AlertManager。"
```

---

## 第7部分 常见场景与最佳实践

### 7.1 场景1：从零开发一个新功能

#### 完整流程（30分钟）

```
0-5分钟：人类规划
├── 写用户故事："作为 [角色]，我希望 [功能]，以便 [价值]"
├── 列验收标准
└── 设计接口草图

5-20分钟：AI 生成
├── 给 AI 提供完整上下文
├── 用模板提示词
├── 生成初版代码 + 测试
└── 让 AI 解释关键设计决策

20-30分钟：人类审查与完善
├── 阅读理解每一行
├── 运行测试
├── 优化代码（命名、错误处理、注释）
└── 提交 PR
```

#### 示例：开发"技能匹配度计算"功能

```python
# 1. 人类先写出函数签名和注释

def calculate_skill_match_score(
    candidate_skills: List[str],
    job_required_skills: List[str],
    job_preferred_skills: List[str],
    skill_weights: Optional[Dict[str, float]] = None
) -> Dict[str, any]:
    """计算候选人与岗位的技能匹配度
    
    综合考虑必备技能和加分技能，使用加权Jaccard相似度。
    
    Args:
        candidate_skills: 候选人掌握的技能
        job_required_skills: 岗位必备技能
        job_preferred_skills: 岗位加分技能
        skill_weights: 技能权重（可选），如 {"Python": 2.0}
        
    Returns:
        {
            "score": float,  # 0-100
            "matched_required": List[str],
            "matched_preferred": List[str],
            "missing_required": List[str],
            "extra_skills": List[str]
        }
        
    Examples:
        >>> calculate_skill_match_score(
        ...     ["Python", "ML"],
        ...     ["Python", "PyTorch"],
        ...     ["LLM"]
        ... )
        {'score': 50.0, ...}
    """
    pass  # TODO: 实现


# 2. 让 AI 基于签名实现

提示词：
"实现 calculate_skill_match_score 函数，要求：
- 必备技能权重 1.0
- 加分技能权重 0.5
- score = (必备技能命中数 + 加分技能命中数 × 0.5) / (必备技能总数 + 加分技能总数 × 0.5) × 100
- 处理空列表、None 值
- 函数签名和 docstring 已在文件中，参考 [文件路径]"

# 3. AI 生成 → 开发者审查 → 测试 → merge
```

### 7.2 场景2：阅读并理解陌生代码

```markdown
# 提示词
请解释 @[文件路径] 的代码：
1. 这个文件实现什么功能？
2. 关键函数的作用和算法
3. 数据流向（输入 → 处理 → 输出）
4. 依赖的外部库
5. 潜在的问题或改进点

请用 Markdown 格式输出，结构清晰。
```

### 7.3 场景3：性能优化

```markdown
# 提示词
优化 @[文件路径] 的性能：

当前性能：
- 接口 P95 响应时间：X ms
- CPU 占用：Y%
- 内存占用：Z MB

性能瓶颈（基于 profile 结果）：
- 函数 A 耗时占比 60%
- 数据库查询 N+1 问题

优化要求：
1. 缓存热点数据（Redis）
2. 批量查询替代循环查询
3. 异步化耗时操作
4. 优化算法复杂度

提供：
1. 优化后的代码
2. 优化前后的 benchmark 对比
3. 性能分析报告
```

### 7.4 场景4：编写复杂 SQL / Cypher

```markdown
# 提示词
编写 Cypher 查询，需求：

业务场景：分析某技能在过去12个月的需求趋势
数据规模：
- 节点 Job 100万
- 节点 Skill 5万
- 关系 REQUIRES 500万

查询要求：
1. 时间范围：过去12个月
2. 技能：Python
3. 输出：每个月的需求次数

性能要求：
- 响应时间 < 500ms

请提供：
1. Cypher 查询语句
2. 必要的索引建议
3. 查询计划分析（EXPLAIN）
4. 优化版本（如需要）"
```

### 7.5 场景5：Debug 大模型生成内容问题

```markdown
# 提示词
我使用讯飞星火生成岗位定义，但经常出现以下问题：

问题1：JSON 格式错误（10%概率）
问题2：虚构不存在的技能（20%概率）
问题3：技能权重不合理（30%概率）

当前 Prompt：
[粘贴 Prompt]

测试样本：
输入1 → 输出1（问题）
输入2 → 输出2（正常）

请优化 Prompt，要求：
1. JSON 格式严格（提供 JSON Schema）
2. 严格基于提供的上下文生成（防虚构）
3. 技能权重合理（提供权重参考表）
4. Few-shot 示例（5个）

输出：
1. 优化后的 Prompt
2. 评估方法
3. 准确率提升预期
```

### 7.6 场景6：生成项目文档

```markdown
# 提示词
为 @[代码路径] 生成 API 文档：

要求：
1. 接口说明（路径、方法、用途）
2. 请求参数（类型、是否必填、说明、示例）
3. 响应格式（JSON Schema + 示例）
4. 错误码定义
5. 调用示例（curl / Python）
6. 注意事项

输出格式：Markdown

参考：@docs/03-api/api-template.md
```

### 7.7 场景7：设计测试用例

```markdown
# 提示词
为 @[函数/接口] 设计完整测试用例：

要求：
1. 单元测试（覆盖所有分支）
2. 边界测试（空值、极值、特殊字符）
3. 异常测试（错误输入、网络异常）
4. 性能测试（基准测试）
5. 安全测试（注入、越权）

测试框架：pytest
覆盖率目标：≥ 90%

输出：
1. tests/test_[module].py
2. 测试用例说明文档
```

---

## 第8部分 AI 生成代码审查清单

> **每个 PR 提交前**，开发者必须对照本清单自审。

### 8.1 通用审查项

- [ ] 代码是否符合项目代码规范（Black / ESLint 通过）
- [ ] 是否有完整的类型注解
- [ ] 是否有完整的 docstring
- [ ] 关键逻辑是否有注释（解释 Why，不是 What）
- [ ] 是否有 AI-Generated 注释（标识非人工原创部分）
- [ ] 是否处理了所有异常路径
- [ ] 是否有单元测试（覆盖率达标）
- [ ] 测试用例是否覆盖边界情况
- [ ] 是否有敏感信息（API Key、个人信息）
- [ ] 是否有硬编码（应使用配置或环境变量）
- [ ] 是否有 SQL 注入 / XSS 等安全风险
- [ ] 是否考虑了并发场景
- [ ] 是否考虑了性能瓶颈
- [ ] 是否有死代码或未使用的 import
- [ ] 命名是否符合规范（变量、函数、类）

### 8.2 后端专项审查

- [ ] API 路径是否符合 RESTful 规范
- [ ] 是否有统一的响应格式
- [ ] 是否有完整的错误处理
- [ ] 是否有请求参数验证
- [ ] 是否有日志记录（关键操作）
- [ ] 是否有事务管理（数据一致性）
- [ ] 数据库查询是否有索引
- [ ] 是否避免了 N+1 查询
- [ ] 异步任务是否有超时控制
- [ ] 是否有资源释放（文件、连接）

### 8.3 前端专项审查

- [ ] 组件是否单一职责
- [ ] 是否有完整的 TypeScript 类型
- [ ] 是否有 loading / error / empty 三态
- [ ] 是否做了响应式适配
- [ ] 是否符合无障碍（A11y）标准
- [ ] 是否考虑了 SEO（如需要）
- [ ] 是否存在内存泄漏（定时器、事件监听）
- [ ] 大列表是否做了虚拟化
- [ ] 是否避免了不必要的 re-render
- [ ] API 调用是否有防抖 / 节流

### 8.4 AI 生成代码特别审查

- [ ] **代码理解**：开发者是否完全理解每一行
- [ ] **依赖核实**：所有 import 的库是否真实存在
- [ ] **API 核实**：调用的 API 是否真实存在且参数正确
- [ ] **算法验证**：复杂算法是否经过人工推导验证
- [ ] **幻觉检查**：是否有 AI 编造的函数、参数、字段
- [ ] **测试覆盖**：AI 生成的代码必须有测试，且开发者必须补充边界用例
- [ ] **注释完整**：AI 生成的代码需要补充 Why 注释

### 8.5 性能审查

- [ ] 数据库查询是否做了索引
- [ ] 是否使用了缓存
- [ ] 大数据量场景是否分页
- [ ] 是否避免了重复计算
- [ ] 前端是否做了懒加载

---

## 第9部分 避坑指南

### 9.1 致命坑（必须避开）

#### 坑1：让 AI 直接生成完整架构

```
❌ 致命错误："用 AI 帮我设计整个系统架构"

后果：
- 生成的架构可能与企业级标准脱节
- 后续重构成本极高
- 团队成员理解困难

✅ 正确做法：
- 架构由 Tech Lead 设计
- 让 AI 实现具体的、有明确边界的模块
```

#### 坑2：盲目相信 AI 的"完成度"自述

```
❌ 致命错误：AI 说"已完成，准确率 95%"，开发者直接 merge

后果：
- 实际准确率可能只有 60%
- 测试用例可能是 AI 编造的
- 评估标准可能不一致

✅ 正确做法：
- 必须由开发者基于标准测试集独立验证
- 准确率指标必须人工跑测试集确认
```

#### 坑3：使用 AI 生成的硬编码密钥

```
❌ 致命错误：让 AI "帮我写死讯飞星火的 API Key 用于测试"

后果：
- 密钥泄露
- 安全事故

✅ 正确做法：
- 密钥一律使用占位符 <SPARK_API_KEY>
- 实际密钥通过环境变量注入
```

#### 坑4：AI 生成的依赖不锁定版本

```
❌ 致命错误：requirements.txt 写 "fastapi"

后果：
- 后续部署时版本不一致
- API 行为变化

✅ 正确做法：
- 必须锁定版本：fastapi==0.115.6
- 使用 pip freeze 或 poetry.lock
```

#### 坑5：忽视 AI 幻觉

```
❌ 致命错误：AI 生成 "from app.utils.helpers import format_date"，
              但实际根本没有这个函数

后果：
- 运行时崩溃
- 调试浪费时间

✅ 正确做法：
- AI 提到的 import 必须验证真实存在
- AI 提到的 API 必须查官方文档确认
- 运行代码前先静态检查
```

#### 坑6：用 AI 生成测试数据后直接用于准确率评估

```
❌ 致命错误：用 AI 生成的 JD 同时作为输入和 ground truth

后果：
- 测试集本身有 AI 幻觉
- 准确率"虚高"

✅ 正确做法：
- 测试数据必须来自真实场景（真实JD、真实简历）
- Ground truth 必须由人工标注
- 标注质量必须有验收机制
```

#### 坑7：忽视 AI 工具的"上下文衰减"

```
❌ 致命错误：与 AI 长对话累积了大量上下文，
              AI 开始遗忘早期约定、出现不一致

后果：
- 生成的代码风格不一致
- 早期决策被违背

✅ 正确做法：
- 重要决策记录到 CLAUDE.md / .cursorrules
- 每个新任务开启新对话
- 用 Plan Mode / Spec 模式锁定需求
```

### 9.2 性能坑

#### 坑1：AI 生成的低效算法

```python
# ❌ AI 可能的低效实现（O(n²)）
def find_common_skills(skills_a, skills_b):
    common = []
    for s in skills_a:
        for t in skills_b:
            if s == t:
                common.append(s)
    return common

# ✅ 优化后（O(n)）
def find_common_skills(skills_a, skills_b):
    return list(set(skills_a) & set(skills_b))
```

#### 坑2：AI 生成不必要的数据库查询

```python
# ❌ N+1 查询
for job in jobs:
    job.skills = db.query("SELECT * FROM skills WHERE job_id = ?", job.id)

# ✅ 批量查询
job_ids = [job.id for job in jobs]
all_skills = db.query("SELECT * FROM skills WHERE job_id IN (?)", job_ids)
```

### 9.3 可维护性坑

#### 坑1：AI 生成过度抽象的代码

```python
# ❌ 过度设计：一个简单的功能搞了5层抽象
class AbstractSkillMatcherFactory:
    def create_matcher(self, strategy):
        return StrategyBasedMatcher(strategy)

# ✅ 简单直接
def match_skills(candidate, job):
    ...
```

#### 坑2：AI 生成不规范的命名

```python
# ❌ AI 命名混乱
def calc_match(c, j):  # 含义不明
    ...

# ✅ 规范命名
def calculate_match_score(candidate_skills, job_required_skills):
    ...
```

### 9.4 安全坑

#### 坑1：AI 生成的 SQL 注入漏洞

```python
# ❌ 拼接字符串（SQL 注入风险）
query = f"SELECT * FROM jobs WHERE title LIKE '%{keyword}%'"

# ✅ 参数化查询
query = "SELECT * FROM jobs WHERE title LIKE %s"
db.execute(query, (f"%{keyword}%",))
```

#### 坑2：AI 生成的不安全反序列化

```python
# ❌ pickle 反序列化（任意代码执行风险）
import pickle
data = pickle.loads(user_input)

# ✅ JSON 反序列化
import json
data = json.loads(user_input)
```

---

## 附录：项目级 AI 规则配置文件

### A.1 `.cursorrules`（Cursor）

```markdown
# 项目：执图破局 - 岗位-能力知识图谱平台
# 赛题编号：XH-202621
# 详细规范：见 @项目开发指南.md

## ⚠️ 强制约定

### 约定1：开发者身份识别
每次会话开始时，必须首先询问：
1. 开发者的角色（5选1）：Tech Lead / 数据工程师 / 后端AI工程师 / 前端工程师 / 测试DevOps工程师
2. 开发者的姓名/花名
3. 本次任务目标

识别后，加载对应角色的工作流和代码所有权。

### 约定2：自动生成项目汇报
当开发者表达任务完成时（"完成"、"搞定"、"结束"、"OK"、"保存"等关键词），必须自动：
1. 询问汇报细节（完成任务、涉及文件、测试情况、问题、待办、需协助事项）
2. 生成汇报文件 → docs/09-daily-reports/YYYY-MM-DD/{角色}-{任务名}.md
3. 更新总进度看板 → docs/09-daily-reports/PROGRESS.md
4. 输出确认信息

## 全局规则
- 所有响应必须基于已提供的上下文，不允许编造
- 不得包含真实 API 密钥、个人信息，使用占位符
- 所有代码必须有完整类型注解
- 所有公开函数必须有 docstring
- 所有 AI 生成的代码块必须添加 # AI-Generated 注释

## 技术栈锁定
- Python 3.11（不可变更）
- FastAPI 0.115.x
- Neo4j 5.x
- Vue 3.5
- 讯飞星火 X2
- BGE-M3

## 命名规范
- 类：PascalCase
- 函数/变量：snake_case
- 常量：UPPER_SNAKE_CASE
- 组件：PascalCase（Vue）
- 文件：snake_case（Python）、kebab-case（前端）

## 强制要求
- 提交前运行 Black + isort + flake8 + mypy
- 测试覆盖率 ≥ 60%
- 三大准确率 ≥ 90%

## 推荐但可选
- Type hints
- Error handling
- Logging
- Docstrings
```

### A.2 `.claude/CLAUDE.md`（Claude Code）

```markdown
# 项目：执图破局

## 项目描述
就业市场岗位-能力知识图谱动态构建与匹配平台。详见 @项目开发指南.md

## 工作流程
1. 接收任务前先理解需求
2. 加载相关上下文文件
3. 基于规范生成代码
4. 标注 AI-Generated 注释
5. 生成单元测试

## 规则
- 架构决策由人类主导
- AI 负责代码生成、文档编写
- 核心算法必须有单元测试
- 准确率指标由人类验证
- 所有依赖锁定版本

## 上下文文件
- @项目开发指南.md（必读）
- @backend/app/（后端代码）
- @frontend/src/（前端代码）
- @docs/（项目文档）

## 错误反馈
当开发者指出错误时：
1. 承认错误
2. 分析原因
3. 重新生成
4. 解释修复
```

### A.3 `.continue/config.json`（Continue.dev）

```json
{
  "models": [
    {
      "title": "Claude 4 Sonnet",
      "provider": "anthropic",
      "model": "claude-sonnet-4-5",
      "contextLength": 200000,
      "systemMessage": "你是'执图破局'项目的 AI 助手。请遵守 @项目开发指南.md 的规范。\n\n⚠️ 强制约定：\n1. 每次会话开始时必须先询问开发者的角色、姓名、本次任务\n2. 当开发者说'完成'/'结束'/'搞定'/'保存'时，必须自动生成项目汇报文件到 docs/09-daily-reports/ 目录\n3. 详细规则见 @AI协作开发teach文档.md 第0.5部分"
    }
  ],
  "tabAutocompleteModel": {
    "title": "Claude 4 Sonnet",
    "provider": "anthropic",
    "model": "claude-sonnet-4-5"
  },
  "customCommands": [
    {
      "name": "/report",
      "description": "生成项目汇报文件",
      "prompt": "请生成本次任务的汇报文件：\n\n1. 收集汇报信息（任务完成情况、修改文件、测试情况、问题、下一步）\n2. 保存到 docs/09-daily-reports/YYYY-MM-DD/[角色]-[任务名].md\n3. 更新 PROGRESS.md\n4. 输出确认信息\n\n模板详见 @AI协作开发teach文档.md 第0.5.3节"
    },
    {
      "name": "/identify",
      "description": "识别当前开发者角色",
      "prompt": "请询问开发者：\n1. 您是项目组的哪位开发者？\n2. 您的姓名/花名？\n3. 本次任务目标？\n\n然后加载对应角色的工作流和代码所有权。"
    },
    {
      "name": "生成测试",
      "description": "为选中代码生成单元测试",
      "prompt": "为以下代码生成完整的单元测试：\n{{{input}}}\n\n要求：\n1. 使用 pytest\n2. 覆盖率 ≥ 90%\n3. 覆盖正常路径、边界、异常"
    },
    {
      "name": "添加文档",
      "description": "为选中代码添加 docstring",
      "prompt": "为以下函数/类添加完整的 docstring：\n{{{input}}}\n\n使用 Google 风格。"
    },
    {
      "name": "性能优化",
      "description": "分析并优化代码性能",
      "prompt": "分析以下代码的性能瓶颈，并提供优化版本：\n{{{input}}}\n\n要求：\n1. 提供复杂度分析\n2. 提供优化后代码\n3. 提供 benchmark 对比"
    }
  ]
}
```

### A.4 `.pre-commit-config.yaml`（Git Hooks）

```yaml
repos:
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        language_version: python3.11
        args: [--line-length=100]
        files: ^backend/

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile=black, --line-length=100]
        files: ^backend/

  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
        args: [--max-line-length=100, --extend-ignore=E203,W503]
        files: ^backend/

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.13.0
    hooks:
      - id: mypy
        files: ^backend/
        additional_dependencies: [types-all]

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        types_or: [javascript, ts, tsx, vue, json, yaml, markdown]
        files: ^frontend/

  - repo: https://github.com/eslint/eslint
    rev: v9.15.0
    hooks:
      - id: eslint
        files: ^frontend/.*\.(js|ts|vue)$
        additional_dependencies:
          - eslint@^9
          - eslint-plugin-vue@^9

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: detect-private-key
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=1024]
```

---

## 文档维护

| 版本 | 日期 | 变更 | 作者 |
|------|------|------|------|
| v1.0.0 | 2026-07-16 | 初始版本 | Tech Lead |
| v1.1.0 | 2026-07-16 | 新增强制约定：身份识别 + 汇报自动生成 | Tech Lead |

---

## 🎯 一句话总结

> **让 AI 做重复劳动，让人做核心决策。
> 用 AI 提效率，用规范保质量。
> AI 是副驾驶，主驾永远是人。
> 身份明确，分工清晰，汇报同步。**

---

> **配套使用**：
> - 📘 项目开发指南 → 架构、规范、流程
> - 📗 AI 协作 Teach 文档 → AI 工具使用最佳实践（本文档）
> - 📙 项目 README → 快速开始

---

## 附录C：汇报文件示例（参考）

### C.1 示例1：后端工程师完成"讯飞星火客户端封装"

```markdown
# 项目汇报 - spark-client-setup

## 📋 基本信息
| 字段 | 内容 |
|------|------|
| 开发者 | 张三 |
| 角色 | 后端/AI 工程师 |
| 日期 | 2026-07-16 |
| 任务名称 | 讯飞星火客户端封装 |
| 关联里程碑 | M4 智能层 |
| 优先级 | P0 |
| 状态 | ✅ 已完成 |

## 📝 本次完成工作
### ✅ 已完成
1. 实现 SparkClient 同步/异步调用
2. 实现指数退避重试（最多3次）
3. 实现多模型路由（X2 / X1.5 / Lite）
4. 完整的 token 用量统计
5. 异常分类（限流、超时、内容安全）

## 📁 涉及的文件清单
### 新增文件
- backend/app/ai/llm/spark_client.py - 客户端主类
- backend/app/ai/llm/prompt.py - Prompt 模板管理
- backend/app/ai/llm/chains.py - LangChain 链
- backend/tests/unit/ai/test_spark_client.py - 单元测试

## 🧪 测试情况
| 测试类型 | 通过率 | 备注 |
|---------|--------|------|
| 单元测试 | 92% | 16/17 用例通过 |
| 集成测试 | 通过 | mock API |

## ⚠️ 遇到的问题与解决方案
### 问题：讯飞 WebSocket 鉴权过期处理
- 现象：长连接运行2小时后认证失效
- 原因：未实现 token 自动刷新
- 解决方案：增加 AuthRefresher 类，每30分钟检查并刷新

## 📊 当前进度
### 本角色负责模块进度
| 模块 | 进度 | 状态 |
|------|------|------|
| LLM 客户端 | 100% | ✅ 已完成 |
| RAG 链路 | 0% | ⬜ 未开始 |
| 简历解析 | 0% | ⬜ 未开始 |

## 🤝 需要协助的事项
- 需要 Tech Lead 决策：是否引入 LiteLLM 统一多模型？
- 需要数据工程师：提供 LLM 调用日志的结构化字段

## 📋 下一步计划
1. 实现 RAG 链路（LangChain + ChromaDB）
2. 集成 Neo4j 检索
3. 准备 Prompt 测试集

## 🔗 相关链接
- PR: #45
- 文档: docs/03-api/llm-api.md
- 测试报告: reports/coverage/spark-client.html
```

### C.2 示例2：前端工程师完成"图谱可视化页面"

```markdown
# 项目汇报 - graph-visualization-page

## 📋 基本信息
| 字段 | 内容 |
|------|------|
| 开发者 | 李四 |
| 角色 | 前端工程师 |
| 日期 | 2026-07-16 |
| 任务名称 | 图谱可视化页面 |
| 关联里程碑 | M6 前端开发 |
| 优先级 | P0 |
| 状态 | ✅ 已完成 |

## 📝 本次完成工作
### ✅ 已完成
1. 使用 AntV G6 5.x 实现图谱可视化
2. 力导向布局，支持拖拽、缩放
3. 4种节点类型（Job、Skill、Industry、Company）
4. 点击节点显示详情面板
5. 按技术栈和级别的筛选器
6. 支持 10000+ 节点流畅渲染

## 📁 涉及的文件清单
### 新增文件
- frontend/src/views/KnowledgeGraph/index.vue - 主页面
- frontend/src/components/GraphCanvas.vue - G6 画布组件
- frontend/src/components/NodeDetailPanel.vue - 详情面板
- frontend/src/composables/useGraphData.ts - 数据加载

## 🧪 测试情况
| 测试类型 | 通过率 | 备注 |
|---------|--------|------|
| 单元测试 | 85% | 组件测试 |
| E2E 测试 | 通过 | Playwright |

## 🤝 需要协助的事项
- 需要后端工程师：图谱可视化 API 返回格式确认（与李四沟通）
- 需要数据工程师：筛选器字段定义

## 📋 下一步计划
1. 实现数据看板页面
2. 简历上传页面
3. 移动端适配
```

---

## 附录D：汇报目录初始化脚本

将以下脚本保存为 `scripts/init_reports_dir.sh`：

```bash
#!/bin/bash
# 初始化项目汇报目录结构

REPORTS_DIR="docs/09-daily-reports"

# 创建主目录
mkdir -p "$REPORTS_DIR"
mkdir -p "$REPORTS_DIR/weekly-summary"

# 创建 PROGRESS.md
cat > "$REPORTS_DIR/PROGRESS.md" << 'EOF'
# 执图破局 - 项目总进度看板

> **最后更新**：待初始化
> **整体进度**：0%

## 📊 项目里程碑进度
[详见 @AI协作开发teach文档.md 第0.5.4节]
EOF

# 创建 README.md
cat > "$REPORTS_DIR/README.md" << 'EOF'
# 项目汇报目录

本目录由 AI 协作助手自动维护，包含：
- PROGRESS.md - 总进度看板（唯一）
- YYYY-MM-DD/ - 按日期归档的每日汇报
- weekly-summary/ - 周报汇总

## 命名规范
- {角色英文}-{任务简述}.md
- 角色：tech-lead / data-engineer / backend-engineer / frontend-engineer / qa-devops

## 详细规则
详见 @AI协作开发teach文档.md 第0.5部分
EOF

# 创建 .gitkeep
touch "$REPORTS_DIR/.gitkeep"

echo "✅ 项目汇报目录初始化完成：$REPORTS_DIR"
```

**使用方式**：

```bash
# 在项目根目录执行
bash scripts/init_reports_dir.sh

# 或在 PowerShell 中手动创建
mkdir docs\09-daily-reports
mkdir docs\09-daily-reports\weekly-summary
echo "# 项目总进度" > docs\09-daily-reports\PROGRESS.md
```