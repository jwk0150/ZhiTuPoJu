# 环境配置最佳实践

## 问题背景

在多人协作开发时，不同开发者的本地环境可能存在差异：

- **端口号不同**：有人用 PostgreSQL 默认 5432，有人用 3309
- **密码不同**：每个人的数据库密码各不相同
- **API Key 不同**：每个开发者使用自己的 DeepSeek API Key
- **文档与实际不符**：启动文档说端口 5000，实际运行在 8000

这些差异导致：
1. 新成员加入时配置困难
2. 代码中硬编码配置导致频繁冲突
3. 文档过时，与实际环境不一致

---

## 解决方案

### 1. 统一配置管理 — `backend/config.py`

**核心思想**：所有配置从 `.env` 文件读取，代码中通过 `config` 对象访问。

```python
from backend.config import config

# 使用配置
db_url = config.get_db_url()
table_name = config.get_table_name("job_postings")
api_key = config.DEEPSEEK_API_KEY
```



---

### 2. 配置模板 — `.env.example`

**用途**：提供标准模板，新成员复制后填写自己的配置。

```ini
# ============ DeepSeek AI 配置 ============
DEEPSEEK_API_KEY=

# ============ PostgreSQL 数据库配置 ============
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=
PG_DB=zhilian_crawl_db

# ============ 后端服务配置 ============
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# ============ 表名前缀配置 ============
TABLE_PREFIX=zhilian_
```


- `.env.example` 提交到 Git，作为模板
- `.env` 不提交到 Git（`.gitignore` 已配置）
- 密码字段留空，由开发者自行填写

---

### 3. 配置验证脚本 — `backend/check_config.py`

**用途**：新成员配置完成后，运行此脚本验证环境是否正常。

```powershell
python backend/check_config.py
```

完成后在控制台输出一下内容（根据情况定）
============================================================
配置检查
============================================================

DeepSeek API 已配置 (deepseek-chat)

数据库配置:
   Host: 127.0.0.1:3309
   Database: zhilian_crawl_db
   User: postgres
   Table Prefix: zhilian_
数据库连接成功
   PostgreSQL Version: PostgreSQL 15.3

表 zhilian_job_postings 存在 (9335 条记录)
表 zhilian_job_posting_details 存在 (9335 条记录)

后端服务配置:
   Host: 127.0.0.1
   Port: 8000
   访问地址: http://127.0.0.1:8000

============================================================
✅ 所有配置检查通过！
============================================================
```

---

### 4. 配置状态端点 — `/api/config-status`

**用途**：快速查看当前运行环境的配置（不暴露敏感信息）。

```bash
curl http://127.0.0.1:8000/api/config-status
```

**返回示例**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "backend_host": "127.0.0.1",
    "backend_port": 8000,
    "pg_host": "127.0.0.1",
    "pg_port": 3309,
    "pg_db": "zhilian_crawl_db",
    "deepseek_configured": true,
    "table_prefix": "zhilian_"
  }
}
```

**用途场景**：
- 排查前后端连接问题
- 确认部署环境配置
- 快速了解当前环境状态

---

## 推荐工作流程

### 新成员加入

1. **克隆代码**
   ```powershell
   git clone <repo-url>
   cd 挑战杯
   ```

2. **复制配置模板**
   ```powershell
   copy .env.example .env
   ```

3. **编辑 `.env`**
   - 填写 `DEEPSEEK_API_KEY`
   - 填写 `PG_PASSWORD`
   - 如果 PostgreSQL 端口不是 5432，修改 `PG_PORT`
   - 如果后端想用其他端口，修改 `BACKEND_PORT`

4. **验证配置**
   ```powershell
   python backend/check_config.py
   ```

5. **启动服务**
   ```powershell
   # 后端
   python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

   # 前端
   cd frontend
   python -m http.server 8080
   ```

6. **验证运行**
   - 健康检查：http://127.0.0.1:8000/api/health
   - 配置状态：http://127.0.0.1:8000/api/config-status
   - 前端页面：http://localhost:8080/index.html

---

### 配置变更

**场景 1：我想换个数据库端口**

编辑 `.env`：
```ini
PG_PORT=3309  # 改成你的端口
```

重启后端即可，无需修改代码。

**场景 2：团队要支持新的数据源（如 Boss 直聘）**

编辑 `.env`：
```ini
TABLE_PREFIX=boss_  # 改成新的表名前缀
```

数据库中对应表名应为：`boss_job_postings`、`boss_job_posting_details`

**场景 3：生产环境部署**

方式 1：使用 `.env` 文件
```ini
PG_HOST=prod-db.example.com
PG_PORT=5432
PG_PASSWORD=强密码
BACKEND_PORT=80
```

方式 2：使用环境变量（推荐）
```bash
export DEEPSEEK_API_KEY=sk-xxx
export PG_HOST=prod-db.example.com
export PG_PASSWORD=强密码
python -m uvicorn backend.main:app --host 0.0.0.0 --port 80
```

---

## Docker 方案（可选）

如果团队希望完全统一环境（包括 Python 版本、PostgreSQL 版本），可以使用 Docker Compose。

### `docker-compose.yml`

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: zhitu_postgres
    environment:
      POSTGRES_DB: zhilian_crawl_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zhitu_backend
    ports:
      - "8000:8000"
    environment:
      PG_HOST: postgres
      PG_PORT: 5432
      PG_USER: postgres
      PG_PASSWORD: postgres
      PG_DB: zhilian_crawl_db
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
    depends_on:
      - postgres
    volumes:
      - .:/app

volumes:
  postgres_data:
```

### `Dockerfile`

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 使用方式

```powershell
# 启动所有服务（PostgreSQL + 后端）
docker-compose up -d

# 查看日志
docker-compose logs -f backend

# 停止所有服务
docker-compose down
```

---

## 总结

| 方案 | 适用场景 | 优势 | 劣势 |
|------|---------|------|------|
| **`.env` 配置管理** | 小团队，环境差异不大 | 简单、灵活、易上手 | 需要手动配置 |
| **Docker Compose** | 大团队，需要完全一致环境 | 零配置、环境隔离 | 需要学习 Docker |
| **混合方案** | 开发用 `.env`，生产用 Docker | 兼顾开发灵活性和生产稳定性 | 维护两套配置 |

**当前推荐**：先用 `.env` 配置管理，如果团队规模扩大或环境差异严重，再引入 Docker。
