# 执图破局 (ZhiTuPoJu) — 项目启动指南

## 项目简介

多源异构数据驱动岗位和能力图谱构建与动态演化分析研究平台。

- **后端**: FastAPI (Python)
- **前端**: 纯 HTML/CSS/JS (单页应用)
- **数据库**: PostgreSQL 15+
- **AI**: DeepSeek API

---

## 一、环境要求

| 组件 | 版本要求 |
|------|----------|
| Python | 3.10+ |
| PostgreSQL | 15+ |
| Git | 任意版本 |
| 浏览器 | Chrome / Edge / Firefox 最新版 |

---

## 二、数据库初始化

### 1. 创建数据库

```sql
CREATE DATABASE zhitu_crawl_db
    WITH ENCODING 'UTF8'
    LC_COLLATE 'zh_CN.UTF-8'
    LC_CTYPE 'zh_CN.UTF-8'
    TEMPLATE template0;
```

### 2. 执行建表 DDL

详见 `数据库建表DDL.md`，按顺序执行：
1. 安装扩展 (`pg_trgm`, `btree_gin`, `pgcrypto`)
2. 创建 `job_postings` 表及索引
3. 创建 `job_posting_details` 表及索引
4. 创建触发器
5. 创建统计视图

---

## 三、快速启动（推荐）

### 使用启动脚本

**Windows 一键启动**：
```powershell
# 双击运行或在命令行执行
start.bat
```

脚本会自动：
1. 检查并停止占用端口的进程
2. 验证环境配置（调用 `check_config.py`）
3. 启动后端服务（新窗口）
4. 启动前端服务（新窗口）

**停止服务**：
```powershell
# 双击运行或在命令行执行
stop.bat
```

---

## 四、手动启动

### 1. 配置环境变量

```powershell
cd 挑战杯
copy .env.example .env
```

编辑 `.env`，填入你的配置：

```ini
# ============ DeepSeek AI 配置 ============
DEEPSEEK_API_KEY=你的API密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ============ PostgreSQL 数据库配置 ============
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=你的数据库密码
PG_DB=zhilian_crawl_db

# ============ 后端服务配置 ============
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# ============ 表名前缀配置 ============
TABLE_PREFIX=zhilian_
```

> **重要**：所有配置项都通过 `.env` 文件管理，方便团队成员使用不同的开发环境。
> - 标准 PostgreSQL 端口为 `5432`，如果你的环境使用其他端口（如 `3309`），请修改 `PG_PORT`
> - 后端默认端口为 `8000`，可通过 `BACKEND_PORT` 修改

### 2. 验证配置

```powershell
python backend/check_config.py
```

这会检查：
- DeepSeek API 是否配置
- 数据库连接是否正常
- 必需的表是否存在
- 后端服务配置

### 3. 安装依赖

```powershell
pip install -r backend/requirements.txt
```

### 4. 启动后端

```powershell
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

> **端口说明**：
> - 默认端口 `8000`（通过 `.env` 的 `BACKEND_PORT` 配置）
> - 如需修改端口，请同时更新 `.env` 文件和启动命令
> - 前端会自动读取后端配置的端口

启动成功后输出：
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

验证：
- 健康检查：`http://127.0.0.1:8000/api/health`
- API 文档：`http://127.0.0.1:8000/docs`

---

## 五、前端启动

**新开一个终端窗口**：

```powershell
cd frontend
python -m http.server 8080
```

浏览器打开：**http://localhost:8080/index.html**

> 如果前端 8080 端口被占用，换一个端口（如 8081）：
> ```powershell
> python -m http.server 8081
> ```

---

## 六、访问地址汇总

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:8080/index.html |
| 后端 API | http://127.0.0.1:8000 |
| 健康检查 | http://127.0.0.1:8000/api/health |
| API 文档 (Swagger) | http://127.0.0.1:8000/docs |

> **注意**：实际端口以 `.env` 中的 `BACKEND_PORT` 配置为准

---

## 七、常见问题

### Q1: `WinError 10013` — 端口被占用

**方案 1：使用停止脚本**
```powershell
stop.bat
```

**方案 2：手动停止**
```powershell
# 查看端口占用
netstat -ano | findstr ":8000"
# 杀掉进程（替换 PID）
taskkill //PID <PID号> //F
```

### Q2: 前端无法连接后端

- 检查后端是否成功启动：访问 `http://127.0.0.1:8000/api/health`
- 检查浏览器控制台是否有 CORS 错误
- 确认 `.env` 中的 `BACKEND_PORT` 与实际启动端口一致

### Q3: 数据库连接失败

- 确认 PostgreSQL 服务已启动
- 运行配置检查：`python backend/check_config.py`
- 确认 `.env` 中 `PG_HOST`、`PG_PORT`、`PG_PASSWORD` 正确
- 确认数据库 `zhilian_crawl_db` 已创建
- 确认表名前缀 `TABLE_PREFIX` 与实际表名一致（默认 `zhilian_`）

### Q4: DeepSeek API 不可用

部分功能（AI 分析、智能问答）依赖 DeepSeek API，请确保 `.env` 中 `DEEPSEEK_API_KEY` 已正确配置。

### Q5: 不同开发者环境配置不一致

**推荐方案**：
1. **统一通过 `.env` 管理配置**：所有环境差异都在 `.env` 中声明
2. **使用配置检查脚本**：新成员加入后先运行 `python backend/check_config.py`
3. **文档化特殊配置**：如果某位开发者使用非标准端口（如 PostgreSQL 3309），在团队文档中说明原因
4. **配置状态端点**：访问 `/api/config-status` 快速确认当前运行配置

**Docker 方案**（可选）：
对于需要完全一致环境的场景，可以使用 Docker Compose 统一管理：
```yaml
# docker-compose.yml
services:
  postgres:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: zhilian_crawl_db
      POSTGRES_PASSWORD: postgres
  
  backend:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - postgres
```

---

## 七、项目结构

```
挑战杯/
├── backend/                # 后端 FastAPI
│   ├── main.py            # 入口文件
│   ├── config.py          # 配置管理（统一从 .env 读取）
│   ├── check_config.py    # 配置验证脚本
│   ├── routers/           # API 路由
│   │   ├── agent.py       # AI 对话
│   │   ├── collection.py  # 数据采集
│   │   ├── discovery.py   # 智能发现
│   │   ├── evolution.py   # 演化分析
│   │   ├── matching.py    # 人岗匹配
│   │   ├── talent_map.py  # 人才地图
│   │   ├── data.py        # 数据查询
│   │   └── graph.py       # 图谱
│   ├── matching/          # 匹配算法
│   ├── evolution_agent/   # 演化分析 Agent
│   └── requirements.txt   # Python 依赖
├── frontend/              # 前端
│   ├── index.html         # 主页面（单页应用）
│   ├── lib/               # 第三方 JS 库
│   ├── samples/           # 示例数据
│   └── china-geo.json     # 中国地图 GeoJSON
├── crawler/               # 数据爬虫模块
├── .env                   # 环境变量配置（个人配置，不提交到 Git）
├── .env.example           # 环境变量模板
├── .gitignore
├── 数据库建表DDL.md        # 数据库建表文档
└── 启动指南.md             # 本文档
```
