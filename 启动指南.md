# 执图破局 (ZhiTuPoJu) — 项目启动指南

## 项目简介

多源异构数据驱动岗位和能力图谱构建与动态演化分析研究平台。

- **后端**: FastAPI (Python)
- **前端**: 纯 HTML/CSS/JS (单页应用)
- **数据库**: PostgreSQL 15+
- **AI**: DeepSeek API

---

## 一、环境要求

| 组件 | 版本要求 |
|------|----------|
| Python | 3.10+ |
| PostgreSQL | 15+ |
| Git | 任意版本 |
| 浏览器 | Chrome / Edge / Firefox 最新版 |

---

## 二、数据库初始化

### 1. 创建数据库

```sql
CREATE DATABASE zhitu_crawl_db
    WITH ENCODING 'UTF8'
    LC_COLLATE 'zh_CN.UTF-8'
    LC_CTYPE 'zh_CN.UTF-8'
    TEMPLATE template0;
```

### 2. 执行建表 DDL

详见 `数据库建表DDL.md`，按顺序执行：
1. 安装扩展 (`pg_trgm`, `btree_gin`, `pgcrypto`)
2. 创建 `job_postings` 表及索引
3. 创建 `job_posting_details` 表及索引
4. 创建触发器
5. 创建统计视图

---

## 三、后端启动

### 1. 配置环境变量

```powershell
cd 挑战杯
copy .env.example .env
```

编辑 `.env`，填入你的配置：

```ini
# DeepSeek LLM API Key（必填）
DEEPSEEK_API_KEY=你的API密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# PostgreSQL 连接（按实际情况修改）
PG_HOST=127.0.0.1
PG_PORT=5433
PG_USER=postgres
PG_PASSWORD=你的数据库密码
PG_DB=zhitu_crawl_db
```

### 2. 安装依赖

```powershell
pip install -r backend/requirements.txt
```

### 3. 启动服务

```powershell
python -m uvicorn backend.main:app --host 127.0.0.1 --port 5000 --reload
```

> 默认端口 `5000`，若端口被占用可换成 `8080`、`8001` 等，同时需同步修改前端 `index.html` 中的 `API_BASE`。

启动成功后输出：
```
INFO:     Uvicorn running on http://127.0.0.1:5000
```

验证：浏览器打开 `http://127.0.0.1:5000/api/health`，返回 `{"code":0,"message":"success",...}` 即可。

---

## 四、前端启动

```powershell
cd frontend
python -m http.server 8080
```

浏览器打开：**http://localhost:8080/index.html**

> 如果前端 8080 端口被占用，换一个端口（如 8081）：
> ```powershell
> python -m http.server 8081
> ```

---

## 五、访问地址汇总

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:8080/index.html |
| 后端 API | http://127.0.0.1:5000 |
| 健康检查 | http://127.0.0.1:5000/api/health |
| API 文档 (Swagger) | http://127.0.0.1:5000/docs |

---

## 六、常见问题

### Q1: `WinError 10013` — 端口被占用

```powershell
# 查看端口占用
netstat -ano | findstr ":5000"
# 杀掉进程（替换 PID）
taskkill /PID <PID号> /F
```

### Q2: 前端无法连接后端

确认 `frontend/index.html` 中 `API_BASE` 和 `window.API_BASE` 的端口与后端一致（默认 `5000`）。

### Q3: 数据库连接失败

- 确认 PostgreSQL 服务已启动
- 确认 `.env` 中 `PG_HOST`、`PG_PORT`、`PG_PASSWORD` 正确
- 确认数据库 `zhitu_crawl_db` 已创建

### Q4: DeepSeek API 不可用

部分功能（AI 分析、智能问答）依赖 DeepSeek API，请确保 `.env` 中 `DEEPSEEK_API_KEY` 已正确配置。

---

## 七、项目结构

```
挑战杯/
├── backend/                # 后端 FastAPI
│   ├── main.py            # 入口文件
│   ├── routers/           # API 路由
│   │   ├── agent.py       # AI 对话
│   │   ├── collection.py  # 数据采集
│   │   ├── discovery.py   # 智能发现
│   │   ├── evolution.py   # 演化分析
│   │   ├── matching.py    # 人岗匹配
│   │   ├── talent_map.py  # 人才地图
│   │   ├── data.py        # 数据查询
│   │   └── graph.py       # 图谱
│   ├── matching/          # 匹配算法
│   ├── evolution_agent/   # 演化分析 Agent
│   └── requirements.txt   # Python 依赖
├── frontend/              # 前端
│   ├── index.html         # 主页面（单页应用）
│   ├── lib/               # 第三方 JS 库
│   ├── samples/           # 示例数据
│   └── china-geo.json     # 中国地图 GeoJSON
├── crawler/               # 数据爬虫模块
├── .env.example           # 环境变量模板
├── .gitignore
├── 数据库建表DDL.md        # 数据库建表文档
└── 启动指南.md             # 本文档
```
