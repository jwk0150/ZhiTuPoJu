# 「我的资料」页面详细说明文档

> 页面文件：`frontend/pages/profile-embed.html`
> 逻辑脚本：`frontend/js/pages/profile.js`
> 样式文件：`frontend/css/profile.css`
> 后端接口：`backend/routers/profile.py`
> 数据服务：`backend/profile_service.py`（技能雷达 / 岗位匹配 / 简历解析等）

---

## 1. 页面概述

「我的资料」是平台的**用户个人中心**，用于集中管理用户的个人信息、简历、AI 职业访谈与职业画像分析结果。

**当前入口**（本次改动后）：
- 顶栏右上角**头像**（`topbar-user`，由 `frontend/js/shell.js` 渲染）点击后跳转到本页；
- 首页「就业指导」快捷入口（`frontend/pages/home.html` 第 64 行）也指向本页；
- 两个历史跳转页 `frontend/profile.html` 与 `frontend/pages/profile.html` 均重定向到本页。

> 注意：本页是**独立页面**，不含侧边导航/顶栏外壳（shell），返回需用浏览器后退。

---

## 2. 页面文件与依赖

| 资源 | 路径 | 作用 |
| --- | --- | --- |
| HTML 结构 | `frontend/pages/profile-embed.html` | 页面骨架、四个 Tab 面板 |
| 页面逻辑 | `frontend/js/pages/profile.js` | 数据加载、交互、图表渲染 |
| 全局 API | `frontend/js/api.js` | 定义 `window.API_BASE`（默认 `http://127.0.0.1:5000`） |
| 样式 | `frontend/css/profile.css` | 本页全部样式（含设计变量） |
| 图表库 | ECharts 5.5（CDN） | 雷达图、仪表盘 |

---

## 3. 整体布局结构（自上而下）

页面容器 `.app`（最大宽度 960px，居中，上下留白），整体按以下顺序排列：

```
┌──────────────────────────────────────────────┐
│ ① Hero 个人头部卡片                            │  ← 头像 + 姓名 + 标签 + 完成度 + 编辑按钮
├──────────────────────────────────────────────┤
│ ② KPI 指标行（4 张卡片）                       │  ← 技能 / 简历 / 岗位 / 评分
├──────────────────────────────────────────────┤
│ ③ Tab 标签栏（4 个标签）                       │  ← 个人资料 / 我的简历 / AI访谈 / 职业画像
├──────────────────────────────────────────────┤
│ ④ 当前 Tab 对应的面板内容                       │  ← 见下文各 Tab 详解
└──────────────────────────────────────────────┘
  ⑤ 全局 Toast 提示（fixed 定位，底部居中）
```

---

## 4. 分区详解

### 4.1 Hero 个人头部卡片

类名 `.hero`，是页面最顶部的信息卡，深色圆角卡片，右上角有淡青色光晕装饰。

| 元素 | 类名 / ID | 内容说明 |
| --- | --- | --- |
| 头像 | `.hero-avatar` `#avatar` | 圆形头像，青色渐变底；显示姓名前 2 个字符（无姓名时显示 `--`） |
| 姓名 | `.hero-name` `#display-name` | 大字显示用户姓名，未填写时显示「加载中...」 |
| 副标题 | `.hero-detail` `#display-sub` | 显示「专业 · 目标岗位」，无数据时显示「请完善资料」 |
| 标签行 | `.hero-meta` 内含三个 `.hero-tag` | `#tag-school`（学校，灰色 muted）、`#tag-major`（专业，灰色 muted）、`#tag-target`（目标岗位，青色高亮） |
| 资料完整度 | `.completion` | 见下方说明 |
| 操作按钮 | `.hero-actions` | `#edit-btn`（编辑资料）、`#save-btn`（保存，默认隐藏） |

**资料完整度**（`.completion`）由三部分组成：
- `.completion-label`：文案「资料完整度」；
- `.completion-pct` `#pct`：百分比数字（青色等宽字体）；
- `.completion-bar` → `.completion-fill` `#bar`：进度条，宽度随百分比变化；
- `.completion-hint` `#hint`：提示文案，按百分比分档：
  - ≥80%：「资料完善度优秀」
  - ≥50%：「继续加油，上传简历可大幅提高」
  - 其余：「填写基本信息即可解锁」

> 完整度前端计算规则（见 `profile.js` 的 `loadProfile`）：
> `pct = 已填写字段数 / 6 × 60% + (有技能 ? 20% : 0) + (有报告 ? 20% : 0)`
> 其中 6 个字段为：姓名、学校、专业、学历、年级、目标岗位。
> （后端 `user_profiles.completion` 字段另有 `已填5项 × 20%` 的计算方式，前端展示并未采用该字段。）

### 4.2 KPI 指标行

类名 `.kpi-row`，栅格布局，桌面端 4 列、窄屏 2 列。四张卡片：

| ID | 标签 | 数值来源 | 颜色 |
| --- | --- | --- | --- |
| `#kpi-skills` | 已识别技能 | 技能列表长度 | 青色 accent |
| `#kpi-resumes` | 上传简历 | 简历列表长度 | 青色 accent |
| `#kpi-matches` | 匹配岗位 | 报告 `match_jobs` 数量 | 青色 accent |
| `#kpi-score` | 竞争力评分 | 报告 `overall_score` | 橙色 warm |

### 4.3 Tab 标签栏

类名 `.tabs`，横向标签，底部有细分割线，激活标签有青色下划线动画。四个标签：

| 标签 | data-tab | 对应面板 |
| --- | --- | --- |
| 个人资料 | `info` | `#panel-info` |
| 我的简历 | `resume` | `#panel-resume` |
| AI 职业访谈 | `interview` | `#panel-interview` |
| 职业画像 | `analysis` | `#panel-analysis` |

切换逻辑：`profile.js` 第 3-9 行，点击后切换 `.active` 类；进入「我的简历」会自动 `loadResumes()`，进入「职业画像」会自动 `loadReport()`。

---

### 4.4 Tab 一：个人资料（`#panel-info`）

一张「基本信息」卡片，内部为 `.form-grid` 两列表单（窄屏一列）：

| 字段 | ID | 控件类型 | 选项 |
| --- | --- | --- | --- |
| 姓名 | `#pf-name` | input | — |
| 学校 | `#pf-school` | input | — |
| 专业 | `#pf-major` | input | — |
| 学历 | `#pf-education` | select | 博士 / 硕士 / 本科 / 大专 |
| 年级 | `#pf-grade` | select | 应届 / 2024级 / 2023级 / 2022级 / 已毕业 |
| 目标岗位 | `#pf-target-job` | input | — |
| 个人简介 | `#pf-bio` | textarea（3 行） | — |

**交互**：
- 默认所有控件 `disabled`（只读）；
- 点击「编辑资料」→ 启用控件，隐藏编辑按钮、显示保存按钮；
- 点击「保存」→ 调 `POST /api/profile/profile/update` 保存，成功后刷新资料并 toast「保存成功」。

---

### 4.5 Tab 二：我的简历（`#panel-resume`）

自上而下包含四块：

**① 双入口卡片 `.dual-entry`**（两列，窄屏一列）：
- 「我有简历」（`.entry-card`，📄）：点击触发隐藏的 `#file-input`，走上传简历流程；
- 「我没有简历」（`.entry-card`，✦）：点击跳转到「AI 职业访谈」Tab。

**② 上传简历卡片**：
- `.upload-zone`：虚线框上传区，支持点击选择与**拖拽上传**（`ondragover/ondrop`）；
- `#file-input`：隐藏的 `<input type="file">`，仅接受 `.pdf/.doc/.docx/.txt`，≤8MB（前端未强制 8MB，后端有格式校验）；
- `#resume-list`：简历列表，每项 `.resume-item` 显示文件名（`.resume-name`）、状态与字符数（`.resume-detail`），右侧「分析」「删除」按钮。

**③ AI 简历分析报告卡 `#analysis-card`**（默认隐藏）：
- 点击简历的「分析」后显示，内容由 `renderAIReport()` 生成，含综合评分圆环、基本信息识别、技能能力分析、项目经历、核心优势、待提升方向，底部有「AI 优化简历」按钮。

**④ AI 优化结果 `#optimize-result`**：点击优化后展示「原始简历 vs AI 优化后」双栏对比。

---

### 4.6 Tab 三：AI 职业访谈（`#panel-interview`）

采用 `.interview-layout` 布局，左窄右宽（窄屏上下堆叠）。

**左侧 `.interview-sidebar` 阶段侧边栏**：
- 标题「职业档案构建」（带青色圆点）；
- 六个阶段条目 `.stage-item`（`#stg-1` ~ `#stg-6`）：
  1. 01 基础信息
  2. 02 教育经历
  3. 03 技能能力
  4. 04 项目经历
  5. 05 职业目标
  6. 06 个人优势
- 底部「当前完成度」`#interview-pct`（百分比数字）。

**右侧 AI 职业顾问聊天卡**：
- 状态标识 `#interview-status`（就绪 / 访谈中 / 已完成）；
- 对话区 `#chat-box`（`.chat-msg.ai` 左对齐、`.chat-msg.user` 右对齐青色渐变）；
- 输入行：`#chat-input` 文本框 + 「发送」+「开始」按钮；
- 「✦ 生成访谈分析报告」按钮，结果渲染到 `#interview-report`。

**交互流程**：开始 → AI 逐条提问 → 用户回答 → 阶段进度随回答数推进 → 访谈完成后自动 `analyzeInterview()` 生成分析报告，并刷新资料、简历、报告。

---

### 4.7 Tab 四：职业画像（`#panel-analysis`）

**① 我的职业画像卡片**（`.analysis-grid` 两列，窄屏一列）：
- 能力雷达图：`.chart-wrap` `#chart-radar`（ECharts 六维雷达：技术能力 / 项目经验 / 数据能力 / 工程能力 / 创新能力 / 学习能力）；
- 职业竞争力：`.gauge-wrap` `#chart-gauge`（ECharts 仪表盘，0-100，三段色：红 <30 / 橙 <70 / 绿 ≥70）；
- 下方「重新分析」按钮，触发 `runAnalysis()`。

**② 第二行 `.analysis-grid`**：
- 技能标签云：`#tag-cloud`（`.skill-tag`，按索引分 `lg/sm` 三档字号）；
- 岗位推荐 TOP 10：`#job-list`（`.match-item`，显示排名圆点、岗位名、公司、匹配度百分比）。

**空状态**：无数据时显示 `.empty-state`（图标 +「请先运行分析」）。

---

## 5. 交互与数据流（API 清单）

前端通过 `UID = 'demo_user'`（**硬编码**）作为当前用户 ID，`API = window.API_BASE || 'http://127.0.0.1:5000'`。所有接口返回统一结构 `{ code, message, data }`。

| 方法 | 接口路径 | 用途 | 触发函数 |
| --- | --- | --- | --- |
| GET | `/api/profile/profile/{user_id}` | 获取资料 + 技能 + 最新报告 | `loadProfile()` |
| POST | `/api/profile/profile/update` | 创建/更新个人资料 | `saveProfile()` |
| GET | `/api/profile/resumes/{user_id}` | 获取简历列表 | `loadResumes()` |
| POST | `/api/profile/resume/upload` | 上传简历并提取文本 | `uploadResume()` |
| DELETE | `/api/profile/resume/{id}` | 删除简历（含磁盘文件） | `deleteResume()` |
| POST | `/api/profile/resume/analyze/{id}` | AI 解析简历 | `analyzeResume()` |
| POST | `/api/profile/resume/optimize` | AI 优化简历 | `optimizeResumeText()` |
| POST | `/api/profile/interview/start` | 开始 AI 职业访谈 | `startInterview()` |
| POST | `/api/profile/interview/chat` | 访谈对话（续聊） | `sendMsg()` |
| POST | `/api/profile/interview/analyze` | 分析访谈生成报告 | `analyzeInterview()` |
| GET | `/api/profile/career/report/{user_id}` | 获取最新职业分析报告 | `loadReport()` |
| POST | `/api/profile/career/analyze/{user_id}` | 运行完整职业画像分析 | `runAnalysis()` |

**关键数据结构**（来自后端 `_profile_to_dict` / `_skill_to_dict` / `_report_to_dict`）：

- **Profile**：`name` `school` `major` `education` `grade` `target_job` `bio` `phone` `email` `avatar_url` `completion`
- **Skill**：`skill_name` `category` `level` `score` `source`（`resume` / `interview` / `manual`）
- **Report**：`tech_score` `project_score` `data_score` `engineering_score` `innovation_score` `learning_score` `overall_score` `advantages` `weaknesses` `suggestions` `match_jobs`

---

## 6. 设计规范（设计变量）

定义于 `profile.css` 顶部 `:root`：

| 类别 | 值 |
| --- | --- |
| 主色 accent | `#2DD4BF`（青色/青绿） |
| 深色背景 | `--bg-deep #080c14`、`--bg-surface #0f1520`、`--bg-card #151c2a` |
| 状态色 | 成功 `#10b981`、警告 `#f59e0b`、危险 `#ef4444`、信息 `#3b82f6` |
| 圆角 | `--radius-sm 8px`、`md 12px`、`lg 16px`、`xl 20px` |
| 字体 | 正文 `DM Sans, Noto Sans SC`；数字 `JetBrains Mono` |
| 边框 | `--border-subtle rgba(255,255,255,.06)`、`--border-card rgba(255,255,255,.08)` |

整体为**深色玻璃拟态**风格：深色卡片 + 半透明边框 + 青色高亮 + 轻微光晕（头像阴影、卡片顶部渐变线、Hero 右上角径向光斑）。

---

## 7. 响应式行为

| 组件 | 桌面端 | 断点 | 窄屏 |
| --- | --- | --- | --- |
| `.kpi-row` | 4 列 | ≤700px | 2 列 |
| `.form-grid` | 2 列 | ≤640px | 1 列 |
| `.dual-entry` | 2 列 | ≤640px | 1 列 |
| `.interview-layout` | 220px + 1fr | ≤700px | 1 列 |
| `.analysis-grid` | 2 列 | ≤700px | 1 列 |
| `.ai-grid-2` | 2 列 | ≤640px | 1 列 |

---

## 8. 已知问题与注意事项

1. **用户 ID 硬编码**：`profile.js` 中 `UID = 'demo_user'` 固定不变，未从登录态（`localStorage.zhitu_user`）读取真实用户 ID，多用户场景下会串数据。
2. **完整度口径不一致**：前端展示的完整度由前端自行计算（6 字段 60% + 技能 20% + 报告 20%），与后端 `user_profiles.completion`（5 字段 × 20%）口径不同。
3. **上传大小限制**：前端提示「≤8MB」但未在 `uploadResume` 中实际校验大小（后端 `profile.py` 只校验格式，不校验大小）。
4. **独立页面无外壳**：本页不含侧边导航/顶栏，返回需浏览器后退（当前按需求「内容先不变」处理）。
5. **依赖后端运行**：所有数据加载依赖 FastAPI 后端（默认 `127.0.0.1:5000`），后端未启动时各面板显示空状态或加载失败。
6. **ECharts 尺寸**：雷达图/仪表盘在容器宽度 <10px 时会先补一个最小高度再初始化（见 `safeChart`）。
