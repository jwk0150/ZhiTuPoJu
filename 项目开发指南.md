# 「执图破局」就业市场岗位-能力知识图谱动态构建与匹配平台

> **提交物主文档 → [`作品设计实现方案_提交版.docx`](./作品设计实现方案_提交版.docx)**（Word 论文体例，评委阅读用；生成脚本 `scripts/devtools/generate_design_doc.py`）

## 项目开发指南（企业级标准 · 历史版本）

> **赛题编号**：XH-202621
> **赛题名称**：多源异构数据驱动岗位和能力图谱构建与动态演化分析研究
> **发榜单位**：科大讯飞
> **赛事**：2026年度中国青年"揭榜挂帅"擂台赛
> **团队规模**：5人
> **文档版本**：v1.0.0
> **文档定位**：团队开发"宪法"，所有研发活动必须严格遵守本规范

---

## 目录

- [第一部分 项目速览](#第一部分-项目速览)
- [第二部分 团队组织架构与分工](#第二部分-团队组织架构与分工)
- [第三部分 技术栈选型（锁定版本）](#第三部分-技术栈选型锁定版本)
- [第四部分 仓库与目录结构](#第四部分-仓库与目录结构)
- [第五部分 Git与协作规范](#第五部分-git与协作规范)
- [第六部分 代码规范](#第六部分-代码规范)
- [第七部分 API规范](#第七部分-api规范)
- [第八部分 数据库规范](#第八部分-数据库规范)
- [第九部分 测试体系](#第九部分-测试体系)
- [第十部分 CI/CD与DevOps](#第十部分-cicd与devops)
- [第十一部分 日志与监控](#第十一部分-日志与监控)
- [第十二部分 文档体系](#第十二部分-文档体系)
- [第十三部分 里程碑与交付物](#第十三部分-里程碑与交付物)
- [第十四部分 风险管控与质量保障](#第十四部分-风险管控与质量保障)
- [附录A 常用命令速查](#附录a-常用命令速查)
- [附录B 项目硬性指标清单](#附录b-项目硬性指标清单)

---

## 第一部分 项目速览

### 1.1 项目目标

构建一个**数据驱动的智能决策系统**——以岗位和能力为核心实体，以图谱为知识载体，以大模型为推理引擎，最终实现人岗匹配和趋势预测的闭环。

### 1.2 核心痛点

- 数字经济快速发展，**技术迭代速度远超人才培养周期**
- 企业在新兴岗位上面临"**招不到合适的人**"的困境
- 新就业形态劳动者**职业发展路径模糊、技能需求动态性强**
- 传统招聘手段**关键词匹配**，缺乏对技术趋势的实时感知能力
- AI生成内容**幻觉问题**严重影响图谱构建科学性

### 1.3 四大核心功能（必须全部实现）

| # | 功能模块 | 具体要求 | 优先级 |
|---|---------|---------|--------|
| 1 | **新岗位发现与定义** | 从多源数据中识别新兴岗位，生成岗位定义（名称、职责、必备/加分技能、行业场景），支持人工优化与动态更新 | P0 |
| 2 | **既有岗位能力动态更新** | 追踪现有岗位（如Java开发工程师）能力变化，明确标注新增/删除/修改项，提供数据源 | P0 |
| 3 | **全景图谱可视化** | 技能点级别粒度，支持按技术栈和级别切换视图 | P0 |
| 4 | **人岗匹配度诊断** | 支持简历解析（PDF/Word），准确率≥90%；多维度匹配分析 + 差距分析 + 学习路径规划 | P0⭐ |

### 1.4 三大硬性指标（必须全部达标）

```
✅ JD解析准确率    ≥ 90%
✅ 简历提取准确率  ≥ 90%
✅ 匹配准确率      ≥ 90%
```

### 1.5 评分标准（满分100分）

```
作品完整性  30分  ← 基础分，所有功能必须闭环
技术创新性  25分  ← 突破点，图谱+RAG融合推理等
用户体验    15分  ← 可视化交互流畅
实用价值    30分  ← 三大90%指标 + 测试方案
```

### 1.6 全链路闭环流程

```
多源数据采集 → 新岗位发现与定义 → 能力图谱动态更新
                                            ↓
              简历解析 ← 精准匹配与差距分析 ← 学习路径推荐
                    ↓
              匹配诊断报告
```

---

## 第二部分 团队组织架构与分工

### 2.1 团队结构（5人）

```
项目经理 / 技术负责人 (Tech Lead)
    ├── 数据工程师 (Data Engineer)
    ├── 后端 / AI工程师 (Backend & AI Engineer)
    ├── 前端工程师 (Frontend Engineer)
    └── 测试 / DevOps工程师 (QA & DevOps Engineer)
```

### 2.2 角色职责矩阵（RACI）

| 模块 | 数据工程师 | 后端/AI工程师 | 前端工程师 | 测试/DevOps | Tech Lead |
|------|:---------:|:------------:|:---------:|:----------:|:---------:|
| **多源数据采集** | **R/A** | C | I | C | A |
| **数据清洗与ETL** | **R/A** | C | - | C | A |
| **Neo4j图谱构建** | C | **R/A** | - | C | A |
| **NER/关系抽取** | C | **R/A** | - | C | A |
| **RAG与大模型调用** | - | **R/A** | - | C | A |
| **人岗匹配算法** | - | **R/A** | C | C | A |
| **简历解析** | C | **R/A** | - | C | A |
| **动态演化分析** | **R/A** | C | C | C | A |
| **后端API** | - | **R/A** | C | C | A |
| **图谱可视化** | - | C | **R/A** | C | A |
| **简历上传与匹配UI** | - | C | **R/A** | C | A |
| **数据看板** | - | C | **R/A** | - | A |
| **单元测试** | R | R | R | **A** | C |
| **集成测试** | C | C | C | **A** | I |
| **CI/CD流水线** | - | C | - | **R/A** | A |
| **部署与运维** | - | C | - | **R/A** | A |
| **文档与答辩** | C | C | C | C | **A** |

> **R** = 执行 / **A** = 负责 / **C** = 咨询 / **I** = 知会

### 2.3 人员分工详细定义

#### 👨‍💼 Tech Lead（项目经理/技术负责人）
- **代码所有权**：架构设计、核心算法模块、跨模块集成
- **职责**：
  - 整体架构设计与技术决策
  - 代码审查（Code Review）把关
  - 进度管理与风险预警
  - 与发榜方对接与汇报
  - 答辩PPT与文档终稿审核

#### 👨‍💻 数据工程师
- **代码所有权**：`/data-collection`、`/data-processing`、`/data-analysis`
- **职责**：
  - Scrapy + Playwright 多源爬虫开发
  - ETL管道（去重/格式统一/质量评分）
  - 时序数据管理（数据版本化）
  - NetworkX 图算法（中心性、社区发现）
  - 时间序列分析与趋势预测

#### 👨‍💻 后端/AI工程师
- **代码所有权**：`/backend`、`/ai-core`、`/knowledge-graph`
- **职责**：
  - FastAPI 后端架构
  - 讯飞星火API集成与Prompt工程
  - NER模型微调（BERT-CRF/GlobalPointer）
  - Neo4j图谱Schema设计与Cypher优化
  - RAG链路编排（LangChain）
  - 简历解析（讯飞OCR + NER）
  - 人岗匹配算法（语义相似度 + 图谱路径距离）

#### 👨‍💻 前端工程师
- **代码所有权**：`/frontend`
- **职责**：
  - Vue 3 + Vite + Pinia 项目搭建
  - AntV G6 图谱可视化（核心亮点）
  - ECharts 趋势图表
  - 简历上传与匹配结果展示
  - 对话式查询界面（自然语言问答）
  - 响应式设计与交互优化

#### 👨‍🔧 测试/DevOps工程师
- **代码所有权**：`/tests`、`/.github`、`/docker`、`/scripts`
- **职责**：
  - 测试用例设计与维护（覆盖率≥60%）
  - 准确率评估体系（JD解析/简历提取/匹配）
  - Docker镜像构建与一键部署
  - GitHub Actions CI/CD流水线
  - 监控告警与日志系统

### 2.4 沟通机制

| 会议 | 频率 | 时长 | 参与人 | 输出 |
|------|------|------|--------|------|
| **每日站会** | 每日 | 15min | 全员 | 昨日完成/今日计划/阻塞项 |
| **周会** | 每周一 | 1h | 全员 | 周计划对齐、风险评审 |
| **代码评审** | PR时 | 异步 | 相关模块负责人 | 代码质量 |
| **技术评审** | 双周 | 1h | 全员 | 架构决策、技术难点研讨 |
| **里程碑评审** | 关键节点 | 2h | 全员 | 阶段交付物验收 |

### 2.5 协作工具

| 用途 | 工具 |
|------|------|
| 代码托管 | GitHub / Gitee |
| 文档协作 | 飞书 / Notion |
| 任务管理 | GitHub Issues / Projects |
| 即时通讯 | 飞书 / 微信 |
| 设计协作 | Figma / 即时设计 |
| API文档 | Swagger / Apifox |

---

## 第三部分 技术栈选型（锁定版本）

> ⚠️ **铁律**：所有依赖必须锁定版本，禁止随意升级。如需升级须经Tech Lead审批。

### 3.1 后端技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 语言 | Python | 3.11.x | 主开发语言 |
| Web框架 | FastAPI | 0.115.x | 高性能、自动OpenAPI文档 |
| 异步任务 | Celery + Redis | 5.4.x / 7.x | 数据采集、图谱构建 |
| ORM | SQLAlchemy | 2.0.x | 数据库ORM |
| 数据库 | MySQL | 8.0.x | 结构化业务数据 |
| 搜索引擎 | Elasticsearch | 8.x | 全文检索与聚合 |
| 图数据库 | Neo4j | 5.x | 知识图谱存储 |
| 向量数据库 | ChromaDB | 0.5.x | RAG向量存储 |
| NLP工具 | HanLP | 2.1.x | 中文分词、NER |
| LLM调用 | LangChain | 0.3.x | RAG编排 |
| LLM推理 | 讯飞星火 X2 / 4.0 Turbo | - | 赛题方推荐 |
| Embedding | BGE-M3 / bge-large-zh | - | 中文向量化 |
| 图算法 | NetworkX | 3.3.x | 图分析算法 |
| 数据处理 | Pandas | 2.2.x | 数据清洗与分析 |
| 文档解析 | python-docx, pdfplumber | latest | 简历解析 |

### 3.2 前端技术栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Vue | 3.5.x | 主框架（Composition API） |
| 构建工具 | Vite | 5.x | 构建工具 |
| 状态管理 | Pinia | 2.2.x | 状态管理 |
| 路由 | Vue Router | 4.x | 路由管理 |
| UI组件库 | Ant Design Vue | 4.x | 组件库 |
| 图谱可视化 | AntV G6 | 5.x | 核心可视化 |
| 图表 | ECharts | 5.5.x | 趋势图表 |
| HTTP客户端 | Axios | 1.7.x | HTTP请求 |
| 工具库 | VueUse | 11.x | Composition API工具集 |
| 代码规范 | ESLint + Prettier | - | 代码风格统一 |

### 3.3 数据采集栈

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 爬虫框架 | Scrapy | 2.12.x | 静态页面爬取 |
| 浏览器自动化 | Playwright | 1.48.x | JS动态页面 |
| 代理池 | scrapy-rotating-proxies | - | 反爬策略 |
| 任务队列 | Redis + Celery | - | 分布式爬取 |

### 3.4 基础设施

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 容器化 | Docker + Docker Compose | 24.x / v2.x | 一键部署 |
| 反向代理 | Nginx | 1.27.x | 负载均衡 |
| 监控 | Prometheus + Grafana | - | 监控告警 |
| 日志 | ELK Stack (Elasticsearch + Logstash + Kibana) | 8.x | 日志分析 |
| CI/CD | GitHub Actions | - | 自动化流水线 |

---

## 第四部分 仓库与目录结构

### 4.1 Monorepo 单仓多包结构

```
tuzhupoju-platform/
├── .github/                        # GitHub Actions配置
│   ├── workflows/
│   │   ├── ci.yml                  # 持续集成
│   │   ├── cd.yml                  # 持续部署
│   │   └── code-review.yml         # 自动代码审查
│   ├── ISSUE_TEMPLATE/             # Issue模板
│   └── PULL_REQUEST_TEMPLATE.md    # PR模板
├── .vscode/                        # VSCode工作区配置
│   ├── settings.json
│   ├── extensions.json
│   └── launch.json
├── docs/                           # 项目文档
│   ├── 01-architecture/            # 架构文档
│   ├── 02-design/                  # 设计文档
│   ├── 03-api/                     # API文档
│   ├── 04-deployment/              # 部署文档
│   ├── 05-testing/                 # 测试文档
│   ├── 06-development/             # 开发规范
│   └── 07-submission/              # 提交物
├── frontend/                       # 前端项目 (Vue 3)
│   ├── src/
│   │   ├── api/                    # API封装
│   │   ├── assets/                 # 静态资源
│   │   ├── components/             # 通用组件
│   │   ├── composables/            # 组合式函数
│   │   ├── layouts/                # 布局组件
│   │   ├── router/                 # 路由
│   │   ├── stores/                 # Pinia状态
│   │   ├── styles/                 # 全局样式
│   │   ├── utils/                  # 工具函数
│   │   ├── views/                  # 页面组件
│   │   │   ├── Dashboard/          # 数据看板
│   │   │   ├── KnowledgeGraph/     # 图谱可视化
│   │   │   ├── JobMatch/           # 人岗匹配
│   │   │   ├── DataCollection/     # 数据采集
│   │   │   └── Settings/           # 系统设置
│   │   ├── App.vue
│   │   └── main.ts
│   ├── public/
│   ├── tests/                      # 前端测试
│   ├── index.html
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── package.json
│   ├── .eslintrc.cjs
│   └── README.md
├── backend/                        # 后端项目 (Python)
│   ├── app/
│   │   ├── api/                    # API路由层
│   │   │   ├── v1/
│   │   │   │   ├── endpoints/      # 端点
│   │   │   │   │   ├── jobs.py     # 岗位
│   │   │   │   │   ├── graph.py    # 图谱
│   │   │   │   │   ├── match.py    # 匹配
│   │   │   │   │   ├── resume.py   # 简历
│   │   │   │   │   └── analysis.py # 分析
│   │   │   │   └── router.py
│   │   │   └── deps.py             # 依赖注入
│   │   ├── core/                   # 核心配置
│   │   │   ├── config.py           # 配置管理
│   │   │   ├── security.py         # 安全
│   │   │   ├── logging.py          # 日志
│   │   │   └── exceptions.py       # 异常
│   │   ├── models/                 # 数据模型
│   │   │   ├── domain/             # 领域模型
│   │   │   ├── schemas/            # Pydantic模式
│   │   │   └── entities/           # 实体类
│   │   ├── services/               # 业务服务层
│   │   │   ├── job_service.py
│   │   │   ├── graph_service.py
│   │   │   ├── match_service.py
│   │   │   └── resume_service.py
│   │   ├── repositories/           # 仓储层
│   │   │   ├── mysql_repo.py
│   │   │   ├── neo4j_repo.py
│   │   │   └── elastic_repo.py
│   │   ├── ai/                     # AI能力层
│   │   │   ├── llm/                # 大模型
│   │   │   │   ├── spark_client.py # 讯飞星火
│   │   │   │   ├── prompt.py
│   │   │   │   └── chains.py
│   │   │   ├── ner/                # 命名实体识别
│   │   │   ├── embedding/          # 向量化
│   │   │   ├── rag/                # RAG
│   │   │   └── hallucination/      # 幻觉检测
│   │   ├── knowledge_graph/        # 知识图谱
│   │   │   ├── builder.py          # 构建器
│   │   │   ├── schema.py           # 图谱Schema
│   │   │   ├── cypher.py           # 查询封装
│   │   │   └── evolution.py        # 动态演化
│   │   ├── matching/               # 人岗匹配
│   │   │   ├── semantic.py         # 语义匹配
│   │   │   ├── graph_match.py      # 图谱匹配
│   │   │   └── scorer.py           # 评分
│   │   ├── utils/                  # 工具函数
│   │   └── main.py                 # 应用入口
│   ├── tests/                      # 后端测试
│   │   ├── unit/
│   │   ├── integration/
│   │   ├── e2e/
│   │   ├── fixtures/
│   │   └── conftest.py
│   ├── alembic/                    # 数据库迁移
│   ├── scripts/                    # 运维脚本
│   ├── pyproject.toml
│   ├── requirements.txt
│   ├── requirements-dev.txt
│   └── README.md
├── data-collection/                # 数据采集子项目
│   ├── spiders/                    # Scrapy爬虫
│   ├── pipelines/                  # 数据管道
│   ├── items.py
│   ├── settings.py
│   ├── middlewares.py
│   └── README.md
├── data-processing/                # 数据处理子项目
│   ├── etl/                        # ETL管道
│   ├── cleaning/                   # 清洗
│   ├── quality/                    # 质量评估
│   └── README.md
├── docker/                         # Docker配置
│   ├── backend.Dockerfile
│   ├── frontend.Dockerfile
│   ├── nginx/
│   │   └── nginx.conf
│   ├── neo4j/
│   ├── mysql/
│   ├── redis/
│   └── elasticsearch/
├── docker-compose.yml              # 一键启动
├── docker-compose.prod.yml         # 生产环境
├── .env.example                    # 环境变量示例
├── .gitignore
├── .editorconfig
├── .pre-commit-config.yaml         # Git Hooks
├── pyproject.toml                  # Python项目配置
├── package.json                    # 前端项目配置（monorepo）
├── README.md
├── LICENSE
└── CONTRIBUTING.md
```

### 4.2 关键目录说明

| 目录 | 责任人 | 说明 |
|------|--------|------|
| `/frontend/src/views/KnowledgeGraph` | 前端 | 图谱可视化核心模块 |
| `/backend/app/knowledge_graph` | 后端 | 图谱构建与查询 |
| `/backend/app/ai` | 后端 | AI能力（大模型/NER/RAG） |
| `/data-collection` | 数据 | 爬虫集群 |
| `/docker` | DevOps | 容器化配置 |

---

## 第五部分 Git与协作规范

### 5.1 分支策略（GitFlow变体）

```
main (生产) ─────────────────────────────●───────
  ↑                                       ↑
release/v1.0 ──────●──────●──────────────┘
                     ↑      ↑
develop (开发) ──●──●──●──●────●──●──●──────
                ↑   ↑  ↑   ↑    ↑  ↑  ↑
              feat  fix feat fix feat fix
```

| 分支类型 | 命名规范 | 生命周期 | 权限 |
|---------|---------|---------|------|
| `main` | `main` | 永久 | 仅Tech Lead可推送 |
| `develop` | `develop` | 永久 | Tech Lead |
| `feature/*` | `feature/job-discovery` | 短期 | 开发者 |
| `bugfix/*` | `bugfix/neo4j-connection` | 短期 | 开发者 |
| `hotfix/*` | `hotfix/critical-bug` | 短期 | Tech Lead |
| `release/*` | `release/v1.0.0` | 中期 | Tech Lead |
| `docs/*` | `docs/api-update` | 短期 | 全部 |

### 5.2 提交规范（Conventional Commits）

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 提交类型

| 类型 | 描述 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(graph): 新增岗位-能力关系抽取模块` |
| `fix` | 修复Bug | `fix(api): 修复简历上传接口超时问题` |
| `docs` | 文档变更 | `docs(readme): 更新部署说明` |
| `style` | 代码格式 | `style(frontend): 统一ESLint规则` |
| `refactor` | 重构 | `refactor(backend): 拆分匹配服务` |
| `perf` | 性能优化 | `perf(neo4j): 优化Cypher查询性能` |
| `test` | 测试 | `test(match): 添加匹配单元测试` |
| `chore` | 构建/工具 | `chore(deps): 升级FastAPI到0.115` |
| `ci` | CI/CD | `ci(github): 添加自动化部署` |

#### Scope 范围

`backend`、`frontend`、`data`、`graph`、`ai`、`docker`、`docs`、`ci`、`deps`

### 5.3 Commit Message 范例

```bash
feat(graph): 新增岗位-能力关系抽取模块

- 集成讯飞星火API进行关系抽取
- 实现实体消歧算法
- 添加Neo4j写入管道
- 单元测试覆盖率92%

Closes #123
```

### 5.4 Pull Request 规范

#### PR 模板（.github/PULL_REQUEST_TEMPLATE.md）

```markdown
## 变更类型
- [ ] 新功能 (feat)
- [ ] Bug修复 (fix)
- [ ] 重构 (refactor)
- [ ] 文档 (docs)
- [ ] 测试 (test)
- [ ] 性能优化 (perf)
- [ ] 其他: ___

## 变更描述
<!-- 详细描述本次变更的内容 -->

## 关联Issue
<!-- 关联的Issue编号 -->

## 测试情况
- [ ] 已添加单元测试
- [ ] 已添加集成测试
- [ ] 已本地测试通过
- [ ] 测试覆盖率达标

## 截图/录屏
<!-- 如果有UI变更，请提供截图 -->

## Checklist
- [ ] 代码遵循项目代码规范
- [ ] 已进行自我代码审查
- [ ] 已更新相关文档
- [ ] 通过CI流水线检查
- [ ] 至少1位团队成员已Review
```

#### PR 合并要求

1. ✅ 通过CI流水线（lint + test + build）
2. ✅ 至少1人Code Review通过
3. ✅ 无冲突
4. ✅ 分支名符合规范
5. ✅ 提交信息符合规范

### 5.5 冲突解决原则

1. **谁创建谁负责**：发起PR的开发者负责解决冲突
2. **沟通优先**：冲突复杂时，与原作者沟通后再合并
3. **谨慎合并**：避免在周五下午合并高风险PR

---

## 第六部分 代码规范

### 6.1 Python 代码规范

#### 基础规范
- 遵循 **PEP 8** + **Google Python Style Guide**
- 使用 **Black** 格式化（行宽100）
- 使用 **isort** 排序import
- 使用 **flake8 + pylint** 静态检查
- 使用 **mypy** 类型检查

#### 命名规范

```python
# 模块名：小写+下划线
job_discovery.py

# 类名：PascalCase
class JobDiscoveryService:
    pass

# 函数/方法：小写+下划线
def extract_job_skills():
    pass

# 变量：小写+下划线
job_id = "123"

# 常量：大写+下划线
MAX_RETRY_COUNT = 3

# 私有成员：前缀下划线
class _PrivateHelper:
    pass
```

#### 类型注解（强制）

```python
from typing import List, Dict, Optional, Union
from pydantic import BaseModel

class JobInfo(BaseModel):
    """岗位信息模型"""
    job_id: str
    title: str
    skills: List[str]
    salary_range: Optional[Dict[str, Union[int, str]]] = None

def parse_resume(file_path: str) -> JobInfo:
    """解析简历文件
    
    Args:
        file_path: 简历文件路径，支持PDF/Word
        
    Returns:
        解析后的岗位信息
        
    Raises:
        FileNotFoundError: 文件不存在
        ValueError: 文件格式错误
    """
    ...
```

#### 注释规范

```python
def calculate_match_score(
    candidate_skills: List[str],
    job_required_skills: List[str],
    graph_distance: float
) -> float:
    """计算候选人-岗位匹配分数
    
    综合考虑技能重合度、图谱语义距离、经验匹配度等维度。
    
    Args:
        candidate_skills: 候选人掌握的技能列表
        job_required_skills: 岗位必备技能列表
        graph_distance: 图谱语义距离（越小越相关）
        
    Returns:
        0-100之间的匹配分数
        
    Example:
        >>> score = calculate_match_score(["Python", "ML"], ["Python", "DL"], 0.2)
        >>> 85.5
    """
    # 1. 技能重合度计算
    overlap = set(candidate_skills) & set(job_required_skills)
    skill_score = len(overlap) / len(job_required_skills) * 60
    
    # 2. 图谱语义距离得分
    graph_score = max(0, 30 - graph_distance * 100)
    
    # 3. 经验加成
    base_score = skill_score + graph_score
    return min(100, base_score)
```

### 6.2 TypeScript/Vue 代码规范

#### 基础规范
- **ESLint** + **Prettier** 强制格式化
- 使用 **Composition API** + `<script setup>`
- 组件命名使用 **PascalCase**
- props/emit 使用 **类型定义**

#### 组件结构

```vue
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import type { JobInfo } from '@/types'

// 1. Props定义
interface Props {
  jobData: JobInfo
  readonly?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  readonly: false
})

// 2. Emits定义
const emit = defineEmits<{
  (e: 'update', value: JobInfo): void
  (e: 'delete', id: string): void
}>()

// 3. 响应式状态
const loading = ref(false)
const skills = computed(() => props.jobData.skills)

// 4. 方法
const handleUpdate = async () => {
  loading.value = true
  try {
    emit('update', props.jobData)
  } finally {
    loading.value = false
  }
}

// 5. 生命周期
onMounted(() => {
  console.log('JobCard mounted')
})
</script>

<template>
  <a-card class="job-card" :loading="loading">
    <template #title>{{ jobData.title }}</template>
    <a-tag v-for="skill in skills" :key="skill">{{ skill }}</a-tag>
    <template #actions>
      <a-button @click="handleUpdate">更新</a-button>
    </template>
  </a-card>
</template>

<style scoped lang="scss">
.job-card {
  margin-bottom: 16px;
}
</style>
```

### 6.3 代码审查清单（Review Checklist）

#### 后端
- [ ] 是否符合分层架构（API/Service/Repository）
- [ ] 是否有完整的类型注解
- [ ] 是否有完整的docstring
- [ ] 是否有单元测试
- [ ] 错误处理是否完善
- [ ] 日志记录是否充分
- [ ] 是否有SQL注入风险
- [ ] 是否有敏感信息泄露
- [ ] 性能是否达标（如大查询是否分页）

#### 前端
- [ ] 组件是否单一职责
- [ ] 是否正确使用Composition API
- [ ] 是否有TypeScript类型定义
- [ ] 是否有loading/error状态
- [ ] 是否做了响应式适配
- [ ] 是否符合无障碍（A11y）标准
- [ ] 是否存在内存泄漏风险

---

## 第七部分 API规范

### 7.1 RESTful 规范

#### URL 设计原则
```
GET    /api/v1/jobs                  # 列表
GET    /api/v1/jobs/{id}             # 详情
POST   /api/v1/jobs                  # 创建
PUT    /api/v1/jobs/{id}             # 全量更新
PATCH  /api/v1/jobs/{id}             # 部分更新
DELETE /api/v1/jobs/{id}             # 删除

# 业务URL
POST   /api/v1/resumes/upload        # 上传简历
POST   /api/v1/match                 # 人岗匹配
GET    /api/v1/graph/visualize       # 图谱可视化数据
GET    /api/v1/graph/evolution       # 图谱演化分析
POST   /api/v1/jobs/discover         # 新岗位发现
```

#### HTTP 状态码

| 状态码 | 含义 | 使用场景 |
|--------|------|---------|
| 200 | OK | 请求成功 |
| 201 | Created | 资源创建成功 |
| 204 | No Content | 删除成功 |
| 400 | Bad Request | 请求参数错误 |
| 401 | Unauthorized | 未认证 |
| 403 | Forbidden | 无权限 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 资源冲突 |
| 422 | Unprocessable Entity | 业务校验失败 |
| 500 | Internal Server Error | 服务器错误 |

### 7.2 统一响应格式

#### 成功响应
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "job_id": "J001",
    "title": "AI算法工程师",
    "skills": ["Python", "PyTorch", "LLM"]
  },
  "trace_id": "abc-123-def"
}
```

#### 列表响应（分页）
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [...],
    "total": 1024,
    "page": 1,
    "page_size": 20,
    "total_pages": 52
  },
  "trace_id": "abc-123-def"
}
```

#### 错误响应
```json
{
  "code": 40001,
  "message": "简历文件格式不支持",
  "data": null,
  "trace_id": "abc-123-def",
  "details": {
    "field": "file_type",
    "supported": ["pdf", "docx"]
  }
}
```

### 7.3 业务错误码定义

| 错误码 | 含义 | HTTP状态 |
|--------|------|---------|
| 0 | 成功 | 200 |
| 40001 | 参数错误 | 400 |
| 40101 | 未登录 | 401 |
| 40301 | 无权限 | 403 |
| 40401 | 资源不存在 | 404 |
| 40901 | 数据冲突 | 409 |
| 42201 | 业务校验失败 | 422 |
| 50001 | 服务器内部错误 | 500 |
| 50002 | 大模型调用失败 | 500 |
| 50003 | 数据库错误 | 500 |
| 50004 | 图谱查询失败 | 500 |

### 7.4 Swagger/OpenAPI 文档

- **强制要求**：所有API必须有OpenAPI注解
- **自动生成**：FastAPI 自动生成 `/docs` 和 `/redoc`
- **导出**：CI流水线自动导出OpenAPI JSON同步到Apifox

### 7.5 关键API示例

#### 1. 简历上传与解析

```yaml
POST /api/v1/resumes/parse
Content-Type: multipart/form-data

Request:
  file: <binary>

Response:
  {
    "code": 0,
    "data": {
      "candidate_id": "C001",
      "name": "张三",
      "skills": ["Python", "机器学习", "深度学习"],
      "experience": [...],
      "education": [...],
      "match_jobs": [
        {
          "job_id": "J001",
          "title": "AI算法工程师",
          "match_score": 85.5,
          "missing_skills": ["CUDA", "分布式训练"]
        }
      ]
    }
  }
```

#### 2. 图谱可视化数据

```yaml
GET /api/v1/graph/visualize?category=AI&level=mid

Response:
  {
    "code": 0,
    "data": {
      "nodes": [
        {"id": "J001", "label": "AI算法工程师", "type": "job"},
        {"id": "S001", "label": "Python", "type": "skill"}
      ],
      "edges": [
        {"source": "J001", "target": "S001", "relation": "requires"}
      ]
    }
  }
```

---

## 第八部分 数据库规范

### 8.1 MySQL 命名规范

#### 数据库命名
```sql
-- 数据库：小写+下划线
tuzhupoju_platform

-- 表名：小写+下划线，复数形式
job_infos
resume_records
match_results
```

#### 字段命名
```sql
-- 字段名：小写+下划线
id
job_id
title
created_at
updated_at
is_deleted
```

#### 必备字段（每张表）

```sql
CREATE TABLE `job_infos` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `job_id` VARCHAR(64) NOT NULL COMMENT '业务唯一标识',
  `title` VARCHAR(255) NOT NULL COMMENT '岗位名称',
  `description` TEXT COMMENT '岗位描述',
  `source` VARCHAR(32) NOT NULL COMMENT '数据来源',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除(0否1是)',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_job_id` (`job_id`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='岗位信息表';
```

### 8.2 Neo4j 命名规范

#### 节点标签（PascalCase）
- `Job` - 岗位
- `Skill` - 技能
- `Industry` - 行业
- `Company` - 公司
- `Category` - 岗位类别

#### 关系类型（UPPER_SNAKE_CASE）
- `REQUIRES` - 岗位需要技能
- `BELONGS_TO` - 属于某类别
- `RELATED_TO` - 相关
- `EVOLVED_TO` - 演化为
- `PRECEDES` - 前置

#### 属性命名（camelCase）
```cypher
CREATE (j:Job {
  jobId: 'J001',
  title: 'AI算法工程师',
  createdAt: datetime(),
  updatedAt: datetime(),
  source: 'lagou'
})
```

### 8.3 数据库版本管理

使用 **Alembic** 管理MySQL迁移：

```bash
# 生成迁移文件
alembic revision --autogenerate -m "create job_infos table"

# 应用迁移
alembic upgrade head

# 回滚
alembic downgrade -1
```

Neo4j使用 **Cypher脚本** + **版本号命名**：

```
neo4j/migrations/
├── V1.0__init_schema.cypher
├── V1.1__add_skill_relations.cypher
└── V1.2__add_temporal_index.cypher
```

---

## 第九部分 测试体系

### 9.1 测试金字塔

```
         /\
        /E2E\           ← 端到端测试（少量）
       /------\
      /  集成  \         ← 集成测试（适量）
     /----------\
    /   单元测试  \      ← 单元测试（大量）
   /--------------\
```

### 9.2 后端测试

#### 测试分层

| 层级 | 工具 | 覆盖率目标 | 说明 |
|------|------|----------|------|
| 单元测试 | pytest | 80%+ | 测试函数/类 |
| 集成测试 | pytest + TestClient | 60%+ | 测试API端点 |
| E2E测试 | pytest + Playwright | 核心流程 | 测试完整链路 |

#### pytest 配置

```python
# pytest.ini
[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --strict-markers --cov=app --cov-report=html --cov-report=term-missing"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: 单元测试",
    "integration: 集成测试",
    "e2e: 端到端测试",
    "slow: 慢速测试",
    "requires_llm: 需要大模型API"
]
```

#### 测试示例

```python
import pytest
from app.services.match_service import MatchService
from app.models.schemas import JobInfo, CandidateInfo

class TestMatchService:
    """匹配服务单元测试"""
    
    @pytest.fixture
    def match_service(self):
        return MatchService()
    
    @pytest.fixture
    def sample_job(self):
        return JobInfo(
            job_id="J001",
            title="AI算法工程师",
            required_skills=["Python", "PyTorch", "LLM"]
        )
    
    @pytest.fixture
    def sample_candidate(self):
        return CandidateInfo(
            candidate_id="C001",
            name="张三",
            skills=["Python", "机器学习", "TensorFlow"]
        )
    
    def test_calculate_match_score_full_overlap(self, match_service, sample_job, sample_candidate):
        """测试完全匹配场景"""
        score = match_service.calculate_match_score(sample_job, sample_candidate)
        assert 0 <= score <= 100
        assert score >= 50  # 至少50分
    
    def test_match_with_missing_skills(self, match_service):
        """测试技能缺失场景"""
        job = JobInfo(job_id="J002", title="资深算法工程师",
                      required_skills=["CUDA", "分布式", "LLM"])
        candidate = CandidateInfo(candidate_id="C002", name="李四",
                                  skills=["Python"])
        result = match_service.match(job, candidate)
        assert "CUDA" in result.missing_skills
        assert result.match_score < 50
```

### 9.3 准确率测试（核心硬性指标）

#### JD解析准确率测试

```python
# tests/accuracy/test_jd_parse_accuracy.py

GROUND_TRUTH_JDS = [
    {
        "job_id": "J001",
        "raw_text": "招聘AI算法工程师，要求Python、PyTorch，3年以上经验",
        "expected": {
            "title": "AI算法工程师",
            "required_skills": ["Python", "PyTorch"],
            "experience_years": 3
        }
    },
    # ... 至少100条测试用例
]

def test_jd_parse_accuracy():
    """JD解析准确率必须 ≥ 90%"""
    correct = 0
    total = len(GROUND_TRUTH_JDS)
    
    for case in GROUND_TRUTH_JDS:
        result = jd_parser.parse(case["raw_text"])
        if is_match(result, case["expected"]):
            correct += 1
    
    accuracy = correct / total * 100
    assert accuracy >= 90, f"JD解析准确率{accuracy}% < 90%"
```

#### 简历提取准确率测试
#### 人岗匹配准确率测试

### 9.4 前端测试

```typescript
// frontend/tests/unit/components/JobCard.spec.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import JobCard from '@/components/JobCard.vue'

describe('JobCard.vue', () => {
  it('renders job title correctly', () => {
    const wrapper = mount(JobCard, {
      props: {
        job: { id: 'J001', title: 'AI工程师', skills: ['Python'] }
      }
    })
    expect(wrapper.text()).toContain('AI工程师')
  })
  
  it('emits update event on button click', async () => {
    const wrapper = mount(JobCard, {
      props: { job: { id: 'J001', title: 'AI工程师', skills: [] } }
    })
    await wrapper.find('button').trigger('click')
    expect(wrapper.emitted('update')).toBeTruthy()
  })
})
```

### 9.5 测试覆盖率要求

| 模块 | 最低覆盖率 |
|------|----------|
| 核心业务逻辑 | 80% |
| API层 | 70% |
| 工具函数 | 90% |
| AI/图谱层 | 60% |
| **整体项目** | **≥ 60%（赛题硬性要求）** |

### 9.6 测试数据准备

#### JD测试数据集

```
data/test/
├── jd_test_set_v1.jsonl       # 100条JD测试集
├── resume_test_set_v1/        # 50份简历样本
│   ├── sample_001.pdf
│   ├── sample_002.docx
│   └── ground_truth.json
└── match_test_set_v1.jsonl    # 100条匹配测试用例
```

---

## 第十部分 CI/CD与DevOps

### 10.1 CI流水线（GitHub Actions）

#### .github/workflows/ci.yml

```yaml
name: CI Pipeline

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  backend-test:
    name: Backend Tests
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: test123
        ports: ['3306:3306']
        options: --health-cmd="mysqladmin ping" --health-interval=10s
      neo4j:
        image: neo4j:5.20
        env:
          NEO4J_AUTH: neo4j/test123
        ports: ['7687:7687']
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/**/requirements*.txt') }}
      
      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt -r requirements-dev.txt
      
      - name: Lint with flake8
        run: |
          cd backend
          flake8 app/ --max-line-length=100
      
      - name: Type check with mypy
        run: |
          cd backend
          mypy app/
      
      - name: Run tests
        env:
          MYSQL_HOST: 127.0.0.1
          NEO4J_URI: bolt://127.0.0.1:7687
        run: |
          cd backend
          pytest --cov=app --cov-report=xml --cov-fail-under=60
      
      - name: Upload coverage
        uses: codecov/codecov-action@v4
  
  frontend-test:
    name: Frontend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        run: npm install -g pnpm
      - name: Install deps
        run: |
          cd frontend
          pnpm install --frozen-lockfile
      - name: Lint
        run: |
          cd frontend
          pnpm lint
      - name: Type check
        run: |
          cd frontend
          vue-tsc --noEmit
      - name: Test
        run: |
          cd frontend
          pnpm test:unit --coverage
      - name: Build
        run: |
          cd frontend
          pnpm build
  
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-test]
    steps:
      - uses: actions/checkout@v4
      - name: Build images
        run: docker-compose -f docker-compose.yml build
```

### 10.2 CD流水线

```yaml
# .github/workflows/cd.yml
name: CD Pipeline

on:
  push:
    tags: ['v*']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build and push images
        run: |
          docker build -t registry.example.com/tuzhupoju/backend:${{ github.ref_name }} -f docker/backend.Dockerfile .
          docker push registry.example.com/tuzhupoju/backend:${{ github.ref_name }}
      
      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /opt/tuzhupoju
            docker-compose pull
            docker-compose up -d
```

### 10.3 Docker Compose 配置

#### docker-compose.yml（开发环境）

```yaml
version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mysql://root:pass@mysql:3306/tuzhupoju
      - NEO4J_URI=bolt://neo4j:7687
      - REDIS_URL=redis://redis:6379
    depends_on:
      - mysql
      - neo4j
      - redis
  
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
  
  celery-worker:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    command: celery -A app.tasks worker -l info
    depends_on:
      - redis
      - mysql
  
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: pass
      MYSQL_DATABASE: tuzhupoju
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
  
  neo4j:
    image: neo4j:5.20
    environment:
      NEO4J_AUTH: neo4j/pass
    volumes:
      - neo4j_data:/data
    ports:
      - "7474:7474"
      - "7687:7687"
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  elasticsearch:
    image: elasticsearch:8.11.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    volumes:
      - es_data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"

volumes:
  mysql_data:
  neo4j_data:
  es_data:
```

### 10.4 一键启动

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f backend

# 初始化数据
docker-compose exec backend python scripts/init_db.py

# 停止所有服务
docker-compose down
```

---

## 第十一部分 日志与监控

### 11.1 日志规范

#### 日志级别

| 级别 | 使用场景 | 示例 |
|------|---------|------|
| DEBUG | 调试信息 | `logger.debug("进入parse_resume函数")` |
| INFO | 正常运行信息 | `logger.info("简历解析成功，candidate_id=C001")` |
| WARNING | 警告（不影响运行） | `logger.warning("简历图片质量较低")` |
| ERROR | 错误（功能失败） | `logger.error("大模型API调用失败")` |
| CRITICAL | 严重错误 | `logger.critical("数据库连接断开")` |

#### 结构化日志格式（JSON）

```python
import structlog

logger = structlog.get_logger()

logger.info(
    "resume_parsed",
    candidate_id="C001",
    file_size=1024000,
    parse_time_ms=1250,
    skills_count=8,
    trace_id="abc-123"
)
```

输出：
```json
{
  "event": "resume_parsed",
  "candidate_id": "C001",
  "file_size": 1024000,
  "parse_time_ms": 1250,
  "skills_count": 8,
  "trace_id": "abc-123",
  "timestamp": "2026-07-16T10:30:00Z",
  "level": "info"
}
```

### 11.2 Trace ID（链路追踪）

每个请求生成唯一 `trace_id`，贯穿整个调用链：

```python
import uuid
from contextvars import ContextVar

trace_id_var: ContextVar[str] = ContextVar('trace_id', default='')

def get_trace_id() -> str:
    if not trace_id_var.get():
        trace_id_var.set(str(uuid.uuid4()))
    return trace_id_var.get()
```

### 11.3 关键监控指标

| 指标 | 阈值 | 告警 |
|------|------|------|
| API响应时间 P95 | < 2s | > 5s |
| 大模型API成功率 | ≥ 99% | < 95% |
| 简历解析准确率 | ≥ 90% | < 85% |
| 图谱查询响应时间 | < 500ms | > 1s |
| 数据库连接数 | < 80% | > 90% |
| 磁盘使用率 | < 70% | > 85% |

---

## 第十二部分 文档体系

### 12.1 文档目录结构

```
docs/
├── README.md                        # 文档导航
├── 01-architecture/                 # 架构文档
│   ├── system-architecture.md       # 系统架构
│   ├── tech-stack.md                # 技术栈说明
│   └── module-design.md             # 模块设计
├── 02-design/                       # 设计文档
│   ├── ui-design.md                 # UI设计
│   ├── database-design.md           # 数据库设计
│   └── api-design.md                # API设计
├── 03-api/                          # API文档
│   ├── openapi.json                 # OpenAPI规范
│   └── api-guide.md                 # API使用指南
├── 04-deployment/                   # 部署文档
│   ├── docker-deploy.md             # Docker部署
│   ├── production-deploy.md         # 生产部署
│   └── troubleshooting.md           # 故障排查
├── 05-testing/                      # 测试文档
│   ├── test-plan.md                 # 测试方案
│   ├── test-cases.md                # 测试用例
│   └── accuracy-report.md           # 准确率报告
├── 06-development/                  # 开发规范
│   ├── coding-standards.md          # 代码规范
│   ├── git-workflow.md              # Git工作流
│   └── code-review-checklist.md     # 代码审查清单
└── 07-submission/                   # 提交物
    ├── design-proposal.md           # 设计方案
    ├── presentation.md              # 答辩PPT
    └── demo-video-script.md         # 演示视频脚本
```

### 12.2 必备文档清单

| 文档 | 责任人 | 状态 |
|------|--------|------|
| 系统架构设计文档 | Tech Lead | 必须 |
| 数据库设计文档 | 后端 | 必须 |
| API接口文档 | 后端 | 必须（自动生成） |
| 部署文档 | DevOps | 必须 |
| 测试方案 | 测试 | 必须 |
| 准确率测试报告 | 测试 | 必须 |
| 用户使用手册 | 前端 | 必须 |
| 设计实现方案 | Tech Lead | 必须（提交物） |
| 答辩PPT | Tech Lead | 必须（提交物） |
| 演示视频脚本 | 前端 | 必须（提交物） |

### 12.3 ADR（架构决策记录）

对于重大技术决策，必须记录ADR：

```markdown
# ADR-001: 选择FastAPI作为后端框架

## 状态
Accepted

## 背景
需要选择一个高性能的Python Web框架支撑AI能力...

## 决策
采用FastAPI 0.115.x...

## 备选方案
- Flask：轻量但异步支持差
- Django：重量级，不适合微服务

## 后果
- ✅ 优点：自动OpenAPI、异步支持、类型注解
- ⚠️ 缺点：生态相对Django较小

## 参考
- https://fastapi.tiangolo.com
```

---

## 第十三部分 里程碑与交付物

### 13.1 项目里程碑（建议8周）

| 阶段 | 周次 | 核心目标 | 交付物 |
|------|------|---------|--------|
| **M1** 启动与架构 | 第1周 | 完成项目初始化、架构设计、环境搭建 | 架构文档、Git仓库、Docker环境 |
| **M2** 数据采集 | 第2周 | 完成爬虫开发、采集1000+岗位 | 原始数据集、ETL管道 |
| **M3** 图谱构建** | 第3周 | 完成NER、关系抽取、Neo4j入库 | 知识图谱（V1.0） |
| **M4** 智能层 | 第4周 | RAG链路、大模型集成、幻觉防控 | AI推理服务 |
| **M5** 人岗匹配 | 第5周 | 简历解析、匹配算法、差距分析 | 匹配服务 |
| **M6** 前端开发** | 第6周 | 图谱可视化、简历上传、看板 | 前端应用（V1.0） |
| **M7** 集成测试 | 第7周 | 端到端测试、准确率达标 | 测试报告 |
| **M8** 提交** | 第8周 | 部署、文档、答辩 | 完整提交物 |

### 13.2 提交物清单

```
📦 提交物
├── 📂 材料文档
│   ├── 作品设计实现方案.pdf        ← 必交（设计文档）
│   ├── 答辩PPT.pdf                ← 必交
│   └── 演示视频.mp4               ← 必交（≤10分钟）
├── 📂 软件模块
│   ├── 源代码/                    ← 必交
│   ├── 可执行程序/
│   ├── Dockerfile                 ← 必交
│   └── 部署说明.md                ← 必交
├── 📂 测试数据
│   ├── 100+岗位JD测试集           ← 必交
│   ├── 1个新岗位能力图谱           ← 必交
│   ├── 1个既有岗位能力图谱         ← 必交
│   └── 数据源（含输入输出示例）   ← 必交
└── 📂 测试报告
    ├── 单元测试报告（覆盖率≥60%）  ← 必交
    ├── JD解析准确率报告（≥90%）   ← 必交
    ├── 简历提取准确率报告（≥90%） ← 必交
    └── 匹配准确率报告（≥90%）      ← 必交
```

### 13.3 准确率验收标准

```
✅ JD解析准确率    ≥ 90%  （100条测试集）
✅ 简历提取准确率  ≥ 90%  （50份简历样本）
✅ 匹配准确率      ≥ 90%  （100条匹配用例）
✅ 单元测试覆盖率  ≥ 60%
```

---

## 第十四部分 风险管控与质量保障

### 14.1 风险登记册

| 风险项 | 等级 | 概率 | 影响 | 应对措施 |
|--------|------|------|------|---------|
| 爬虫被反爬封禁 | 高 | 高 | 数据采集停滞 | 多代理池 + 备用数据源 |
| 大模型API不稳定 | 中 | 中 | 核心功能失效 | 多模型fallback + 本地备份模型 |
| Neo4j性能瓶颈 | 中 | 中 | 图谱查询慢 | 索引优化 + 缓存层 |
| 准确率不达标 | 高 | 中 | 失去竞争优势 | 早介入 + 持续评测 |
| 团队成员变动 | 中 | 低 | 进度延期 | 文档完善 + 代码评审 |
| 数据采集合规风险 | 中 | 低 | 法律风险 | 仅采集公开数据 + robots.txt |

### 14.2 质量保障措施

#### 每日质量检查
- [ ] CI流水线全部通过
- [ ] 代码审查完成
- [ ] 测试覆盖率不下降
- [ ] 阻塞Bug清零

#### 每周质量检查
- [ ] 准确率指标趋势分析
- [ ] 性能基准测试
- [ ] 文档更新同步
- [ ] 风险评审

#### 里程碑质量门
- [ ] 所有P0功能完成
- [ ] 所有硬性指标达标
- [ ] 演示视频录制完成
- [ ] 部署文档完整

### 14.3 代码冻结规则

| 阶段 | 冻结时间 | 允许变更 |
|------|---------|---------|
| 集成测试前1周 | 冻结 | 仅Bugfix |
| 提交前3天 | 严格冻结 | 仅Critical Bugfix |
| 提交当天 | 完全冻结 | 禁止任何变更 |

---

## 附录A 常用命令速查

### A.1 Git 命令

```bash
# 创建功能分支
git checkout -b feature/job-discovery

# 提交规范
git add .
git commit -m "feat(graph): 新增岗位发现模块"

# 推送到远程
git push origin feature/job-discovery

# 同步主分支
git checkout develop
git pull origin develop
git checkout feature/job-discovery
git rebase develop
```

### A.2 Docker 命令

```bash
# 一键启动
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f backend

# 重启服务
docker-compose restart backend

# 进入容器
docker-compose exec backend bash

# 停止所有服务
docker-compose down

# 完全清理
docker-compose down -v
```

### A.3 数据库命令

```bash
# MySQL
docker-compose exec mysql mysql -uroot -p tuzhupoju

# Neo4j Browser
# 访问 http://localhost:7474

# Neo4j Cypher
docker-compose exec neo4j cypher-shell -u neo4j -p pass

# Alembic 迁移
docker-compose exec backend alembic upgrade head
```

### A.4 测试命令

```bash
# 后端单元测试
cd backend && pytest tests/unit -v

# 后端覆盖率测试
cd backend && pytest --cov=app --cov-report=html

# 准确率测试
cd backend && pytest tests/accuracy -v

# 前端测试
cd frontend && pnpm test

# 前端构建
cd frontend && pnpm build
```

### A.5 代码质量

```bash
# Python 格式化
cd backend && black app/
cd backend && isort app/

# Python Lint
cd backend && flake8 app/
cd backend && mypy app/

# 前端 Lint
cd frontend && pnpm lint

# 前端格式化
cd frontend && pnpm format
```

---

## 附录B 项目硬性指标清单

### B.1 功能指标（必须全部实现）

- [ ] 新岗位发现与定义
- [ ] 既有岗位能力动态更新
- [ ] 全景图谱可视化（技能点级粒度）
- [ ] 人岗匹配度诊断（简历解析 + 多维度匹配 + 学习路径）

### B.2 性能指标

- [ ] JD解析准确率 ≥ 90%
- [ ] 简历提取准确率 ≥ 90%
- [ ] 匹配准确率 ≥ 90%
- [ ] 单元测试覆盖率 ≥ 60%
- [ ] 文本生成响应时间 ≤ 5秒

### B.3 数据指标

- [ ] 测试数据集 ≥ 100条JD
- [ ] 1个新岗位能力图谱示例
- [ ] 1个既有岗位能力图谱示例
- [ ] 多源异构数据采集（≥3类来源）

### B.4 提交指标

- [ ] 完整源代码
- [ ] 可执行程序
- [ ] Dockerfile部署说明
- [ ] 单元测试用例（覆盖率≥60%）
- [ ] 设计实现方案
- [ ] 答辩PPT
- [ ] 演示视频（≤10分钟）

### B.5 评分维度

| 维度 | 分数 | 目标 |
|------|------|------|
| 作品完整性 | 30分 | 满分 |
| 技术创新性 | 25分 | ≥ 20分 |
| 用户体验 | 15分 | ≥ 12分 |
| 实用价值 | 30分 | 满分 |
| **总分** | **100分** | **≥ 90分（冲金奖）** |

---

## 文档维护

| 版本 | 日期 | 变更 | 作者 |
|------|------|------|------|
| v1.0.0 | 2026-07-16 | 初始版本 | Tech Lead |

> **最后更新**：请通过 `git log docs/` 查看文档变更历史。

---

> 📌 **使用本指南**：
> - 所有团队成员入职第一天必读
> - 每个PR必须符合本规范
> - 任何对本规范的修改须经团队评审
> - 本文档是"宪法"，与代码同等重要