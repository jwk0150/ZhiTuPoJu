# Review Task 3
Base:6edc08f6130649dcd6927d001f42f5afab51819f
Head:32a6f082ee26e1e0251397e014e31e19734829d9

## Commits
32a6f08 feat(frontend): add judge-first home journey page

## Stat
 frontend/css/components.css         |  85 +++++++++++++++++++++++++
 frontend/js/pages/home.js           |  25 ++++++++
 frontend/js/pages/particles-teal.js | 122 ++++++++++++++++++++++++++++++++++++
 frontend/pages/home.html            |  68 ++++++++++++++++++++
 4 files changed, 300 insertions(+)

## Diff
`diff
diff --git a/frontend/css/components.css b/frontend/css/components.css
index af66173..be23bce 100644
--- a/frontend/css/components.css
+++ b/frontend/css/components.css
@@ -247,6 +247,91 @@
   }
 
   .journey-step.is-primary .journey-step-title {
     font-size: 21px;
   }
 }
+
+/* ── Home journey stage + teal particle canvas ── */
+
+.home-stage {
+  position: relative;
+  isolation: isolate;
+  margin: -28px -32px -48px;
+  padding: 36px 32px 56px;
+  min-height: calc(100vh - var(--topbar-h) - var(--demo-h));
+  background:
+    radial-gradient(ellipse at 18% 0%, rgba(45, 212, 191, 0.2), transparent 46%),
+    radial-gradient(ellipse at 88% 18%, rgba(13, 148, 136, 0.16), transparent 42%),
+    linear-gradient(180deg, #071412 0%, #0B1220 52%, #0a1f1c 100%);
+}
+
+.home-stage.is-static {
+  background:
+    radial-gradient(ellipse at 30% 10%, rgba(45, 212, 191, 0.22), transparent 48%),
+    linear-gradient(180deg, #042f2e 0%, #0B1220 55%, #0a1f1c 100%);
+}
+
+.home-particles {
+  position: absolute;
+  inset: 0;
+  width: 100%;
+  height: 100%;
+  pointer-events: none;
+  z-index: 0;
+}
+
+.home-journey {
+  position: relative;
+  z-index: 1;
+  max-width: 760px;
+}
+
+.home-lead {
+  margin-bottom: 28px;
+}
+
+.home-lead-kicker {
+  font-family: var(--font-mono);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: 1.6px;
+  text-transform: uppercase;
+  color: var(--signal);
+  margin-bottom: 8px;
+}
+
+.home-lead-title {
+  font-family: var(--font-display);
+  font-size: 28px;
+  font-weight: 700;
+  color: #F8FAFC;
+  letter-spacing: 0.02em;
+  line-height: 1.35;
+  margin-bottom: 8px;
+}
+
+.home-lead-desc {
+  font-size: 14px;
+  line-height: 1.65;
+  color: rgba(226, 232, 240, 0.78);
+  max-width: 560px;
+}
+
+.home-stage .journey-step {
+  background: rgba(255, 255, 255, 0.96);
+}
+
+.home-stage .journey-step.is-primary {
+  background: linear-gradient(135deg, #fff 0%, rgba(45, 212, 191, 0.1) 100%);
+}
+
+@media (max-width: 900px) {
+  .home-stage {
+    margin: -20px -16px -36px;
+    padding: 24px 16px 40px;
+  }
+
+  .home-lead-title {
+    font-size: 22px;
+  }
+}
diff --git a/frontend/js/pages/home.js b/frontend/js/pages/home.js
new file mode 100644
index 0000000..f7c97d4
--- /dev/null
+++ b/frontend/js/pages/home.js
@@ -0,0 +1,25 @@
+(function () {
+  if (!window.Shell || typeof window.Shell.mount !== 'function') {
+    return;
+  }
+
+  window.Shell.mount({
+    pageId: 'home',
+    title: '演示路径',
+    subtitle: '数据 → 图谱 → 匹配'
+  });
+
+  var hrefs = window.PAGE_HREF || {};
+  var nodes = document.querySelectorAll('[data-page-href]');
+  for (var i = 0; i < nodes.length; i++) {
+    var key = nodes[i].getAttribute('data-page-href');
+    if (key && hrefs[key]) {
+      nodes[i].setAttribute('href', hrefs[key]);
+    }
+  }
+
+  var canvas = document.getElementById('home-particles');
+  if (canvas && window.TealParticles) {
+    window.TealParticles.mount(canvas);
+  }
+})();
diff --git a/frontend/js/pages/particles-teal.js b/frontend/js/pages/particles-teal.js
new file mode 100644
index 0000000..278e82c
--- /dev/null
+++ b/frontend/js/pages/particles-teal.js
@@ -0,0 +1,122 @@
+(function () {
+  var COUNT = 180;
+  var LINK_DIST = 92;
+  var PALETTE = ['#2DD4BF', '#0D9488', '#5EEAD4', '#99F6E4', '#14B8A6'];
+
+  function prefersReducedMotion() {
+    return window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
+  }
+
+  function mount(canvas, options) {
+    options = options || {};
+    if (!canvas || !canvas.getContext) return { destroy: function () {} };
+
+    var stage = canvas.closest('.home-stage');
+    if (prefersReducedMotion()) {
+      if (stage) stage.classList.add('is-static');
+      canvas.style.display = 'none';
+      return { destroy: function () {} };
+    }
+
+    var ctx = canvas.getContext('2d');
+    var particles = [];
+    var raf = 0;
+    var running = true;
+    var dpr = 1;
+    var width = 0;
+    var height = 0;
+    var count = options.count || COUNT;
+
+    function resize() {
+      var rect = canvas.getBoundingClientRect();
+      dpr = Math.min(window.devicePixelRatio || 1, 2);
+      width = Math.max(1, rect.width);
+      height = Math.max(1, rect.height);
+      canvas.width = Math.floor(width * dpr);
+      canvas.height = Math.floor(height * dpr);
+      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
+    }
+
+    function seed() {
+      particles = [];
+      var n = Math.max(120, Math.min(250, count));
+      for (var i = 0; i < n; i++) {
+        particles.push({
+          x: Math.random() * width,
+          y: Math.random() * height,
+          vx: (Math.random() - 0.5) * 0.28,
+          vy: (Math.random() - 0.5) * 0.28,
+          r: Math.random() * 1.6 + 0.5,
+          color: PALETTE[i % PALETTE.length],
+          a: Math.random() * 0.45 + 0.25
+        });
+      }
+    }
+
+    function step() {
+      if (!running) return;
+      ctx.clearRect(0, 0, width, height);
+
+      var i, j, p, q, dx, dy, dist;
+      for (i = 0; i < particles.length; i++) {
+        p = particles[i];
+        p.x += p.vx;
+        p.y += p.vy;
+        if (p.x < 0 || p.x > width) p.vx *= -1;
+        if (p.y < 0 || p.y > height) p.vy *= -1;
+      }
+
+      ctx.lineWidth = 0.6;
+      for (i = 0; i < particles.length; i++) {
+        p = particles[i];
+        var links = 0;
+        for (j = i + 1; j < particles.length && links < 4; j++) {
+          q = particles[j];
+          dx = p.x - q.x;
+          dy = p.y - q.y;
+          dist = Math.sqrt(dx * dx + dy * dy);
+          if (dist < LINK_DIST) {
+            ctx.strokeStyle = 'rgba(45, 212, 191,' + (0.14 * (1 - dist / LINK_DIST)) + ')';
+            ctx.beginPath();
+            ctx.moveTo(p.x, p.y);
+            ctx.lineTo(q.x, q.y);
+            ctx.stroke();
+            links++;
+          }
+        }
+      }
+
+      for (i = 0; i < particles.length; i++) {
+        p = particles[i];
+        ctx.globalAlpha = p.a;
+        ctx.fillStyle = p.color;
+        ctx.beginPath();
+        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
+        ctx.fill();
+      }
+      ctx.globalAlpha = 1;
+
+      raf = window.requestAnimationFrame(step);
+    }
+
+    function onResize() {
+      resize();
+      seed();
+    }
+
+    resize();
+    seed();
+    raf = window.requestAnimationFrame(step);
+    window.addEventListener('resize', onResize);
+
+    return {
+      destroy: function () {
+        running = false;
+        window.cancelAnimationFrame(raf);
+        window.removeEventListener('resize', onResize);
+      }
+    };
+  }
+
+  window.TealParticles = { mount: mount, COUNT: COUNT };
+})();
diff --git a/frontend/pages/home.html b/frontend/pages/home.html
new file mode 100644
index 0000000..a8d705c
--- /dev/null
+++ b/frontend/pages/home.html
@@ -0,0 +1,68 @@
+<!DOCTYPE html>
+<html lang="zh-CN">
+<head>
+  <meta charset="UTF-8" />
+  <meta name="viewport" content="width=device-width, initial-scale=1" />
+  <title>执图破局 · 演示路径</title>
+  <link rel="stylesheet" href="../css/tokens.css" />
+  <link rel="stylesheet" href="../css/shell.css" />
+  <link rel="stylesheet" href="../css/components.css" />
+</head>
+<body data-page="home">
+  <div id="app-shell"></div>
+  <main class="page-main" id="page-main">
+    <div class="home-stage">
+      <canvas class="home-particles" id="home-particles" aria-hidden="true"></canvas>
+      <div class="home-journey">
+        <div class="home-lead">
+          <div class="home-lead-kicker">Judge path</div>
+          <h1 class="home-lead-title">先走通一条演示路径</h1>
+          <p class="home-lead-desc">数据沉淀为图谱，图谱落到地图与演化，最后用人岗匹配收束结论。不堆运维指标，只讲评委要看的故事。</p>
+        </div>
+
+        <section class="journey-step" data-step="1">
+          <div class="journey-step-num">01 · 底座</div>
+          <h2 class="journey-step-title">数据与图谱底座</h2>
+          <p class="journey-step-desc">多源异构数据驱动岗位能力图谱。招聘、培训与评价信号经采集与质检后，沉淀为可检索、可演化的能力节点与岗位关系。</p>
+          <div class="journey-step-actions">
+            <a class="btn-ghost" data-page-href="collection" href="../portal.html#view-collection">采集与质量</a>
+            <a class="btn-ghost" data-page-href="quality" href="../portal.html#view-quality">质量监控</a>
+          </div>
+        </section>
+
+        <section class="journey-step" data-step="2">
+          <div class="journey-step-num">02 · 地图</div>
+          <h2 class="journey-step-title">看地图</h2>
+          <p class="journey-step-desc">进入数字人才地图，按区域与岗位热度读供需格局，把图谱铺到地理空间上。</p>
+          <div class="journey-step-actions">
+            <a class="btn btn-primary" data-page-href="map" href="../portal.html#view-map">进入地图</a>
+          </div>
+        </section>
+
+        <section class="journey-step" data-step="3">
+          <div class="journey-step-num">03 · 演化</div>
+          <h2 class="journey-step-title">看演化与发现</h2>
+          <p class="journey-step-desc">沿能力演化轨迹看岗位变迁，并发现尚未被目录覆盖的新岗位信号。</p>
+          <div class="journey-step-actions">
+            <a class="btn" data-page-href="evolution" href="../portal.html#view-evolution">岗位能力演化</a>
+            <a class="btn" data-page-href="discovery" href="../portal.html#view-discovery">新岗位发现</a>
+          </div>
+        </section>
+
+        <section class="journey-step is-primary" data-step="4">
+          <div class="journey-step-num">04 · 匹配</div>
+          <h2 class="journey-step-title">做人岗匹配</h2>
+          <p class="journey-step-desc">把能力图谱落到具体人岗诊断，给出缺口与补齐路径。这是演示路径的收束动作。</p>
+          <div class="journey-step-actions">
+            <a class="btn btn-primary btn-lg" data-page-href="match" href="../portal.html#view-match">开始人岗匹配</a>
+          </div>
+        </section>
+      </div>
+    </div>
+  </main>
+  <script src="../js/api.js"></script>
+  <script src="../js/shell.js"></script>
+  <script src="../js/pages/particles-teal.js"></script>
+  <script src="../js/pages/home.js"></script>
+</body>
+</html>

`
